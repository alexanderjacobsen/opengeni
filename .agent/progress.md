# Rigs program — progress log (append-only)

Read `.agent/implementation-dossier.md` (binding) + `.agent/human-scope-brief.md` before any phase.
Research evidence: `.agent/research/rig-runtime-final.md`, `.agent/research/rename-packs-final.md`.
Rules for all implementers: commit incrementally after each verified step; NEVER `git reset`/`git checkout` on this branch; report commit SHAs; `bun run check` green before declaring a milestone done.

## 2026-07-08
- Worktree created: `feat/rigs` off origin/main @ `6203d1b`. Dossier + scope brief written and confirmed. Full autonomy granted (owner: Fable session): implement 100%, verify all, merge, staging, live proof. Milestones M0–M6 tracked as tasks #1–#7.
- M0 baseline (`bun install --frozen-lockfile && bun run check`) started in background.
- M0 first pass RED (24 fail) — diagnosed as shared-test-pg contention (isolated rerun of failing files: 45/45 pass in 4s; main CI green). NOT a code problem. Mitigation going forward: distrust scattered `db unreachable` failures during busy periods; rerun failing files in isolation to distinguish real breakage from contention before acting.
- Worktree fast-forwarded feat/rigs 6203d1b → 06cdc60 (latest main; gained workspace-memory V1 #313 incl. new MCP tools — rebase before M1 avoids mcp/server.ts conflicts). Zero commits lost (branch had none).
- M0 retry (bun run check) running.
- **M0 COMPLETE (green baseline on 06cdc60)**: typecheck+builds green; unit suite shows 21 machine-local flakes (3 files: machine-home-backend, sandbox-shared-and-viewer, toolspace-surface) that ALL pass in isolation (twice verified) and are green in CI on the identical SHA. **Flake protocol for every milestone gate**: full `bun test` → rerun failing files in isolation → isolated-green + no NEW failures in areas the milestone touched = pass; anything failing in isolation = real, fix before proceeding.
- M1 launched on codex (session 019f4084-5d32-7463-89d1-aa88238baab6, xhigh, workdir = this worktree). Spec: .agent/briefs/M1-rename.md. Resume via `codex exec resume 019f4084-5d32-7463-89d1-aa88238baab6 "<prompt>"` if it dies.
- INCIDENT: root disk hit 0 free (other sessions' scratch: 7.4G+4G) → codex M1 died on ENOSPC mid-work, uncommitted tree intact, no commits lost (there were none). Freed own scratch; ALL big logs now under .agent/logs/ in-worktree (tmp task-output files can be deleted by other sessions' startup cleanup — observed). Resumed session 019f4084-5d32 with orient→checkpoint-commit-first instruction. NOTE: resumed turn runs at reasoning effort LOW (codex resume quirk); acceptable for mechanical rename bulk — output will be judged at gates, weak parts redone at xhigh.
- M1 gates: check ran (22 fail = known trio [isolation-green re-verified on M1 code] + 1 REAL stale SDK test — fixed by owner: canonical variable-set paths test + deprecated-wrapper delegation test, committed). Migration renumbered 0045→0046 (main took 0045 for workspace-memory); rigs migration becomes 0047. check:rename + check:docs-refs PASS. test:integration running detached.
- INCIDENT 2: codex OAuth token revoked mid-M1 (token_revoked → 401; killed the resume session AFTER it committed everything + its own verification was already 90% done). No local re-auth tooling (interactive device flow only). DECISION: grunt work → opus subagents until codex auth returns; Jørgen notified non-blockingly.
- M1 integration tier: fixed api.integration (agent, 092d510), db.integration RLS (owner), workspace-isolation msg (owner). Then isolation-checking caught a 4th rename-affected file the agent's api.integration scope missed: worker-activity.integration.ts — 5 REAL failures (stale `environmentId` passed to CANONICAL-ONLY low-level DB createSession/setVariableSetVariable → field dropped → session's variable_set_id NULL → variable set never attached → secret not redacted). Source confirmed CLEAN (setVariableSetVariable maps variableSetId correctly; mapSession dual-keys OUTPUT; DB INPUT is canonical-only). Delegated the ~8-site mechanical fix to sonnet (m1-worker-test-fix) with full diagnosis + instruction to STOP+report if a real source regression, not paper over. DOCTRINE CONFIRMED: full-tier `&&` chain hid file-2..N behind file-1 flakes; must isolation-run EACH rename-touched integration file (grep concept-refs to know which) — a real 5-failure regression was masked as "contention".
- **M2 verified by owner (independent of agent's missing report)**: 5 commits (819a1c9 db/migration/contracts, ce53009 domain+REST, 5f570a7 sdk+react, 19ce485 tests, 5b06b44 typecheck fix). Migration 0047 correct: 3 tables + 3 RLS policies, partial-unique `(rig_id) WHERE active` (single-active enforced in DB), unique (rig_id,version), all 4 M3 wiring cols present. DB tests 8/0 real (51s against pg) covering the load-bearing invariants: monotonic-under-concurrency, concurrent-activation→exactly-one-active, content-immutability, guarded transitions, delete-guard, cross-workspace RLS isolation. Contract 17/0, routes 3/0. M2 = COMPLETE (final consolidated check pending worker-activity settle).
- **M1 COMPLETE**: worker-activity rename fix committed b8f8d4a; the 5 original rename-failures now PASS. Remaining worker-activity failures (13) are ALL 5000ms timeouts from EXTERNAL CPU contention (load avg 30 on 20 cores — a separate peloton test workload in ~/repos/.worktrees/peloton hammering the box), NOT rename defects — they're non-rename infra tests (repo-clone-hook, MCP-tool-exec, model-usage). M1 rename correctness fully established (api 78/0, db fixed, isolation fixed, worker-activity 5 target tests green). Final clean full-tier confirmation deferred to M6 check:full under controlled load.
- **M2 COMPLETE** (verified above). Launching M3 (runtime binding) on opus — bulk is code-writing (contention-immune); Fable reviews M3 diff personally (hot turn path). Note: M3/M4 test-verification may hit contention timeouts; isolation-verify + diff-review compensate.
- Codex auth RECOVERED (~13:10) — AUTH-OK. Route M4 grunt work back to codex (spares Claude pool per economics doctrine). M3 already running on opus (hot-path; Fable reviews diff personally) — let it finish. M4 launches on codex after M3 lands + is reviewed. Critical path M3→M4→M5→M6 is sequential (each depends on prior); M5 UI needs M3 binding + M4 promotion endpoints to be meaningful.
- **M3 CODE + TESTS COMPLETE (opus)**. Commits: `7bf730e` (foundation: contracts+SDK rigId on session/scheduled-task, db persist+map, getWorkspaceDefaultRigId/listDistinctRigVersionIdsInGroup/getRigName, migration 0048 sandbox_leases.rig_version_id, config rigSetupTimeoutMs, settingsWithRigImage, core createSessionForRequest resolve+freeze+sharing-gate, scheduled-task store+per-fire resolve), `f7626a4` (image precedence outermost, env-layer rig-default merge session-wins, lease rig stamp+conflict+solo-reset+SandboxRigConflictError), `b0b87e1` (rig-setup hook marker/timeout/events/fail-closed, credential-hook union+validate, non-bypassable doctrine, telemetry attrs), `d6b0e3d` (unit tests), `997ef69` (integration: 5 lease-conflict + 10 create/gate + env-merge extraction), `0e09ecc` (docker e2e). Typecheck GREEN across all 18 packages. Isolation-verified GREEN: runtime.test 129/0, sdk-parity 14/0, contracts 60/0, config 56/0, packs 15/0, rig-runtime 12/0, sandbox-leases 21/0 (incl. 5 new rig + migration 0048), worker-activity.integration 64/0 (**rig-less turn path unchanged — prime-directive proof**), db.integration 49/0, api.integration 78/0, sandbox-shared-and-viewer 19/0 (env-gate unbroken by rig-gate additions), rig-session-binding 10/0. E2e written (docker, scripted-model, not run locally — heavy; orchestrator runs final gate). **FLAG for Fable: found an M1 regression (commit `1ff7d0b`) — agent-turn passes buildAgent option key `workspaceVariableSet` but BuildAgentOptions/buildAgent read `workspaceEnvironment`; because it's spread (not a literal) TS doesn't catch it, so the workspace variable-set instructions block is silently dropped for every session with an attached variable set. NOT fixed in M3 (out of scope, changes non-rig behavior, risks M1 gates); rig doctrine threaded independently+correctly. Recommend a separate M1 fixup.** Deferred to M4 (per boundaries): MCP rig tools, verification workflow, auto-merge, Temporal.
- M1 DEFINITIVELY green: worker-test-fix agent got clean 64 pass/0 fail on worker-activity in a low-load window (my 13-fail run was contention; load had hit 83). Agent also fixed 2 extra stale error-string assertions (2523 "variable set not found", 2709 "scheduled task variableSet attachment...") — grep-confirmed canonical renames, source untouched. M1+M2 both independently confirmed complete. M3 running on opus.
- **M3 REVIEW (personal spot-check + codex adversarial pass)**. My checks: prime-directive gating, setup-hook fail-closed-on-nonzero, selfhosted exclusion (runtime line 2528 guard confirmed), image precedence, env-merge order, lease-conflict shape — all correct. Codex (high-effort, verdict saved .agent/evidence/codex-m3-review-verdict.md) found 6; my adjudication:
  - #1 rig-less variable-set instruction regression → ALREADY FIXED dec2521. ✓
  - #2 HIGH REAL: rig-setup timeout is a YIELD not a hard kill — process keeps running in box after turn fails; on later rc=0 it touches the marker → next turn SKIPS setup the platform declared failed. NOT fail-closed. Fix: wrap script in coreutils `timeout ${s}` so timeout KILLS it (rc!=0, marker unwritten).
  - #3 HIGH REAL (narrow): marker is check-then-touch, not atomic → two sessions cold-hitting one shared box both run setup ("once per box" violated on multi-holder). Fix: atomic mkdir-lockdir claim.
  - #4 HIGH REAL (limited): sandbox:"shared" gate only compares to PARENT, not all group members (the {groupId} path correctly checks all). Fix: shared path also checks all members.
  - #5 INVESTIGATE: rig default variable-set VALUES reloaded live each turn (only IDs frozen) → operator value-rotation could trip fatal manifest-drift. MUST verify if values ride manifest-env or the off-manifest injected env (like git token) before deciding real vs non-issue — same question applies to existing session variable sets.
  - #6 MEDIUM REAL vs spec: emits sandbox.operation.* w/ payload.name="rig-setup", not dedicated rig.setup.* event types. Fix for M5 observability.
- #5 ADJUDICATED = NON-ISSUE (personal investigation): variable-set VALUES ride the manifest via stableSandboxEnvironmentForRun, and M3 merges rig-default values into the IDENTICAL workspaceEnvironment values map that session variable sets use → rotation-drift behavior is identical for both, an established variable-set-system property (handled by pinProvidedSessionManifestEnvironment), NOT an M3 regression. Out of scope. M3-hardening scope = findings #2,#3,#4,#6. Brief: .agent/briefs/M3-hardening.md. Delegating to codex (found them, separate pool); Fable reviews fix diff.
- #5 ruling CONFIRMED by direct read (agent-turn.ts:1236-1248): rig defaults merged into sandboxWorkspaceEnvironmentValues, passed in the SAME workspaceEnvironment arg position of sandboxEnvironmentForRun as session variable sets → identical pinned path, identical drift handling; rig-less passes exactly workspaceVariableSet?.values (byte-for-byte). Non-issue confirmed.
- **FABLE HOT-PATH SIGN-OFF on M3**: prime directive held; setup-hook fail-closed-on-nonzero + selfhosted exclusion (runtime:2528 guard) confirmed; image precedence + env-merge + lease-conflict correct. 4 real gaps (timeout-not-hard, marker-race, shared-gate-parent-only, generic-events) being fixed by codex per M3-hardening brief. M3 core is sound; hardening closes the edges. After hardening review → M4.
- **M3 HARDENING COMPLETE (codex)**. Commits: `bec523a` (hard-kill rig setup via in-box coreutils `timeout`, atomic mkdir lockdir claim, dedicated `rig.setup.*` runtime events + contract/SDK event mirrors), `385406d` (`sandbox:"shared"` rig gate now checks all group members via `listDistinctRigVersionIdsInGroup`, with mixed-rig regression test), `d2d7692` (web rig contract fixture/permission mirror fixes needed for typecheck). Finding #5 intentionally untouched per orchestrator ruling. Verification: `bun run typecheck` green; focused acceptance set green (`bun test packages/runtime/test/rig-runtime.test.ts apps/api/test/rig-session-binding.test.ts packages/db/test/rigs.test.ts packages/db/test/sandbox-leases.test.ts apps/worker/test/packs.test.ts apps/worker/test/agent-turn.test.ts` → 110 pass / 0 fail); known flaky trio plus web expectation isolation green (`machine-home-backend`, `sandbox-shared-and-viewer`, `toolspace-surface`, `apps/web/src/App.test.ts` → 116 pass / 0 fail / 1 skip). Full `bun test` under load: 2380 pass / 4 skip / 31 fail; failures matched shared-test-db/contention behavior in the known flaky files and were isolation-green as above.
- **M3 HARDENING VERIFIED + M3 COMPLETE**. Codex (session 019f41c5) fixed all 4 (commits bec523a timeout+marker, 385406d shared-gate, d2d7692 web fixtures, 9fbfac9 record). Fable personally verified each: #1 hard-kill via `timeout -k 5s ${secs}s bash` (kill→nonzero→marker unwritten, fail-closed); #2 atomic `mkdir` lockdir + EXIT-trap + loser-wait where lockWaitSecs(timeout+6) > winner max(timeout+5) so no steal from a live winner — timing correct; #3 shared path now uses listDistinctRigVersionIdsInGroup (all members, 422); #4 dedicated rig.setup.* event types added. Confirm runs: rig-runtime+contracts 20/0, sharing-gate+session-binding+shared-viewer 30/0. Launching M4 on codex (fresh, brief .agent/briefs/M4-verification-promotion.md).
- **M4 PARTIAL IMPLEMENTATION (codex)**. Commits: `14e1527` (rigVerificationWorkflow/activity, clean throwaway sandbox with no env, setup_append auto-promote, API triggers, MCP rig tools, default first-party `rigs:use` but not `rigs:manage`, unit test), `7b9b04f` (route test workflow-client stub), `6690934` (definition_edit verification replays candidate setup/checks). Verification: `bun run typecheck` green before commits; focused `bun test packages/core/test/rig-ci.test.ts apps/api/test/rigs-routes.test.ts` green twice (latest 10 pass / 0 fail / 30 expects); `cd apps/worker && bun run typecheck` green after candidate replay fix. Full `bun run check` under load was not a valid gate: unit phase hit `desktop-image.test.ts` hook timeout, known flaky `machine-home-backend`/`sandbox-shared-and-viewer`/`toolspace-surface`, and shared DB connection-ended fallout; interrupted after failures cascaded. Deviations remaining for M4 acceptance: optional reverify Schedule not implemented; docker A10/A11 e2e not added/run; MCP fake-server tests not added; no full integration/e2e green evidence yet.
- **M4 CODE reviewed by Fable = sound**: verifyRigChange secret-free (environment:{}) + terminateThrowaway in finally; auto-merge matrix correct (!passed→rejected, passed+setup_append→auto_promote, definition_edit→explicit rigs:manage promote requiring verification.passed). BUT codex DEFERRED the required proofs: A10/A11 docker e2e + MCP tests NOT added, no green gate. NOT acceptable as done. Resumed codex M4 (019f41ef) to add A10 (auto-merge mints version) + A11 (poisoned change REJECTED from clean replay — the thesis proof) following test/e2e/rig-setup.e2e.ts, + MCP fake-server tests, and RUN them (isolation if load blocks). Reverify Schedule deprioritized (optional). M4 stays in_progress until proofs exist + pass.
- **M4 PROOF TESTS COMPLETE (codex)**. Commit `78ed40b` adds `test/e2e/rig-verification.e2e.ts` (A10 + A11 real Docker clean-replay verification) and `apps/api/test/rigs-mcp.test.ts` (rig_list/rig_get under rigs:use, rig_propose_change creates a setup_append change + records `session:<id>` + triggers verification, rig_promote absent without rigs:manage). Activity fix in same commit runs rig verification setup/commands/checks as root inside Docker so the M3 rig-setup marker path can create `/var/opengeni`. Verification evidence: `bun test apps/api/test/rigs-mcp.test.ts` → `3 pass / 0 fail / 13 expect() calls / Ran 3 tests across 1 file. [52.69s]`; `bun test ./test/e2e/rig-verification.e2e.ts` → `2 pass / 0 fail / 14 expect() calls / Ran 2 tests across 1 file. [210.73s]`. A11 genuinely exercised a real clean Docker verification box: poisoned command `test -f /tmp/only-in-proposer-box` returned commandResult.exitCode 1 and persisted status `rejected`.
- **M4 COMPLETE + THESIS PROVEN**. Code reviewed sound (secret-free verify + finally-terminate + correct auto-merge matrix). Required proofs added (78ed40b): test/e2e/rig-verification.e2e.ts A10 (real docker: setup_append verifies clean → auto-merges → new active version's setupScript contains the command → file exists on a box from the new version) + A11 (poisoned `test -f /tmp/only-in-proposer-box` REJECTED from clean replay — fidelity tested-not-trusted); apps/api/test/rigs-mcp.test.ts (rig_promote absent+throws without rigs:manage — security gating). **Fable INDEPENDENTLY re-ran the e2e: 2 pass/0 fail in 254s (real boxes), plus MCP 3/0.** Deferred (optional, per decision): default-off reverify Schedule. M4 done.
- **Docs task complete (sonnet, docs/-only agent, `803892a`)**: new `docs/rigs.md` (flat, template-matched to `variable-sets.md`) covering invariants, runtime binding (image precedence rig>pack>deployment, setup hook marker/timeout/selfhosted-exclusion, credential-hook union+validation, rig-default-variable-set layering below the session's own set, non-bypassable agent doctrine text), permissions table, full REST API table, the verification/auto-merge/promote model (with the classification table), MCP surface (rig_promote NOT in the default sandboxed-agent grant), and deletion/rollback semantics (no cascade-detach, activate≠revert-content, a bound session's rig_version_id never migrates). Cross-refs added: `docs/README.md` Canonical Homes row, `docs/architecture.md` §8 paragraph + §13/§14 table rows, `docs/packs.md` (sandboxImage superseded by rig image, not literally deprecated in code — no `@deprecated` marker exists), `docs/variable-sets.md` (rig-default layering note). Deliberately did NOT touch `docs/capabilities.md` — no natural overlap (rig MCP tools are first-party, not capability-catalog items; forcing a link would be noise). `bun run check:docs-refs` → `Docs reference freshness guard passed.` on first run, no fixes needed. One fact worth flagging: there is currently no API or web-console route to set `workspaces.default_rig_id` — it's a schema column consumed only by session-create fallback resolution; documented as a known gap in docs/rigs.md rather than assumed away.
- **M6 BACKEND GATE GREEN**: full `bun run test:integration` (all 9 files: api/db/workspace-isolation/worker-activity/temporal/nats/selfhosted×2/worker-restart) INTEGRATION_EXIT=0, ZERO failures, under controlled low load (7.77). Definitive M0-M4 backend integration proof: rename aliases + RLS isolation + rig domain + runtime binding + verification all integrate against real pg/NATS/Temporal. Remaining M6: web/react check (post-M5), grep sweep, codex review triage, PR, merge, staging, A20 live.
- **BACKEND ADVERSARIAL REVIEW** (codex high-effort, verdict .agent/evidence/codex-backend-review-verdict.md): M1 rename + M2 domain CONFIRMED CLEAN (all aliases work, version insert-only, active-flip serialized via rig row lock, 3 tables FORCE RLS). 5 findings ALL in M4 promotion/verification path: #1 HIGH stale-base promote overwrites newer active; #2 HIGH concurrent promote double-merges (merged→merged mutates); #3 HIGH delete-rig race (SET NULL silent detach); #4 MED re-verify of completed/failed no-ops (workflowId REJECT_DUPLICATE swallowed); #5 MED verification output not scrubbed → secret leak into rig_changes.verification + audit. All real. Brief .agent/briefs/M4-hardening.md. Delegating to codex (found+understands them); touches core/db/worker/api only (no overlap w/ M5's web/react). Fable reviews fix diff.
- **M5 UI COMPLETE (opus)**. Commits: `914c3e8` (rigs list+create page, rig detail with Overview/Setup/Versions/Changes tabs, composer rig picker, hooks + SDK), `dc76fd0` (screenshot harness + review-pass polish). Built: nav entry (`ServerCogIcon`, workspace-nav) + routes `/rigs` + `/rigs/:rigId` (App.tsx); `routes/rigs.tsx` (list cards: name, active vN, image/check-count chips, updated; progressive-disclosure create form) + `routes/rig-detail.tsx` (tabbed, live-polling 5s); feature components `components/rigs/*` (rig-overview with derived health + per-check results + replay log, rig-setup-section whose edit proposes a `definition_edit` change (never a direct mutation), rig-versions-timeline with activate/rollback + confirm, rig-changes-queue with status chips + verification-log viewer + promote, rig-status-chip, rig-definition-fields shared editor, verification-log). Composer rig picker in `sessions-index.tsx` (inline Select, preselects the rig's first default variable set into the existing control). Human-copy boundary `lib/rig-status.ts` (no enum slugs rendered). Hooks: added `useRig`/`useRigVersions`/`useRigChanges` (packages/react) + `SessionClientLike` extend + demo mock; SDK client `verifyRigChange`/`promoteRigChange`/`verifyRig` over M4's REST. `TurnSubmission.rigId` + `SessionDraft.rigId` threaded through session-create → context.startSession → CreateSessionRequest.
  - **Verification: full-stack local screenshot pass.** Real API+web+Postgres/NATS/Temporal booted (local mode = no-auth deterministic "Local" workspace w/ admin), seeded the full state space (2 variable sets; a plain default rig; dev-machine w/ 12-version history + changes in EVERY status incl. verified-awaiting-promote + a rejected change w/ a failing check + verifying + merged + verification-error + fresh proposal; a ci-runner w/ unverified checks). Playwright + nix-store chromium captured 11 states × {1440,800}px → `.agent/evidence/m5-ui/` (git-ignored; 22 PNGs). Permission-denied captured by intercepting `/v1/access/me` to strip rigs perms (real denied-view code path). Empty-list captured by intercepting the list GET → `[]`.
  - **Five review passes (inspection on real screenshots): (1) IA/flow** — list→detail w/ 4 tabs, change→verify→promote loop legible via status chips + verification logs. **(2) layout/composition** — reads native to OpenGeni; FIX: declared-check rows stacked name-over-command to match verification check-result rows. **(3) state coverage** — all 12 states present + good (empty/loading/error/verifying/failed/verification-error/merged/verified/proposed/passing/unknown/permission-denied). **(4) responsive/density** — 800px holds (header actions wrap, info grid stays 2-col, dense 12-version/6-change lists readable, no h-overflow). **(5) polish/copy** — human labels throughout, no enum slugs; FIX: corrected re-verify toast (rig-level verify has no readable result surface, dropped the false "activity log" promise). `cd apps/web && bun run typecheck && bun run build` GREEN after fixes.
  - **Missing-API / gap notes (no backend invented):** (a) the rig LIST payload carries no verification-health summary, so list cards show check-COUNT not pass/fail — a `checkHealth`/`lastVerifiedAt` field on `Rig` would let cards show the dot; overview derives real pass/unknown from the change whose `resultVersionId===activeVersion.id` (can't be "failing" since a version that became active passed). (b) `POST /rigs/:id/verify` (re-verify active version) writes only audit events — no readable result field — so the overview can't reflect a re-verify outcome (toast worded honestly). (c) pre-existing (per docs task): no API/UI to set `workspaces.default_rig_id`; the composer's "Workspace default" relies on server-side fallback only. Harness scripts committed: `scripts/rig-ui-stack.ts` (seed+boot) + `scripts/rig-ui-shots.ts` (playwright). Boundaries honored: web+packages/react+sdk+scripts only; no domain/runtime/verification/mcp/e2e edits.
- **M5 UI COMPLETE + taste-verified by Fable**: build+typecheck green (independently rerun); 22 screenshots (11 states×2vp) in .agent/evidence/m5-ui/. Reviewed list-dense + the failing-change log view personally = excellent: the rejected-change surface makes "fidelity tested not trusted" legible (poisoned pip cmd + per-check exit codes [rg exit0 green / internal-tool exit127 red] + real clean-sandbox replay log). Native to OpenGeni, a11y color+text chips, human-copy boundary.
- **M5 gap notes → 2 are DOSSIER-REQUIRED (must close), sequenced AFTER M4-hardening (they touch core/db/api, would conflict with the running codex)**: #1 dossier §10 wants list cards to show verification pass/fail/unknown DOT but the Rig LIST payload has no health summary → add lastVerification/checkHealth to Rig list response + render dot. #3 dossier §10 says default_rig_id "set via workspace settings API/UI" but only the column+server-fallback exist → add setter API + small UI control. #2 (readable re-verify result) = minor, fold into #1 or defer. Plan: M4-hardening → review → M5-backend-gaps (#1+#3 server) → M5-ui-followup (dot + default-rig control) → M6 final gate/PR/merge/staging/A20.
- **M4 HARDENING COMPLETE (codex, commit pending at entry time)**. Fixed all 5 backend-review findings: promotion now mints+merges in one rig-row-locked CAS tx requiring current active == baseVersionId; generic terminal change transitions now reject even merged→merged/rejected→rejected; delete-rig guard locks the rig row and counts active (non failed/cancelled) sessions before delete; REST/MCP verification triggers allocate per-attempt unique workflow ids, reset failed/rejected changes to verifying, and reject already-verifying; verification command/check output, command text, errors, and audit metadata are scrubbed through existing sanitizers before persistence. Tests added/updated for stale-base 409/no mint, concurrent promote one winner, active-session delete refusal, failed-change reverify attempt-2 + duplicate 409, MCP attempt workflow ids, and real Docker secret redaction in stored verification + audit. Verification evidence:
  - `bun run typecheck` → final tail: `$ tsc --noEmit` ... `$ tsc --noEmit` (exit 0).
  - `bun test packages/core/test/rig-ci.test.ts` → `7 pass / 0 fail / 8 expect() calls / Ran 7 tests across 1 file. [198.00ms]`.
  - `bun test packages/db/test/rigs.test.ts` → `11 pass / 0 fail / 64 expect() calls / Ran 11 tests across 1 file. [6.88s]`.
  - `bun test apps/api/test/rigs-routes.test.ts` → `5 pass / 0 fail / 35 expect() calls / Ran 5 tests across 1 file. [3.45s]`.
  - `bun test apps/api/test/rigs-mcp.test.ts` → `3 pass / 0 fail / 13 expect() calls / Ran 3 tests across 1 file. [2.58s]`.
  - First isolated Docker e2e redaction run caught an unsanitized `command` field carrying `rig-secret-token-123456`; fixed before commit. Final rerun `bun test ./test/e2e/rig-verification.e2e.ts` → `3 pass / 0 fail / 19 expect() calls / Ran 3 tests across 1 file. [55.70s]`.
- M4 hardening commit SHA: `af422e9` (`fix(rig-ci): harden verification promotion races`). Evidence-record commit follows separately because the final SHA cannot be known until after the code commit exists.
- **M4-HARDENING VERIFIED CORRECT (Fable personal review) + COMPLETE**. Commits af422e9 + 5ae286f. All 5: #1 CAS (createRigVersionForChangePromotion locks rig .for("update") in withWorkspaceRls=real db.transaction, checks active==baseVersionId → RigActiveVersionChangedError, before mint — atomic); #2 terminal guard (merged/rejected reject all transitions incl. merged→merged; concurrent promote serializes on lock → one version, loser 409); #3 atomic deleteRigIfNoActiveSessions (lock+count+delete in tx); #4 unique attempt workflow-ids + reset-to-verifying + reject already-verifying; #5 scrub via sanitizeEventString/sanitizeMemoryText/sanitizeEventPayload on output+command+payload before persist+audit (e2e genuinely caught the leak pre-fix). Tests: core 7/0, db 11/0, routes 5/0, mcp 3/0, e2e 3/0(55s). Backend fully hardened.
- Closing 2 DOSSIER-REQUIRED M5 gaps (§10): #1 Rig list health dot (needs list-response verification summary) + #3 default_rig_id setter API/UI. Backend → codex (resume, full context); UI follow → opus; then M6.
- **M5 BACKEND GAPS COMPLETE (codex)**. Commit `08e31a7` adds `Rig.activeVersionHealth` (`{ checkHealth: "passing"|"failing"|"unknown"; lastVerifiedAt: string|null } | null`) to list/get rig responses, derived from merged-change verification records and direct active-version verification audit rows; never-verified active versions report `unknown`. Also exposes `Workspace.defaultRigId`, `SetWorkspaceDefaultRigRequest`, SDK `setWorkspaceDefaultRig`, and `PUT /v1/workspaces/:workspaceId/default-rig` requiring `rigs:manage`, validating workspace-local rig ids, and clearing on `{ rigId: null }`. Verification evidence: `bun run typecheck` → final tail `$ tsc --noEmit` / `$ tsc --noEmit` (exit 0); `bun test packages/db/test/rigs.test.ts` → `12 pass / 0 fail / 68 expect() calls / Ran 12 tests across 1 file. [50.00s]`; `bun test apps/api/test/rigs-routes.test.ts` → `6 pass / 0 fail / 42 expect() calls / Ran 6 tests across 1 file. [50.17s]`; `bun test packages/sdk/test/client-coverage.test.ts` → `32 pass / 0 fail / 106 expect() calls / Ran 32 tests across 1 file. [2.01s]`; `bun test apps/api/test/app.test.ts` → `31 pass / 0 fail / 102 expect() calls / Ran 31 tests across 1 file. [1.68s]`. Boundary note: did not touch `apps/web` or `packages/react`; SDK fields are optional for backwards-compatible existing consumers while server contract responses include them.
- **M5 UI FOLLOW-UP COMPLETE (opus) — dossier §10 health dot + default rig**. Commits `0dcb571` (feat(rigs-ui): list health dot + workspace default-rig control) + `2a20885` (feat(rigs-ui): inline default badge + seed failing-health & default rig).
  - **(1) List health dot**: each Rigs list card now renders the active version's verification health as a StatusDot+label via the existing `RigStatusChip`/`rigCheckHealthView` human-copy boundary, reading the new `Rig.activeVersionHealth.checkHealth` (passing → green "Checks passing", failing → red "Check failing", unknown/missing → neutral "Not verified"). Gated on `active && versionHasChecks(active)` so a no-checks rig (e.g. `default`) shows no health chip rather than a misleading "Not verified" next to "No checks" — matches the overview's taste. Check-count chip kept alongside.
  - **(2) Default rig**: added a rigs:manage "Set as default"/"Clear default" control on the rig detail header (star / star-off), a "Default" badge (star + label, brand-toned) on the matching list card AND detail title row, wired through a new `context.setWorkspaceDefaultRig(workspaceId, rigId|null)` that PUTs `/v1/workspaces/:id/default-rig` via `client.setWorkspaceDefaultRig` and upserts the returned workspace so the badge updates without reload. Reads current default from `context.workspaces.find(id).defaultRigId`.
  - **Bug caught + fixed during screenshot review**: Tailwind preflight sets `svg{display:block}`, and MetaChip nests children in a non-flex `truncate` span, so the badge's star stacked ABOVE the label. Fixed by wrapping badge contents in an `inline-flex items-center` span (both list card + detail header). Verified via crop.
  - **Seed exercised all three health states + default**: extended `scripts/rig-ui-stack.ts` — added `legacy-box` whose active version's most recent re-verify FAILED (seeded a `rig.verification.failed` audit row with `metadata.versionId`, the same derivation path `loadRigHealthByActiveVersion` reads → failing dot), and `setWorkspaceDefaultRig(db, ws, dev.id)` so dev-machine is the default. API confirmed: default=no-checks, dev-machine=passing (+default), ci-runner=unknown, legacy-box=failing.
  - **Verify**: `cd apps/web && bun run typecheck && bun run build` GREEN (both, after the badge fix). Re-captured 1440+800 shots via `scripts/rig-ui-shots.ts` (added `12-detail-default-control` = dev-machine "Clear default" + Default badge, `13-detail-set-default` = ci-runner "Set as default"; `02-list-dense` now waits for all three health labels + Default). Evidence in `.agent/evidence/m5-ui/` (git-ignored). Reviewed personally: dots legible + correctly colored at both viewports, badge inline, no overflow at 800px.
  - **End-to-end proven (not just render)**: drove Playwright — clicking "Set as default" on ci-runner flipped the button to "Clear default", showed the Default badge, and `GET /v1/workspaces` reported `defaultRigId === ci-runner`; clicking "Clear default" reset `defaultRigId` to `null`. Full click→context→SDK PUT→persist→upsert→re-render loop works, incl. explicit clear.
  - Boundaries honored: touched only `apps/web/src/{routes/rigs.tsx,routes/rig-detail.tsx,context.tsx}` + `scripts/rig-ui-{stack,shots}.ts` + `.agent/evidence`. No packages/react hook change needed (context owns the workspace-level mutation, as it does for rename/settings); SDK method + contract types were already added by the backend-gaps pass.
