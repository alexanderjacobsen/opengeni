// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v5.28.3
// source: opengeni_agent.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "opengeni.agent.v1";

export enum ErrorCode {
  ERROR_CODE_UNSPECIFIED = 0,
  /** ERROR_CODE_UNSUPPORTED - The requested op is not implemented / not permitted on this platform. */
  ERROR_CODE_UNSUPPORTED = 1,
  /** ERROR_CODE_OS - A filesystem/exec/git op failed at the OS layer (see `detail`). */
  ERROR_CODE_OS = 2,
  /** ERROR_CODE_NOT_FOUND - The path/ref/target referenced by the op does not exist. */
  ERROR_CODE_NOT_FOUND = 3,
  /** ERROR_CODE_CONSENT_REQUIRED - The op was rejected by consent/capability gating (e.g. screen-control off). */
  ERROR_CODE_CONSENT_REQUIRED = 4,
  /** ERROR_CODE_TIMEOUT - The op timed out inside the agent. */
  ERROR_CODE_TIMEOUT = 5,
  /** ERROR_CODE_DRAINING - The agent is shutting down / draining and cannot accept new work. */
  ERROR_CODE_DRAINING = 6,
  /** ERROR_CODE_PROTOCOL - A protocol-level violation (bad envelope, unknown op for this version). */
  ERROR_CODE_PROTOCOL = 7,
  /** ERROR_CODE_STREAM - A required stream channel could not be opened on the relay. */
  ERROR_CODE_STREAM = 8,
  /** ERROR_CODE_AGENT_OFFLINE - The control plane synthesized this because no agent responded (offline). */
  ERROR_CODE_AGENT_OFFLINE = 9,
  /** ERROR_CODE_FENCED - An epoch fence rejected a stale op; caller should re-resolve and retry. */
  ERROR_CODE_FENCED = 10,
}

export function errorCodeFromJSON(object: any): ErrorCode {
  switch (object) {
    case 0:
    case "ERROR_CODE_UNSPECIFIED":
      return ErrorCode.ERROR_CODE_UNSPECIFIED;
    case 1:
    case "ERROR_CODE_UNSUPPORTED":
      return ErrorCode.ERROR_CODE_UNSUPPORTED;
    case 2:
    case "ERROR_CODE_OS":
      return ErrorCode.ERROR_CODE_OS;
    case 3:
    case "ERROR_CODE_NOT_FOUND":
      return ErrorCode.ERROR_CODE_NOT_FOUND;
    case 4:
    case "ERROR_CODE_CONSENT_REQUIRED":
      return ErrorCode.ERROR_CODE_CONSENT_REQUIRED;
    case 5:
    case "ERROR_CODE_TIMEOUT":
      return ErrorCode.ERROR_CODE_TIMEOUT;
    case 6:
    case "ERROR_CODE_DRAINING":
      return ErrorCode.ERROR_CODE_DRAINING;
    case 7:
    case "ERROR_CODE_PROTOCOL":
      return ErrorCode.ERROR_CODE_PROTOCOL;
    case 8:
    case "ERROR_CODE_STREAM":
      return ErrorCode.ERROR_CODE_STREAM;
    case 9:
    case "ERROR_CODE_AGENT_OFFLINE":
      return ErrorCode.ERROR_CODE_AGENT_OFFLINE;
    case 10:
    case "ERROR_CODE_FENCED":
      return ErrorCode.ERROR_CODE_FENCED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ErrorCode");
  }
}

export function errorCodeToJSON(object: ErrorCode): string {
  switch (object) {
    case ErrorCode.ERROR_CODE_UNSPECIFIED:
      return "ERROR_CODE_UNSPECIFIED";
    case ErrorCode.ERROR_CODE_UNSUPPORTED:
      return "ERROR_CODE_UNSUPPORTED";
    case ErrorCode.ERROR_CODE_OS:
      return "ERROR_CODE_OS";
    case ErrorCode.ERROR_CODE_NOT_FOUND:
      return "ERROR_CODE_NOT_FOUND";
    case ErrorCode.ERROR_CODE_CONSENT_REQUIRED:
      return "ERROR_CODE_CONSENT_REQUIRED";
    case ErrorCode.ERROR_CODE_TIMEOUT:
      return "ERROR_CODE_TIMEOUT";
    case ErrorCode.ERROR_CODE_DRAINING:
      return "ERROR_CODE_DRAINING";
    case ErrorCode.ERROR_CODE_PROTOCOL:
      return "ERROR_CODE_PROTOCOL";
    case ErrorCode.ERROR_CODE_STREAM:
      return "ERROR_CODE_STREAM";
    case ErrorCode.ERROR_CODE_AGENT_OFFLINE:
      return "ERROR_CODE_AGENT_OFFLINE";
    case ErrorCode.ERROR_CODE_FENCED:
      return "ERROR_CODE_FENCED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ErrorCode");
  }
}

/** The operating system family the agent runs on. */
export enum Os {
  OS_UNSPECIFIED = 0,
  OS_LINUX = 1,
  OS_MACOS = 2,
  OS_WINDOWS = 3,
}

export function osFromJSON(object: any): Os {
  switch (object) {
    case 0:
    case "OS_UNSPECIFIED":
      return Os.OS_UNSPECIFIED;
    case 1:
    case "OS_LINUX":
      return Os.OS_LINUX;
    case 2:
    case "OS_MACOS":
      return Os.OS_MACOS;
    case 3:
    case "OS_WINDOWS":
      return Os.OS_WINDOWS;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Os");
  }
}

export function osToJSON(object: Os): string {
  switch (object) {
    case Os.OS_UNSPECIFIED:
      return "OS_UNSPECIFIED";
    case Os.OS_LINUX:
      return "OS_LINUX";
    case Os.OS_MACOS:
      return "OS_MACOS";
    case Os.OS_WINDOWS:
      return "OS_WINDOWS";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Os");
  }
}

/** CPU architecture. */
export enum Arch {
  ARCH_UNSPECIFIED = 0,
  ARCH_X86_64 = 1,
  ARCH_AARCH64 = 2,
}

export function archFromJSON(object: any): Arch {
  switch (object) {
    case 0:
    case "ARCH_UNSPECIFIED":
      return Arch.ARCH_UNSPECIFIED;
    case 1:
    case "ARCH_X86_64":
      return Arch.ARCH_X86_64;
    case 2:
    case "ARCH_AARCH64":
      return Arch.ARCH_AARCH64;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Arch");
  }
}

export function archToJSON(object: Arch): string {
  switch (object) {
    case Arch.ARCH_UNSPECIFIED:
      return "ARCH_UNSPECIFIED";
    case Arch.ARCH_X86_64:
      return "ARCH_X86_64";
    case Arch.ARCH_AARCH64:
      return "ARCH_AARCH64";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Arch");
  }
}

/** The kind of byte stream a relay channel carries. */
export enum StreamKind {
  STREAM_KIND_UNSPECIFIED = 0,
  /** STREAM_KIND_PTY - An interactive pseudo-terminal (ttyd-equivalent). */
  STREAM_KIND_PTY = 1,
  /** STREAM_KIND_DESKTOP - A desktop framebuffer (noVNC-equivalent) for view + computer-use. */
  STREAM_KIND_DESKTOP = 2,
}

export function streamKindFromJSON(object: any): StreamKind {
  switch (object) {
    case 0:
    case "STREAM_KIND_UNSPECIFIED":
      return StreamKind.STREAM_KIND_UNSPECIFIED;
    case 1:
    case "STREAM_KIND_PTY":
      return StreamKind.STREAM_KIND_PTY;
    case 2:
    case "STREAM_KIND_DESKTOP":
      return StreamKind.STREAM_KIND_DESKTOP;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamKind");
  }
}

export function streamKindToJSON(object: StreamKind): string {
  switch (object) {
    case StreamKind.STREAM_KIND_UNSPECIFIED:
      return "STREAM_KIND_UNSPECIFIED";
    case StreamKind.STREAM_KIND_PTY:
      return "STREAM_KIND_PTY";
    case StreamKind.STREAM_KIND_DESKTOP:
      return "STREAM_KIND_DESKTOP";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamKind");
  }
}

export enum DeviceAuthState {
  DEVICE_AUTH_STATE_UNSPECIFIED = 0,
  /** DEVICE_AUTH_STATE_PENDING - The user has not yet completed the flow; keep polling. */
  DEVICE_AUTH_STATE_PENDING = 1,
  /** DEVICE_AUTH_STATE_AUTHORIZED - The user authorized; `credentials` are present. */
  DEVICE_AUTH_STATE_AUTHORIZED = 2,
  /** DEVICE_AUTH_STATE_DENIED - The user explicitly denied the request. */
  DEVICE_AUTH_STATE_DENIED = 3,
  /** DEVICE_AUTH_STATE_EXPIRED - The device_code expired before authorization. */
  DEVICE_AUTH_STATE_EXPIRED = 4,
  /** DEVICE_AUTH_STATE_SLOW_DOWN - The agent is polling too fast and must slow down. */
  DEVICE_AUTH_STATE_SLOW_DOWN = 5,
}

export function deviceAuthStateFromJSON(object: any): DeviceAuthState {
  switch (object) {
    case 0:
    case "DEVICE_AUTH_STATE_UNSPECIFIED":
      return DeviceAuthState.DEVICE_AUTH_STATE_UNSPECIFIED;
    case 1:
    case "DEVICE_AUTH_STATE_PENDING":
      return DeviceAuthState.DEVICE_AUTH_STATE_PENDING;
    case 2:
    case "DEVICE_AUTH_STATE_AUTHORIZED":
      return DeviceAuthState.DEVICE_AUTH_STATE_AUTHORIZED;
    case 3:
    case "DEVICE_AUTH_STATE_DENIED":
      return DeviceAuthState.DEVICE_AUTH_STATE_DENIED;
    case 4:
    case "DEVICE_AUTH_STATE_EXPIRED":
      return DeviceAuthState.DEVICE_AUTH_STATE_EXPIRED;
    case 5:
    case "DEVICE_AUTH_STATE_SLOW_DOWN":
      return DeviceAuthState.DEVICE_AUTH_STATE_SLOW_DOWN;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DeviceAuthState");
  }
}

export function deviceAuthStateToJSON(object: DeviceAuthState): string {
  switch (object) {
    case DeviceAuthState.DEVICE_AUTH_STATE_UNSPECIFIED:
      return "DEVICE_AUTH_STATE_UNSPECIFIED";
    case DeviceAuthState.DEVICE_AUTH_STATE_PENDING:
      return "DEVICE_AUTH_STATE_PENDING";
    case DeviceAuthState.DEVICE_AUTH_STATE_AUTHORIZED:
      return "DEVICE_AUTH_STATE_AUTHORIZED";
    case DeviceAuthState.DEVICE_AUTH_STATE_DENIED:
      return "DEVICE_AUTH_STATE_DENIED";
    case DeviceAuthState.DEVICE_AUTH_STATE_EXPIRED:
      return "DEVICE_AUTH_STATE_EXPIRED";
    case DeviceAuthState.DEVICE_AUTH_STATE_SLOW_DOWN:
      return "DEVICE_AUTH_STATE_SLOW_DOWN";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DeviceAuthState");
  }
}

export enum FsEntryKind {
  FS_ENTRY_KIND_UNSPECIFIED = 0,
  FS_ENTRY_KIND_FILE = 1,
  FS_ENTRY_KIND_DIRECTORY = 2,
  FS_ENTRY_KIND_SYMLINK = 3,
}

export function fsEntryKindFromJSON(object: any): FsEntryKind {
  switch (object) {
    case 0:
    case "FS_ENTRY_KIND_UNSPECIFIED":
      return FsEntryKind.FS_ENTRY_KIND_UNSPECIFIED;
    case 1:
    case "FS_ENTRY_KIND_FILE":
      return FsEntryKind.FS_ENTRY_KIND_FILE;
    case 2:
    case "FS_ENTRY_KIND_DIRECTORY":
      return FsEntryKind.FS_ENTRY_KIND_DIRECTORY;
    case 3:
    case "FS_ENTRY_KIND_SYMLINK":
      return FsEntryKind.FS_ENTRY_KIND_SYMLINK;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FsEntryKind");
  }
}

export function fsEntryKindToJSON(object: FsEntryKind): string {
  switch (object) {
    case FsEntryKind.FS_ENTRY_KIND_UNSPECIFIED:
      return "FS_ENTRY_KIND_UNSPECIFIED";
    case FsEntryKind.FS_ENTRY_KIND_FILE:
      return "FS_ENTRY_KIND_FILE";
    case FsEntryKind.FS_ENTRY_KIND_DIRECTORY:
      return "FS_ENTRY_KIND_DIRECTORY";
    case FsEntryKind.FS_ENTRY_KIND_SYMLINK:
      return "FS_ENTRY_KIND_SYMLINK";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FsEntryKind");
  }
}

export enum GitOp {
  GIT_OP_UNSPECIFIED = 0,
  GIT_OP_STATUS = 1,
  GIT_OP_DIFF = 2,
  GIT_OP_LOG = 3,
  GIT_OP_ADD = 4,
  GIT_OP_COMMIT = 5,
  GIT_OP_BRANCH = 6,
  GIT_OP_CHECKOUT = 7,
  GIT_OP_PULL = 8,
  GIT_OP_PUSH = 9,
  /** GIT_OP_RAW - Any other subcommand, passed through verbatim via `args`. */
  GIT_OP_RAW = 15,
}

export function gitOpFromJSON(object: any): GitOp {
  switch (object) {
    case 0:
    case "GIT_OP_UNSPECIFIED":
      return GitOp.GIT_OP_UNSPECIFIED;
    case 1:
    case "GIT_OP_STATUS":
      return GitOp.GIT_OP_STATUS;
    case 2:
    case "GIT_OP_DIFF":
      return GitOp.GIT_OP_DIFF;
    case 3:
    case "GIT_OP_LOG":
      return GitOp.GIT_OP_LOG;
    case 4:
    case "GIT_OP_ADD":
      return GitOp.GIT_OP_ADD;
    case 5:
    case "GIT_OP_COMMIT":
      return GitOp.GIT_OP_COMMIT;
    case 6:
    case "GIT_OP_BRANCH":
      return GitOp.GIT_OP_BRANCH;
    case 7:
    case "GIT_OP_CHECKOUT":
      return GitOp.GIT_OP_CHECKOUT;
    case 8:
    case "GIT_OP_PULL":
      return GitOp.GIT_OP_PULL;
    case 9:
    case "GIT_OP_PUSH":
      return GitOp.GIT_OP_PUSH;
    case 15:
    case "GIT_OP_RAW":
      return GitOp.GIT_OP_RAW;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GitOp");
  }
}

export function gitOpToJSON(object: GitOp): string {
  switch (object) {
    case GitOp.GIT_OP_UNSPECIFIED:
      return "GIT_OP_UNSPECIFIED";
    case GitOp.GIT_OP_STATUS:
      return "GIT_OP_STATUS";
    case GitOp.GIT_OP_DIFF:
      return "GIT_OP_DIFF";
    case GitOp.GIT_OP_LOG:
      return "GIT_OP_LOG";
    case GitOp.GIT_OP_ADD:
      return "GIT_OP_ADD";
    case GitOp.GIT_OP_COMMIT:
      return "GIT_OP_COMMIT";
    case GitOp.GIT_OP_BRANCH:
      return "GIT_OP_BRANCH";
    case GitOp.GIT_OP_CHECKOUT:
      return "GIT_OP_CHECKOUT";
    case GitOp.GIT_OP_PULL:
      return "GIT_OP_PULL";
    case GitOp.GIT_OP_PUSH:
      return "GIT_OP_PUSH";
    case GitOp.GIT_OP_RAW:
      return "GIT_OP_RAW";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GitOp");
  }
}

export enum GoingOfflineReason {
  GOING_OFFLINE_REASON_UNSPECIFIED = 0,
  /** GOING_OFFLINE_REASON_USER_STOP - The user stopped the foreground process. */
  GOING_OFFLINE_REASON_USER_STOP = 1,
  /** GOING_OFFLINE_REASON_UPDATE - The agent is restarting itself for a self-update. */
  GOING_OFFLINE_REASON_UPDATE = 2,
  /** GOING_OFFLINE_REASON_HOST_SHUTDOWN - The host is shutting down / suspending. */
  GOING_OFFLINE_REASON_HOST_SHUTDOWN = 3,
}

export function goingOfflineReasonFromJSON(object: any): GoingOfflineReason {
  switch (object) {
    case 0:
    case "GOING_OFFLINE_REASON_UNSPECIFIED":
      return GoingOfflineReason.GOING_OFFLINE_REASON_UNSPECIFIED;
    case 1:
    case "GOING_OFFLINE_REASON_USER_STOP":
      return GoingOfflineReason.GOING_OFFLINE_REASON_USER_STOP;
    case 2:
    case "GOING_OFFLINE_REASON_UPDATE":
      return GoingOfflineReason.GOING_OFFLINE_REASON_UPDATE;
    case 3:
    case "GOING_OFFLINE_REASON_HOST_SHUTDOWN":
      return GoingOfflineReason.GOING_OFFLINE_REASON_HOST_SHUTDOWN;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GoingOfflineReason");
  }
}

export function goingOfflineReasonToJSON(object: GoingOfflineReason): string {
  switch (object) {
    case GoingOfflineReason.GOING_OFFLINE_REASON_UNSPECIFIED:
      return "GOING_OFFLINE_REASON_UNSPECIFIED";
    case GoingOfflineReason.GOING_OFFLINE_REASON_USER_STOP:
      return "GOING_OFFLINE_REASON_USER_STOP";
    case GoingOfflineReason.GOING_OFFLINE_REASON_UPDATE:
      return "GOING_OFFLINE_REASON_UPDATE";
    case GoingOfflineReason.GOING_OFFLINE_REASON_HOST_SHUTDOWN:
      return "GOING_OFFLINE_REASON_HOST_SHUTDOWN";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum GoingOfflineReason");
  }
}

export enum StreamRole {
  STREAM_ROLE_UNSPECIFIED = 0,
  /** STREAM_ROLE_AGENT - The agent producing pty/desktop bytes. */
  STREAM_ROLE_AGENT = 1,
  /** STREAM_ROLE_CLIENT - The viewer/pty client consuming them (and sending input back). */
  STREAM_ROLE_CLIENT = 2,
}

export function streamRoleFromJSON(object: any): StreamRole {
  switch (object) {
    case 0:
    case "STREAM_ROLE_UNSPECIFIED":
      return StreamRole.STREAM_ROLE_UNSPECIFIED;
    case 1:
    case "STREAM_ROLE_AGENT":
      return StreamRole.STREAM_ROLE_AGENT;
    case 2:
    case "STREAM_ROLE_CLIENT":
      return StreamRole.STREAM_ROLE_CLIENT;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamRole");
  }
}

export function streamRoleToJSON(object: StreamRole): string {
  switch (object) {
    case StreamRole.STREAM_ROLE_UNSPECIFIED:
      return "STREAM_ROLE_UNSPECIFIED";
    case StreamRole.STREAM_ROLE_AGENT:
      return "STREAM_ROLE_AGENT";
    case StreamRole.STREAM_ROLE_CLIENT:
      return "STREAM_ROLE_CLIENT";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamRole");
  }
}

export enum StreamCloseReason {
  STREAM_CLOSE_REASON_UNSPECIFIED = 0,
  /** STREAM_CLOSE_REASON_NORMAL - Normal end (pty exited, viewer left). */
  STREAM_CLOSE_REASON_NORMAL = 1,
  /** STREAM_CLOSE_REASON_PROCESS_EXIT - The producing process ended. */
  STREAM_CLOSE_REASON_PROCESS_EXIT = 2,
  /** STREAM_CLOSE_REASON_FENCED - The epoch fence invalidated the channel (a swap-away). */
  STREAM_CLOSE_REASON_FENCED = 3,
  /** STREAM_CLOSE_REASON_TRANSPORT_ERROR - A relay/transport error (the client should reconnect+resume). */
  STREAM_CLOSE_REASON_TRANSPORT_ERROR = 4,
  /** STREAM_CLOSE_REASON_RATE_LIMITED - Rate-limit / backpressure shed the channel. */
  STREAM_CLOSE_REASON_RATE_LIMITED = 5,
}

export function streamCloseReasonFromJSON(object: any): StreamCloseReason {
  switch (object) {
    case 0:
    case "STREAM_CLOSE_REASON_UNSPECIFIED":
      return StreamCloseReason.STREAM_CLOSE_REASON_UNSPECIFIED;
    case 1:
    case "STREAM_CLOSE_REASON_NORMAL":
      return StreamCloseReason.STREAM_CLOSE_REASON_NORMAL;
    case 2:
    case "STREAM_CLOSE_REASON_PROCESS_EXIT":
      return StreamCloseReason.STREAM_CLOSE_REASON_PROCESS_EXIT;
    case 3:
    case "STREAM_CLOSE_REASON_FENCED":
      return StreamCloseReason.STREAM_CLOSE_REASON_FENCED;
    case 4:
    case "STREAM_CLOSE_REASON_TRANSPORT_ERROR":
      return StreamCloseReason.STREAM_CLOSE_REASON_TRANSPORT_ERROR;
    case 5:
    case "STREAM_CLOSE_REASON_RATE_LIMITED":
      return StreamCloseReason.STREAM_CLOSE_REASON_RATE_LIMITED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamCloseReason");
  }
}

export function streamCloseReasonToJSON(object: StreamCloseReason): string {
  switch (object) {
    case StreamCloseReason.STREAM_CLOSE_REASON_UNSPECIFIED:
      return "STREAM_CLOSE_REASON_UNSPECIFIED";
    case StreamCloseReason.STREAM_CLOSE_REASON_NORMAL:
      return "STREAM_CLOSE_REASON_NORMAL";
    case StreamCloseReason.STREAM_CLOSE_REASON_PROCESS_EXIT:
      return "STREAM_CLOSE_REASON_PROCESS_EXIT";
    case StreamCloseReason.STREAM_CLOSE_REASON_FENCED:
      return "STREAM_CLOSE_REASON_FENCED";
    case StreamCloseReason.STREAM_CLOSE_REASON_TRANSPORT_ERROR:
      return "STREAM_CLOSE_REASON_TRANSPORT_ERROR";
    case StreamCloseReason.STREAM_CLOSE_REASON_RATE_LIMITED:
      return "STREAM_CLOSE_REASON_RATE_LIMITED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum StreamCloseReason");
  }
}

export enum PointerAction {
  POINTER_ACTION_UNSPECIFIED = 0,
  POINTER_ACTION_MOVE = 1,
  POINTER_ACTION_DOWN = 2,
  POINTER_ACTION_UP = 3,
  POINTER_ACTION_CLICK = 4,
  POINTER_ACTION_DOUBLE_CLICK = 5,
}

export function pointerActionFromJSON(object: any): PointerAction {
  switch (object) {
    case 0:
    case "POINTER_ACTION_UNSPECIFIED":
      return PointerAction.POINTER_ACTION_UNSPECIFIED;
    case 1:
    case "POINTER_ACTION_MOVE":
      return PointerAction.POINTER_ACTION_MOVE;
    case 2:
    case "POINTER_ACTION_DOWN":
      return PointerAction.POINTER_ACTION_DOWN;
    case 3:
    case "POINTER_ACTION_UP":
      return PointerAction.POINTER_ACTION_UP;
    case 4:
    case "POINTER_ACTION_CLICK":
      return PointerAction.POINTER_ACTION_CLICK;
    case 5:
    case "POINTER_ACTION_DOUBLE_CLICK":
      return PointerAction.POINTER_ACTION_DOUBLE_CLICK;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum PointerAction");
  }
}

export function pointerActionToJSON(object: PointerAction): string {
  switch (object) {
    case PointerAction.POINTER_ACTION_UNSPECIFIED:
      return "POINTER_ACTION_UNSPECIFIED";
    case PointerAction.POINTER_ACTION_MOVE:
      return "POINTER_ACTION_MOVE";
    case PointerAction.POINTER_ACTION_DOWN:
      return "POINTER_ACTION_DOWN";
    case PointerAction.POINTER_ACTION_UP:
      return "POINTER_ACTION_UP";
    case PointerAction.POINTER_ACTION_CLICK:
      return "POINTER_ACTION_CLICK";
    case PointerAction.POINTER_ACTION_DOUBLE_CLICK:
      return "POINTER_ACTION_DOUBLE_CLICK";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum PointerAction");
  }
}

export enum PointerButton {
  POINTER_BUTTON_UNSPECIFIED = 0,
  POINTER_BUTTON_LEFT = 1,
  POINTER_BUTTON_RIGHT = 2,
  POINTER_BUTTON_MIDDLE = 3,
}

export function pointerButtonFromJSON(object: any): PointerButton {
  switch (object) {
    case 0:
    case "POINTER_BUTTON_UNSPECIFIED":
      return PointerButton.POINTER_BUTTON_UNSPECIFIED;
    case 1:
    case "POINTER_BUTTON_LEFT":
      return PointerButton.POINTER_BUTTON_LEFT;
    case 2:
    case "POINTER_BUTTON_RIGHT":
      return PointerButton.POINTER_BUTTON_RIGHT;
    case 3:
    case "POINTER_BUTTON_MIDDLE":
      return PointerButton.POINTER_BUTTON_MIDDLE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum PointerButton");
  }
}

export function pointerButtonToJSON(object: PointerButton): string {
  switch (object) {
    case PointerButton.POINTER_BUTTON_UNSPECIFIED:
      return "POINTER_BUTTON_UNSPECIFIED";
    case PointerButton.POINTER_BUTTON_LEFT:
      return "POINTER_BUTTON_LEFT";
    case PointerButton.POINTER_BUTTON_RIGHT:
      return "POINTER_BUTTON_RIGHT";
    case PointerButton.POINTER_BUTTON_MIDDLE:
      return "POINTER_BUTTON_MIDDLE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum PointerButton");
  }
}

export enum KeyAction {
  KEY_ACTION_UNSPECIFIED = 0,
  KEY_ACTION_DOWN = 1,
  KEY_ACTION_UP = 2,
  KEY_ACTION_PRESS = 3,
}

export function keyActionFromJSON(object: any): KeyAction {
  switch (object) {
    case 0:
    case "KEY_ACTION_UNSPECIFIED":
      return KeyAction.KEY_ACTION_UNSPECIFIED;
    case 1:
    case "KEY_ACTION_DOWN":
      return KeyAction.KEY_ACTION_DOWN;
    case 2:
    case "KEY_ACTION_UP":
      return KeyAction.KEY_ACTION_UP;
    case 3:
    case "KEY_ACTION_PRESS":
      return KeyAction.KEY_ACTION_PRESS;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum KeyAction");
  }
}

export function keyActionToJSON(object: KeyAction): string {
  switch (object) {
    case KeyAction.KEY_ACTION_UNSPECIFIED:
      return "KEY_ACTION_UNSPECIFIED";
    case KeyAction.KEY_ACTION_DOWN:
      return "KEY_ACTION_DOWN";
    case KeyAction.KEY_ACTION_UP:
      return "KEY_ACTION_UP";
    case KeyAction.KEY_ACTION_PRESS:
      return "KEY_ACTION_PRESS";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum KeyAction");
  }
}

/**
 * A typed error returned by the agent (or synthesized by the control plane for
 * a non-responding agent). `code` is a stable machine-readable discriminant;
 * `message` is human-facing; `retryable` lets the turn layer decide whether to
 * pause-and-retry (a reconnect blip) versus surface a hard failure.
 */
export interface AgentError {
  code: ErrorCode;
  message: string;
  retryable: boolean;
  /** Optional structured detail (e.g. an OS errno, a git exit code) for logs. */
  detail: { [key: string]: string };
}

export interface AgentError_DetailEntry {
  key: string;
  value: string;
}

/**
 * The agent's self-description, sent on connect (the "hello") and refreshed on
 * reconnect. The control plane folds this into the negotiated SessionCapabilities
 * document (every capability cell is available:false+reason, never absent).
 */
export interface Hello {
  agentId: string;
  workspaceId: string;
  /** Semantic version of the running agent binary (drives staged self-update). */
  agentVersion: string;
  os: Os;
  arch: Arch;
  /** Human-friendly machine name (hostname by default; user-overridable). */
  machineName: string;
  /** The agent-reported working root the control plane treats as the sandbox cwd. */
  workspaceRoot: string;
  /** Capabilities the agent can serve given its platform + consent grants. */
  capabilities:
    | Capabilities
    | undefined;
  /** The update channel this agent follows (stable|beta), for fleet-pinning. */
  updateChannel: string;
  /**
   * A resume token minted by a prior connection, echoed so the control plane can
   * recognize a reconnect (vs a fresh enrollment) and fence by epoch. Empty on
   * first connect.
   */
  resumeToken: string;
}

/**
 * What the agent is able and consented to do. Each bool is a hard capability
 * gate; the control plane maps a false to a CapabilityUnavailableReason.
 */
export interface Capabilities {
  /** Channel-A structural surface (always true for a connected agent). */
  exec: boolean;
  filesystem: boolean;
  git: boolean;
  /** An interactive PTY can be opened over the relay. */
  pty: boolean;
  /** A desktop framebuffer is available (a display exists or Xvfb is enabled). */
  desktop: boolean;
  /** The user consented to whole-machine access during enrollment. */
  consentedWholeMachine: boolean;
  /** The user consented to screen capture + synthetic input (computer-use). */
  consentedScreenControl: boolean;
  /** Display detail (absent => headless / no display). */
  display: Display | undefined;
}

/** Detail about the agent's graphical display, when one is present. */
export interface Display {
  /** e.g. ":0", ":99" (Xvfb), or a platform handle. */
  id: string;
  width: number;
  height: number;
  /** True when backed by a virtual framebuffer (Xvfb) rather than a real screen. */
  virtual: boolean;
}

/**
 * The agent begins enrollment by asking the control plane to start a device
 * flow. The control plane returns a user-code + verification URL the user visits
 * to consent (whole-machine + optional screen-control) and authorize.
 */
export interface DeviceAuthStartRequest {
  /**
   * The agent's stable install identity (a per-install keypair fingerprint),
   * bound to the enrollment so the issued credentials are non-transferable.
   */
  installFingerprint: string;
  os: Os;
  arch: Arch;
  machineName: string;
  /** The update channel the user selected at install time. */
  updateChannel: string;
}

export interface DeviceAuthStartResponse {
  /** Short code the user types at the verification URL. */
  userCode: string;
  /** The opaque device code the agent polls with. */
  deviceCode: string;
  /** Where the user goes to authorize (e.g. https://get.opengeni.ai/device). */
  verificationUri: string;
  /** A pre-filled convenience URL embedding the user_code. */
  verificationUriComplete: string;
  /** Seconds the device_code remains valid. */
  expiresInSeconds: number;
  /** Minimum seconds the agent must wait between polls. */
  pollIntervalSeconds: number;
}

/** The agent polls until the user authorizes (or it expires / is denied). */
export interface DeviceAuthPollRequest {
  deviceCode: string;
}

export interface DeviceAuthPollResponse {
  state: DeviceAuthState;
  /** Present only when state == AUTHORIZED. */
  credentials: EnrollmentCredentials | undefined;
}

/**
 * The scoped credentials persisted (0600) on the machine after enrollment.
 * These are workspace-scoped — they can address only this workspace's agents.
 */
export interface EnrollmentCredentials {
  agentId: string;
  workspaceId: string;
  /**
   * The NATS CONNECT AUTH-TOKEN the agent presents (the signed `oge_` enrollment
   * bearer). The server runs with AUTH CALLOUT (dossier §10.1 / M-AUTH): on connect
   * the agent passes this as the auth-token; nats-server issues an authorization
   * request to the control-plane's callout responder, which validates the bearer
   * and returns a SIGNED user JWT scoping the connection to pub/sub ONLY
   * `agent.<ws>.>` (+ `_INBOX.>`). That per-subject scope IS the per-workspace
   * tenancy boundary — there is NO per-machine creds file to ship. (Field name kept
   * for proto stability; it now carries the bearer, not an operator creds-file body.)
   */
  natsCredentials: string;
  /**
   * The NATS server URL(s) to dial. For the control plane these are `wss://` URLs
   * (the relay-symmetric TLS ingress) so the agent rides the same TLS endpoint as
   * the stream relay — no separate public TCP load balancer (dossier M-AUTH).
   */
  natsUrls: string[];
  /** The relay edge base URL for opening stream channels. */
  relayUrl: string;
  /** The minisign public key the agent pins for self-update verification. */
  updatePubkey: string;
  /** Whether the user consented to whole-machine access. */
  consentedWholeMachine: boolean;
  /** Whether the user consented to screen capture + synthetic input. */
  consentedScreenControl: boolean;
  /**
   * The agent's PRODUCER token for the relay edge (dossier §10.5 / the relay-dial
   * protocol). Presented as `StreamOpen.token` when the agent registers a
   * pty/desktop channel (role=AGENT). It is a control-plane-minted, workspace+agent
   * -scoped `ogr_` HMAC token the relay verifies on its own merits — DISTINCT from
   * the viewer's `ogs_` stream token (which the control plane mints per viewer and
   * the relay also verifies). The relay pairs the producer and consumer by the
   * channel key once BOTH tokens pass. Additive proto3 field; empty for an
   * enrollment minted before the relay-token plane is configured (the agent then
   * presents an empty token the relay rejects, surfacing the gap loudly).
   */
  relayToken: string;
}

export interface ExecRequest {
  /** argv[0] is the program; the agent does NOT shell-interpret unless `shell`. */
  command: string[];
  /** When true, `command` is joined and run through the platform shell. */
  shell: boolean;
  /** Working directory; defaults to the agent's workspace_root when empty. */
  cwd: string;
  /** Extra environment variables layered over the agent's environment. */
  env: { [key: string]: string };
  /** Optional stdin fed to the process. */
  stdin: Uint8Array;
  /** Hard wall-clock timeout; 0 means the control-plane default. */
  timeoutMs: number;
}

export interface ExecRequest_EnvEntry {
  key: string;
  value: string;
}

export interface ExecResponse {
  exitCode: number;
  stdout: Uint8Array;
  stderr: Uint8Array;
  /** True when the process was killed by `timeout_ms`. */
  timedOut: boolean;
  /** Wall-clock duration the process ran. */
  durationMs: string;
}

export interface FsReadRequest {
  path: string;
  /** Optional byte offset + length for ranged reads (0 length => to EOF). */
  offset: string;
  length: string;
}

export interface FsReadResponse {
  content: Uint8Array;
  /** Total size of the file (so a ranged read knows there is more). */
  totalSize: string;
}

export interface FsWriteRequest {
  path: string;
  content: Uint8Array;
  /** Create parent directories as needed. */
  createParents: boolean;
  /** Append rather than truncate-and-write. */
  append: boolean;
  /** POSIX mode for a newly created file (e.g. 0644); 0 => platform default. */
  mode: number;
}

export interface FsWriteResponse {
  bytesWritten: string;
}

export interface FsListRequest {
  path: string;
  /** Recurse into subdirectories. */
  recursive: boolean;
}

export interface FsListResponse {
  entries: FsEntry[];
}

export interface FsEntry {
  name: string;
  /** Path relative to the listed directory. */
  path: string;
  kind: FsEntryKind;
  size: string;
  /** Last-modified time, unix epoch ms. */
  modifiedMs: string;
  mode: number;
}

export interface FsMkdirRequest {
  path: string;
  /** Create intermediate directories (mkdir -p). */
  parents: boolean;
  mode: number;
}

export interface FsMkdirResponse {
}

export interface FsMoveRequest {
  from: string;
  to: string;
  /** Overwrite the destination if it exists. */
  overwrite: boolean;
}

export interface FsMoveResponse {
}

export interface FsStatRequest {
  path: string;
}

export interface FsStatResponse {
  /** Absent when the path does not exist (the op still succeeds; `exists=false`). */
  exists: boolean;
  entry: FsEntry | undefined;
}

export interface FsRemoveRequest {
  path: string;
  /** Remove directories recursively. */
  recursive: boolean;
}

export interface FsRemoveResponse {
}

/**
 * A single git operation. The agent runs git against the repo rooted at `cwd`
 * (or workspace_root) and returns structured results where it can, falling back
 * to raw stdout for pass-through subcommands.
 */
export interface GitRequest {
  op: GitOp;
  /** Repo working directory; defaults to workspace_root. */
  cwd: string;
  /** Free-form args for ops that pass through (e.g. log/diff flags). */
  args: string[];
}

export interface GitResponse {
  exitCode: number;
  /** Raw stdout/stderr for pass-through fidelity. */
  stdout: Uint8Array;
  stderr: Uint8Array;
  /** Structured status, present for GIT_OP_STATUS. */
  status: GitStatus | undefined;
}

export interface GitStatus {
  branch: string;
  /** The upstream tracking branch, if any. */
  upstream: string;
  ahead: number;
  behind: number;
  files: GitFileStatus[];
  clean: boolean;
}

export interface GitFileStatus {
  path: string;
  /** Two-letter porcelain code (e.g. " M", "??", "A "). */
  code: string;
  staged: boolean;
}

/**
 * Open a PTY. The agent allocates a pseudo-terminal, spawns the shell/command,
 * and registers a relay stream channel of kind PTY. The returned `channel`
 * identifies the stream the viewer connects to.
 */
export interface PtyOpenRequest {
  /** Shell/command to run; empty => the user's default login shell. */
  command: string[];
  cwd: string;
  env: { [key: string]: string };
  cols: number;
  rows: number;
  /** The TERM value to advertise (e.g. "xterm-256color"). */
  term: string;
}

export interface PtyOpenRequest_EnvEntry {
  key: string;
  value: string;
}

export interface PtyOpenResponse {
  /** A handle for subsequent resize/close control ops. */
  ptyId: string;
  /** The relay stream channel carrying the PTY bytes. */
  channel: StreamChannel | undefined;
}

/**
 * Write input bytes to a PTY. (Bulk interactive bytes normally ride the stream
 * plane as StreamFrames; this control op exists for programmatic injection.)
 */
export interface PtyWriteRequest {
  ptyId: string;
  data: Uint8Array;
}

export interface PtyWriteResponse {
  bytesWritten: string;
}

export interface PtyResizeRequest {
  ptyId: string;
  cols: number;
  rows: number;
}

export interface PtyResizeResponse {
}

export interface PtyCloseRequest {
  ptyId: string;
}

export interface PtyCloseResponse {
  /** The exit code of the PTY's root process, if it had exited. */
  exitCode: number;
}

/**
 * Ensure a desktop framebuffer stream exists, registering a relay channel of
 * kind DESKTOP. Idempotent: repeated calls return the same live channel.
 */
export interface DesktopEnsureRequest {
  /** Request a specific geometry (0 => the display's native geometry). */
  width: number;
  height: number;
}

export interface DesktopEnsureResponse {
  channel: StreamChannel | undefined;
  display: Display | undefined;
}

/**
 * Inject one synthetic computer-use input event on the machine's desktop — the
 * CONTROL-PLANE twin of the relay DesktopInput (the agent drives its OWN screen
 * for computer-use; there is no human viewer channel). Reuses the relay
 * DesktopInput event shapes; there is no channel_id (this goes straight to the
 * display, not a relay channel). Injection is gated on consented_screen_control:
 * an ungated call fails with ERROR_CODE_CONSENT_REQUIRED and never touches the OS.
 */
export interface DesktopInputRequest {
  event: { $case: "pointer"; pointer: PointerEvent } | { $case: "key"; key: KeyEvent } | {
    $case: "scroll";
    scroll: ScrollEvent;
  } | undefined;
}

export interface DesktopInputResponse {
}

/**
 * Capture a single screenshot of the machine's desktop as a PNG. A VIEW op: it
 * needs a display, NOT screen-control consent (the view/control decoupling), so
 * it is not consent-gated. Returns the encoded image plus its geometry so the
 * caller can size a canvas without decoding.
 */
export interface DesktopScreenshotRequest {
}

export interface DesktopScreenshotResponse {
  png: Uint8Array;
  width: number;
  height: number;
}

/**
 * The agent's periodic liveness ping. Carries a metrics sample so the control
 * plane can upsert the machine's last-sample without a separate RPC. The
 * `seq` increments per heartbeat so the control plane can detect a gap.
 */
export interface Heartbeat {
  seq: string;
  /** Agent monotonic uptime in ms. */
  uptimeMs: string;
  /**
   * The number of sessions currently sharing this machine (lease refcount as the
   * agent sees it; the authoritative count lives in Postgres).
   */
  activeSessions: number;
  metrics:
    | MetricsSample
    | undefined;
  /**
   * True when the agent is draining for a pending self-update (prefer other
   * sandboxes for new work).
   */
  draining: boolean;
}

export interface HeartbeatAck {
  /** The control plane may instruct the agent to adjust its heartbeat cadence. */
  nextIntervalMs: number;
  /** The control plane may signal a pending fleet action (e.g. update available). */
  updateAvailable: boolean;
}

/** On-demand metrics request (the dashboard force-poll path). */
export interface MetricsRequest {
}

/** A point-in-time machine metrics sample. */
export interface MetricsSample {
  /** Unix epoch ms when sampled. */
  sampledAtMs: string;
  /** CPU utilization 0..100 (whole machine). */
  cpuPercent: number;
  /** Load averages. */
  load1: number;
  load5: number;
  load15: number;
  memUsedBytes: string;
  memTotalBytes: string;
  diskUsedBytes: string;
  diskTotalBytes: string;
  /** A contention signal (run-queue length / pressure), if available. */
  runQueue: number;
  /** Per-GPU samples, when GPUs are present. */
  gpus: GpuSample[];
}

export interface GpuSample {
  name: string;
  /** Utilization 0..100. */
  utilPercent: number;
  memUsedBytes: string;
  memTotalBytes: string;
}

/**
 * A cheap liveness probe — the control plane requests it to test that a
 * responder exists on the subject (the NATS "is there an agent" check).
 */
export interface PingRequest {
  /** Echoed back so the caller can correlate / measure RTT. */
  nonce: string;
}

export interface PingResponse {
  nonce: string;
  /** The agent's monotonic clock, for skew estimation. */
  agentMonotonicMs: string;
}

/**
 * Sent by the agent on a clean stop (SIGINT/SIGTERM) so the lease flips to
 * offline IMMEDIATELY rather than waiting on heartbeat dead-detection.
 */
export interface GoingOffline {
  reason: GoingOfflineReason;
  /** Optional human note (e.g. "user pressed Ctrl-C"). */
  message: string;
}

/**
 * On reconnect, the agent presents its resume token; the control plane fences by
 * epoch and either accepts the resume (same machine, new connection generation)
 * or, if the token is unknown/stale, requires a fresh hello.
 */
export interface ResumeRequest {
  agentId: string;
  resumeToken: string;
  /** The lease epoch the agent last observed, for the integer fence. */
  lastKnownEpoch: number;
}

export interface ResumeResponse {
  accepted: boolean;
  /**
   * The new lease epoch the control plane assigned to this connection. Any op
   * fenced by an older epoch must re-resolve and retry.
   */
  epoch: number;
  /** A fresh resume token for the NEXT reconnect (tokens rotate per generation). */
  resumeToken: string;
}

/** The agent asks whether a newer build is available for its channel + target. */
export interface UpdateCheckRequest {
  currentVersion: string;
  channel: string;
  os: Os;
  arch: Arch;
}

export interface UpdateCheckResponse {
  updateAvailable: boolean;
  manifest: UpdateManifest | undefined;
}

/**
 * A signed channel manifest. It is itself minisign-signed out-of-band; this
 * message is the decoded body the updater reasons over.
 */
export interface UpdateManifest {
  channel: string;
  version: string;
  /** The minimum version still permitted (downgrade-attack protection). */
  minSupported: string;
  /** Staged-rollout gate: an agent applies when blake3(agentId+salt)%100 < percent. */
  rolloutPercent: number;
  cohortSalt: string;
  artifacts: UpdateArtifact[];
  notesUrl: string;
  signedAtMs: string;
  /** When true, override drain and update mid-session (security CVE path). */
  force: boolean;
}

export interface UpdateArtifact {
  /** Target triple, e.g. "x86_64-unknown-linux-musl". */
  target: string;
  url: string;
  size: string;
  sha256: string;
  /** URL of the detached minisign signature for the artifact. */
  minisigUrl: string;
}

/**
 * Before applying, the updater asks the control plane whether it may proceed
 * (the control plane reads the Postgres lease refcount). The agent drains until
 * the refcount reaches zero or a grace window elapses.
 */
export interface UpdateMayProceedRequest {
  agentId: string;
  targetVersion: string;
}

export interface UpdateMayProceedResponse {
  mayProceed: boolean;
  /** When false, the number of live sessions blocking the update. */
  activeSessions: number;
}

/**
 * Wraps an op-specific request. `request_id` correlates a response to a request
 * even over fan-out; `epoch` carries the lease/active epoch the control plane
 * resolved this op against, so the agent can reject a stale op (ERROR_CODE_FENCED).
 */
export interface ControlRequest {
  requestId: string;
  epoch: number;
  op:
    | { $case: "ping"; ping: PingRequest }
    | { $case: "hello"; hello: Hello }
    | { $case: "resume"; resume: ResumeRequest }
    | { $case: "exec"; exec: ExecRequest }
    | { $case: "fsRead"; fsRead: FsReadRequest }
    | { $case: "fsWrite"; fsWrite: FsWriteRequest }
    | { $case: "fsList"; fsList: FsListRequest }
    | { $case: "fsMkdir"; fsMkdir: FsMkdirRequest }
    | { $case: "fsMove"; fsMove: FsMoveRequest }
    | { $case: "fsStat"; fsStat: FsStatRequest }
    | { $case: "fsRemove"; fsRemove: FsRemoveRequest }
    | { $case: "git"; git: GitRequest }
    | { $case: "ptyOpen"; ptyOpen: PtyOpenRequest }
    | { $case: "ptyWrite"; ptyWrite: PtyWriteRequest }
    | { $case: "ptyResize"; ptyResize: PtyResizeRequest }
    | { $case: "ptyClose"; ptyClose: PtyCloseRequest }
    | { $case: "desktopEnsure"; desktopEnsure: DesktopEnsureRequest }
    | { $case: "metrics"; metrics: MetricsRequest }
    | { $case: "updateMayProceed"; updateMayProceed: UpdateMayProceedRequest }
    | { $case: "desktopInput"; desktopInput: DesktopInputRequest }
    | { $case: "desktopScreenshot"; desktopScreenshot: DesktopScreenshotRequest }
    | undefined;
}

/** Wraps an op-specific response, or an error. `request_id` echoes the request. */
export interface ControlResponse {
  requestId: string;
  /** Present when the op failed; when set, `result` is empty. */
  error: AgentError | undefined;
  result:
    | { $case: "ping"; ping: PingResponse }
    | { $case: "hello"; hello: HelloAck }
    | { $case: "resume"; resume: ResumeResponse }
    | { $case: "exec"; exec: ExecResponse }
    | { $case: "fsRead"; fsRead: FsReadResponse }
    | { $case: "fsWrite"; fsWrite: FsWriteResponse }
    | { $case: "fsList"; fsList: FsListResponse }
    | { $case: "fsMkdir"; fsMkdir: FsMkdirResponse }
    | { $case: "fsMove"; fsMove: FsMoveResponse }
    | { $case: "fsStat"; fsStat: FsStatResponse }
    | { $case: "fsRemove"; fsRemove: FsRemoveResponse }
    | { $case: "git"; git: GitResponse }
    | { $case: "ptyOpen"; ptyOpen: PtyOpenResponse }
    | { $case: "ptyWrite"; ptyWrite: PtyWriteResponse }
    | { $case: "ptyResize"; ptyResize: PtyResizeResponse }
    | { $case: "ptyClose"; ptyClose: PtyCloseResponse }
    | { $case: "desktopEnsure"; desktopEnsure: DesktopEnsureResponse }
    | { $case: "metrics"; metrics: MetricsSample }
    | { $case: "updateMayProceed"; updateMayProceed: UpdateMayProceedResponse }
    | { $case: "desktopInput"; desktopInput: DesktopInputResponse }
    | { $case: "desktopScreenshot"; desktopScreenshot: DesktopScreenshotResponse }
    | undefined;
}

/**
 * The control plane's acknowledgement of a Hello, carrying the epoch it assigned
 * and a resume token for the next reconnect.
 */
export interface HelloAck {
  epoch: number;
  resumeToken: string;
  /** The heartbeat interval the agent should use. */
  heartbeatIntervalMs: number;
}

/**
 * Wraps a message the AGENT originates (not a reply to a ControlRequest):
 * heartbeats, the clean going-offline signal. These are published on the agent's
 * outbound subject; the control plane folds them into lease/metrics state.
 */
export interface AgentEvent {
  agentId: string;
  event:
    | { $case: "heartbeat"; heartbeat: Heartbeat }
    | { $case: "goingOffline"; goingOffline: GoingOffline }
    | undefined;
}

/** The control plane's ack of an AgentEvent (e.g. a HeartbeatAck). */
export interface AgentEventAck {
  ack: { $case: "heartbeat"; heartbeat: HeartbeatAck } | undefined;
}

/**
 * Identifies a relay stream channel. The viewer/pty client connects to the
 * relay with this channel's coordinates plus a scoped `ogs_` token; the agent
 * dials out and registers the same channel. `{workspace_id, agent_id, port}`
 * is the routing key.
 */
export interface StreamChannel {
  channelId: string;
  workspaceId: string;
  agentId: string;
  kind: StreamKind;
  /** The logical port the channel maps to (so resolveExposedPort can address it). */
  port: number;
}

/**
 * Open a stream channel on the relay. Sent by both ends (agent registers; client
 * attaches) as the first frame of a relay connection.
 */
export interface StreamOpen {
  channel:
    | StreamChannel
    | undefined;
  /**
   * The scoped stream token (`ogs_` envelope) authorizing this channel; the
   * relay validates authenticity + the lease/active-epoch fence.
   */
  token: string;
  /** Whether the sender is the producing agent or the consuming viewer. */
  role: StreamRole;
  /**
   * For resume: the last frame sequence the sender successfully processed, so
   * the peer can replay from there after a reconnect blip.
   */
  resumeFromSeq: string;
}

/** The relay's acknowledgement of a StreamOpen. */
export interface StreamOpenAck {
  accepted: boolean;
  error:
    | AgentError
    | undefined;
  /** The sequence the peer will resume sending from. */
  resumeFromSeq: string;
}

/**
 * A data frame carrying raw stream bytes in either direction. `seq` is a
 * monotonic per-channel-per-direction counter enabling resume-from-seq. For a
 * DESKTOP channel `data` is an encoded framebuffer chunk; for PTY it is raw tty
 * bytes. Input (client→agent) for desktop computer-use rides DesktopInput
 * instead so synthetic events are typed, not opaque bytes.
 */
export interface StreamFrame {
  channelId: string;
  seq: string;
  data: Uint8Array;
  /** Unix epoch ms the frame was produced (for latency/lag metrics). */
  producedAtMs: string;
}

/**
 * Close a stream channel. Either end may send it; the relay tears down the
 * channel and notifies the peer.
 */
export interface StreamClose {
  channelId: string;
  reason: StreamCloseReason;
  message: string;
}

/**
 * Typed computer-use input from a client to a desktop channel (client→agent).
 * Synthetic input is gated on consented_screen_control; an ungated input op
 * fails with ERROR_CODE_CONSENT_REQUIRED.
 */
export interface DesktopInput {
  channelId: string;
  event: { $case: "pointer"; pointer: PointerEvent } | { $case: "key"; key: KeyEvent } | {
    $case: "scroll";
    scroll: ScrollEvent;
  } | undefined;
}

export interface PointerEvent {
  x: number;
  y: number;
  action: PointerAction;
  button: PointerButton;
}

export interface KeyEvent {
  /** A platform-independent key name (e.g. "Enter", "a", "ctrl+c") OR text to type. */
  key: string;
  isText: boolean;
  action: KeyAction;
}

export interface ScrollEvent {
  x: number;
  y: number;
  deltaX: number;
  deltaY: number;
}

function createBaseAgentError(): AgentError {
  return { code: 0, message: "", retryable: false, detail: {} };
}

export const AgentError: MessageFns<AgentError> = {
  encode(message: AgentError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.retryable !== false) {
      writer.uint32(24).bool(message.retryable);
    }
    globalThis.Object.entries(message.detail).forEach(([key, value]: [string, string]) => {
      AgentError_DetailEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.retryable = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = AgentError_DetailEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.detail[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentError {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      retryable: isSet(object.retryable) ? globalThis.Boolean(object.retryable) : false,
      detail: isObject(object.detail)
        ? (globalThis.Object.entries(object.detail) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: AgentError): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.retryable !== false) {
      obj.retryable = message.retryable;
    }
    if (message.detail) {
      const entries = globalThis.Object.entries(message.detail) as [string, string][];
      if (entries.length > 0) {
        obj.detail = {};
        entries.forEach(([k, v]) => {
          obj.detail[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentError>, I>>(base?: I): AgentError {
    return AgentError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentError>, I>>(object: I): AgentError {
    const message = createBaseAgentError();
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.retryable = object.retryable ?? false;
    message.detail = (globalThis.Object.entries(object.detail ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseAgentError_DetailEntry(): AgentError_DetailEntry {
  return { key: "", value: "" };
}

export const AgentError_DetailEntry: MessageFns<AgentError_DetailEntry> = {
  encode(message: AgentError_DetailEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentError_DetailEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentError_DetailEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentError_DetailEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AgentError_DetailEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentError_DetailEntry>, I>>(base?: I): AgentError_DetailEntry {
    return AgentError_DetailEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentError_DetailEntry>, I>>(object: I): AgentError_DetailEntry {
    const message = createBaseAgentError_DetailEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseHello(): Hello {
  return {
    agentId: "",
    workspaceId: "",
    agentVersion: "",
    os: 0,
    arch: 0,
    machineName: "",
    workspaceRoot: "",
    capabilities: undefined,
    updateChannel: "",
    resumeToken: "",
  };
}

export const Hello: MessageFns<Hello> = {
  encode(message: Hello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.agentVersion !== "") {
      writer.uint32(26).string(message.agentVersion);
    }
    if (message.os !== 0) {
      writer.uint32(32).int32(message.os);
    }
    if (message.arch !== 0) {
      writer.uint32(40).int32(message.arch);
    }
    if (message.machineName !== "") {
      writer.uint32(50).string(message.machineName);
    }
    if (message.workspaceRoot !== "") {
      writer.uint32(58).string(message.workspaceRoot);
    }
    if (message.capabilities !== undefined) {
      Capabilities.encode(message.capabilities, writer.uint32(66).fork()).join();
    }
    if (message.updateChannel !== "") {
      writer.uint32(74).string(message.updateChannel);
    }
    if (message.resumeToken !== "") {
      writer.uint32(82).string(message.resumeToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.os = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.arch = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.machineName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.workspaceRoot = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.capabilities = Capabilities.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.updateChannel = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.resumeToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hello {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
      agentVersion: isSet(object.agentVersion)
        ? globalThis.String(object.agentVersion)
        : isSet(object.agent_version)
        ? globalThis.String(object.agent_version)
        : "",
      os: isSet(object.os) ? osFromJSON(object.os) : 0,
      arch: isSet(object.arch) ? archFromJSON(object.arch) : 0,
      machineName: isSet(object.machineName)
        ? globalThis.String(object.machineName)
        : isSet(object.machine_name)
        ? globalThis.String(object.machine_name)
        : "",
      workspaceRoot: isSet(object.workspaceRoot)
        ? globalThis.String(object.workspaceRoot)
        : isSet(object.workspace_root)
        ? globalThis.String(object.workspace_root)
        : "",
      capabilities: isSet(object.capabilities) ? Capabilities.fromJSON(object.capabilities) : undefined,
      updateChannel: isSet(object.updateChannel)
        ? globalThis.String(object.updateChannel)
        : isSet(object.update_channel)
        ? globalThis.String(object.update_channel)
        : "",
      resumeToken: isSet(object.resumeToken)
        ? globalThis.String(object.resumeToken)
        : isSet(object.resume_token)
        ? globalThis.String(object.resume_token)
        : "",
    };
  },

  toJSON(message: Hello): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    if (message.agentVersion !== "") {
      obj.agentVersion = message.agentVersion;
    }
    if (message.os !== 0) {
      obj.os = osToJSON(message.os);
    }
    if (message.arch !== 0) {
      obj.arch = archToJSON(message.arch);
    }
    if (message.machineName !== "") {
      obj.machineName = message.machineName;
    }
    if (message.workspaceRoot !== "") {
      obj.workspaceRoot = message.workspaceRoot;
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = Capabilities.toJSON(message.capabilities);
    }
    if (message.updateChannel !== "") {
      obj.updateChannel = message.updateChannel;
    }
    if (message.resumeToken !== "") {
      obj.resumeToken = message.resumeToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Hello>, I>>(base?: I): Hello {
    return Hello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Hello>, I>>(object: I): Hello {
    const message = createBaseHello();
    message.agentId = object.agentId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.agentVersion = object.agentVersion ?? "";
    message.os = object.os ?? 0;
    message.arch = object.arch ?? 0;
    message.machineName = object.machineName ?? "";
    message.workspaceRoot = object.workspaceRoot ?? "";
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? Capabilities.fromPartial(object.capabilities)
      : undefined;
    message.updateChannel = object.updateChannel ?? "";
    message.resumeToken = object.resumeToken ?? "";
    return message;
  },
};

function createBaseCapabilities(): Capabilities {
  return {
    exec: false,
    filesystem: false,
    git: false,
    pty: false,
    desktop: false,
    consentedWholeMachine: false,
    consentedScreenControl: false,
    display: undefined,
  };
}

export const Capabilities: MessageFns<Capabilities> = {
  encode(message: Capabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exec !== false) {
      writer.uint32(8).bool(message.exec);
    }
    if (message.filesystem !== false) {
      writer.uint32(16).bool(message.filesystem);
    }
    if (message.git !== false) {
      writer.uint32(24).bool(message.git);
    }
    if (message.pty !== false) {
      writer.uint32(32).bool(message.pty);
    }
    if (message.desktop !== false) {
      writer.uint32(40).bool(message.desktop);
    }
    if (message.consentedWholeMachine !== false) {
      writer.uint32(48).bool(message.consentedWholeMachine);
    }
    if (message.consentedScreenControl !== false) {
      writer.uint32(56).bool(message.consentedScreenControl);
    }
    if (message.display !== undefined) {
      Display.encode(message.display, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exec = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.filesystem = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.git = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pty = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.desktop = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.consentedWholeMachine = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.consentedScreenControl = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.display = Display.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capabilities {
    return {
      exec: isSet(object.exec) ? globalThis.Boolean(object.exec) : false,
      filesystem: isSet(object.filesystem) ? globalThis.Boolean(object.filesystem) : false,
      git: isSet(object.git) ? globalThis.Boolean(object.git) : false,
      pty: isSet(object.pty) ? globalThis.Boolean(object.pty) : false,
      desktop: isSet(object.desktop) ? globalThis.Boolean(object.desktop) : false,
      consentedWholeMachine: isSet(object.consentedWholeMachine)
        ? globalThis.Boolean(object.consentedWholeMachine)
        : isSet(object.consented_whole_machine)
        ? globalThis.Boolean(object.consented_whole_machine)
        : false,
      consentedScreenControl: isSet(object.consentedScreenControl)
        ? globalThis.Boolean(object.consentedScreenControl)
        : isSet(object.consented_screen_control)
        ? globalThis.Boolean(object.consented_screen_control)
        : false,
      display: isSet(object.display) ? Display.fromJSON(object.display) : undefined,
    };
  },

  toJSON(message: Capabilities): unknown {
    const obj: any = {};
    if (message.exec !== false) {
      obj.exec = message.exec;
    }
    if (message.filesystem !== false) {
      obj.filesystem = message.filesystem;
    }
    if (message.git !== false) {
      obj.git = message.git;
    }
    if (message.pty !== false) {
      obj.pty = message.pty;
    }
    if (message.desktop !== false) {
      obj.desktop = message.desktop;
    }
    if (message.consentedWholeMachine !== false) {
      obj.consentedWholeMachine = message.consentedWholeMachine;
    }
    if (message.consentedScreenControl !== false) {
      obj.consentedScreenControl = message.consentedScreenControl;
    }
    if (message.display !== undefined) {
      obj.display = Display.toJSON(message.display);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Capabilities>, I>>(base?: I): Capabilities {
    return Capabilities.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Capabilities>, I>>(object: I): Capabilities {
    const message = createBaseCapabilities();
    message.exec = object.exec ?? false;
    message.filesystem = object.filesystem ?? false;
    message.git = object.git ?? false;
    message.pty = object.pty ?? false;
    message.desktop = object.desktop ?? false;
    message.consentedWholeMachine = object.consentedWholeMachine ?? false;
    message.consentedScreenControl = object.consentedScreenControl ?? false;
    message.display = (object.display !== undefined && object.display !== null)
      ? Display.fromPartial(object.display)
      : undefined;
    return message;
  },
};

function createBaseDisplay(): Display {
  return { id: "", width: 0, height: 0, virtual: false };
}

export const Display: MessageFns<Display> = {
  encode(message: Display, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.width !== 0) {
      writer.uint32(16).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(24).uint32(message.height);
    }
    if (message.virtual !== false) {
      writer.uint32(32).bool(message.virtual);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Display {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.virtual = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Display {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      virtual: isSet(object.virtual) ? globalThis.Boolean(object.virtual) : false,
    };
  },

  toJSON(message: Display): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.virtual !== false) {
      obj.virtual = message.virtual;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Display>, I>>(base?: I): Display {
    return Display.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Display>, I>>(object: I): Display {
    const message = createBaseDisplay();
    message.id = object.id ?? "";
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.virtual = object.virtual ?? false;
    return message;
  },
};

function createBaseDeviceAuthStartRequest(): DeviceAuthStartRequest {
  return { installFingerprint: "", os: 0, arch: 0, machineName: "", updateChannel: "" };
}

export const DeviceAuthStartRequest: MessageFns<DeviceAuthStartRequest> = {
  encode(message: DeviceAuthStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.installFingerprint !== "") {
      writer.uint32(10).string(message.installFingerprint);
    }
    if (message.os !== 0) {
      writer.uint32(16).int32(message.os);
    }
    if (message.arch !== 0) {
      writer.uint32(24).int32(message.arch);
    }
    if (message.machineName !== "") {
      writer.uint32(34).string(message.machineName);
    }
    if (message.updateChannel !== "") {
      writer.uint32(42).string(message.updateChannel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAuthStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAuthStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.installFingerprint = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.os = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.arch = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.machineName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updateChannel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAuthStartRequest {
    return {
      installFingerprint: isSet(object.installFingerprint)
        ? globalThis.String(object.installFingerprint)
        : isSet(object.install_fingerprint)
        ? globalThis.String(object.install_fingerprint)
        : "",
      os: isSet(object.os) ? osFromJSON(object.os) : 0,
      arch: isSet(object.arch) ? archFromJSON(object.arch) : 0,
      machineName: isSet(object.machineName)
        ? globalThis.String(object.machineName)
        : isSet(object.machine_name)
        ? globalThis.String(object.machine_name)
        : "",
      updateChannel: isSet(object.updateChannel)
        ? globalThis.String(object.updateChannel)
        : isSet(object.update_channel)
        ? globalThis.String(object.update_channel)
        : "",
    };
  },

  toJSON(message: DeviceAuthStartRequest): unknown {
    const obj: any = {};
    if (message.installFingerprint !== "") {
      obj.installFingerprint = message.installFingerprint;
    }
    if (message.os !== 0) {
      obj.os = osToJSON(message.os);
    }
    if (message.arch !== 0) {
      obj.arch = archToJSON(message.arch);
    }
    if (message.machineName !== "") {
      obj.machineName = message.machineName;
    }
    if (message.updateChannel !== "") {
      obj.updateChannel = message.updateChannel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAuthStartRequest>, I>>(base?: I): DeviceAuthStartRequest {
    return DeviceAuthStartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAuthStartRequest>, I>>(object: I): DeviceAuthStartRequest {
    const message = createBaseDeviceAuthStartRequest();
    message.installFingerprint = object.installFingerprint ?? "";
    message.os = object.os ?? 0;
    message.arch = object.arch ?? 0;
    message.machineName = object.machineName ?? "";
    message.updateChannel = object.updateChannel ?? "";
    return message;
  },
};

function createBaseDeviceAuthStartResponse(): DeviceAuthStartResponse {
  return {
    userCode: "",
    deviceCode: "",
    verificationUri: "",
    verificationUriComplete: "",
    expiresInSeconds: 0,
    pollIntervalSeconds: 0,
  };
}

export const DeviceAuthStartResponse: MessageFns<DeviceAuthStartResponse> = {
  encode(message: DeviceAuthStartResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userCode !== "") {
      writer.uint32(10).string(message.userCode);
    }
    if (message.deviceCode !== "") {
      writer.uint32(18).string(message.deviceCode);
    }
    if (message.verificationUri !== "") {
      writer.uint32(26).string(message.verificationUri);
    }
    if (message.verificationUriComplete !== "") {
      writer.uint32(34).string(message.verificationUriComplete);
    }
    if (message.expiresInSeconds !== 0) {
      writer.uint32(40).uint32(message.expiresInSeconds);
    }
    if (message.pollIntervalSeconds !== 0) {
      writer.uint32(48).uint32(message.pollIntervalSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAuthStartResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAuthStartResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deviceCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.verificationUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.verificationUriComplete = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.expiresInSeconds = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.pollIntervalSeconds = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAuthStartResponse {
    return {
      userCode: isSet(object.userCode)
        ? globalThis.String(object.userCode)
        : isSet(object.user_code)
        ? globalThis.String(object.user_code)
        : "",
      deviceCode: isSet(object.deviceCode)
        ? globalThis.String(object.deviceCode)
        : isSet(object.device_code)
        ? globalThis.String(object.device_code)
        : "",
      verificationUri: isSet(object.verificationUri)
        ? globalThis.String(object.verificationUri)
        : isSet(object.verification_uri)
        ? globalThis.String(object.verification_uri)
        : "",
      verificationUriComplete: isSet(object.verificationUriComplete)
        ? globalThis.String(object.verificationUriComplete)
        : isSet(object.verification_uri_complete)
        ? globalThis.String(object.verification_uri_complete)
        : "",
      expiresInSeconds: isSet(object.expiresInSeconds)
        ? globalThis.Number(object.expiresInSeconds)
        : isSet(object.expires_in_seconds)
        ? globalThis.Number(object.expires_in_seconds)
        : 0,
      pollIntervalSeconds: isSet(object.pollIntervalSeconds)
        ? globalThis.Number(object.pollIntervalSeconds)
        : isSet(object.poll_interval_seconds)
        ? globalThis.Number(object.poll_interval_seconds)
        : 0,
    };
  },

  toJSON(message: DeviceAuthStartResponse): unknown {
    const obj: any = {};
    if (message.userCode !== "") {
      obj.userCode = message.userCode;
    }
    if (message.deviceCode !== "") {
      obj.deviceCode = message.deviceCode;
    }
    if (message.verificationUri !== "") {
      obj.verificationUri = message.verificationUri;
    }
    if (message.verificationUriComplete !== "") {
      obj.verificationUriComplete = message.verificationUriComplete;
    }
    if (message.expiresInSeconds !== 0) {
      obj.expiresInSeconds = Math.round(message.expiresInSeconds);
    }
    if (message.pollIntervalSeconds !== 0) {
      obj.pollIntervalSeconds = Math.round(message.pollIntervalSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAuthStartResponse>, I>>(base?: I): DeviceAuthStartResponse {
    return DeviceAuthStartResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAuthStartResponse>, I>>(object: I): DeviceAuthStartResponse {
    const message = createBaseDeviceAuthStartResponse();
    message.userCode = object.userCode ?? "";
    message.deviceCode = object.deviceCode ?? "";
    message.verificationUri = object.verificationUri ?? "";
    message.verificationUriComplete = object.verificationUriComplete ?? "";
    message.expiresInSeconds = object.expiresInSeconds ?? 0;
    message.pollIntervalSeconds = object.pollIntervalSeconds ?? 0;
    return message;
  },
};

function createBaseDeviceAuthPollRequest(): DeviceAuthPollRequest {
  return { deviceCode: "" };
}

export const DeviceAuthPollRequest: MessageFns<DeviceAuthPollRequest> = {
  encode(message: DeviceAuthPollRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceCode !== "") {
      writer.uint32(10).string(message.deviceCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAuthPollRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAuthPollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAuthPollRequest {
    return {
      deviceCode: isSet(object.deviceCode)
        ? globalThis.String(object.deviceCode)
        : isSet(object.device_code)
        ? globalThis.String(object.device_code)
        : "",
    };
  },

  toJSON(message: DeviceAuthPollRequest): unknown {
    const obj: any = {};
    if (message.deviceCode !== "") {
      obj.deviceCode = message.deviceCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAuthPollRequest>, I>>(base?: I): DeviceAuthPollRequest {
    return DeviceAuthPollRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAuthPollRequest>, I>>(object: I): DeviceAuthPollRequest {
    const message = createBaseDeviceAuthPollRequest();
    message.deviceCode = object.deviceCode ?? "";
    return message;
  },
};

function createBaseDeviceAuthPollResponse(): DeviceAuthPollResponse {
  return { state: 0, credentials: undefined };
}

export const DeviceAuthPollResponse: MessageFns<DeviceAuthPollResponse> = {
  encode(message: DeviceAuthPollResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.credentials !== undefined) {
      EnrollmentCredentials.encode(message.credentials, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAuthPollResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAuthPollResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentials = EnrollmentCredentials.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAuthPollResponse {
    return {
      state: isSet(object.state) ? deviceAuthStateFromJSON(object.state) : 0,
      credentials: isSet(object.credentials) ? EnrollmentCredentials.fromJSON(object.credentials) : undefined,
    };
  },

  toJSON(message: DeviceAuthPollResponse): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = deviceAuthStateToJSON(message.state);
    }
    if (message.credentials !== undefined) {
      obj.credentials = EnrollmentCredentials.toJSON(message.credentials);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceAuthPollResponse>, I>>(base?: I): DeviceAuthPollResponse {
    return DeviceAuthPollResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceAuthPollResponse>, I>>(object: I): DeviceAuthPollResponse {
    const message = createBaseDeviceAuthPollResponse();
    message.state = object.state ?? 0;
    message.credentials = (object.credentials !== undefined && object.credentials !== null)
      ? EnrollmentCredentials.fromPartial(object.credentials)
      : undefined;
    return message;
  },
};

function createBaseEnrollmentCredentials(): EnrollmentCredentials {
  return {
    agentId: "",
    workspaceId: "",
    natsCredentials: "",
    natsUrls: [],
    relayUrl: "",
    updatePubkey: "",
    consentedWholeMachine: false,
    consentedScreenControl: false,
    relayToken: "",
  };
}

export const EnrollmentCredentials: MessageFns<EnrollmentCredentials> = {
  encode(message: EnrollmentCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.natsCredentials !== "") {
      writer.uint32(26).string(message.natsCredentials);
    }
    for (const v of message.natsUrls) {
      writer.uint32(34).string(v!);
    }
    if (message.relayUrl !== "") {
      writer.uint32(42).string(message.relayUrl);
    }
    if (message.updatePubkey !== "") {
      writer.uint32(50).string(message.updatePubkey);
    }
    if (message.consentedWholeMachine !== false) {
      writer.uint32(56).bool(message.consentedWholeMachine);
    }
    if (message.consentedScreenControl !== false) {
      writer.uint32(64).bool(message.consentedScreenControl);
    }
    if (message.relayToken !== "") {
      writer.uint32(74).string(message.relayToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnrollmentCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnrollmentCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.natsCredentials = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.natsUrls.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.relayUrl = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatePubkey = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.consentedWholeMachine = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.consentedScreenControl = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.relayToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnrollmentCredentials {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
      natsCredentials: isSet(object.natsCredentials)
        ? globalThis.String(object.natsCredentials)
        : isSet(object.nats_credentials)
        ? globalThis.String(object.nats_credentials)
        : "",
      natsUrls: globalThis.Array.isArray(object?.natsUrls)
        ? object.natsUrls.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.nats_urls)
        ? object.nats_urls.map((e: any) => globalThis.String(e))
        : [],
      relayUrl: isSet(object.relayUrl)
        ? globalThis.String(object.relayUrl)
        : isSet(object.relay_url)
        ? globalThis.String(object.relay_url)
        : "",
      updatePubkey: isSet(object.updatePubkey)
        ? globalThis.String(object.updatePubkey)
        : isSet(object.update_pubkey)
        ? globalThis.String(object.update_pubkey)
        : "",
      consentedWholeMachine: isSet(object.consentedWholeMachine)
        ? globalThis.Boolean(object.consentedWholeMachine)
        : isSet(object.consented_whole_machine)
        ? globalThis.Boolean(object.consented_whole_machine)
        : false,
      consentedScreenControl: isSet(object.consentedScreenControl)
        ? globalThis.Boolean(object.consentedScreenControl)
        : isSet(object.consented_screen_control)
        ? globalThis.Boolean(object.consented_screen_control)
        : false,
      relayToken: isSet(object.relayToken)
        ? globalThis.String(object.relayToken)
        : isSet(object.relay_token)
        ? globalThis.String(object.relay_token)
        : "",
    };
  },

  toJSON(message: EnrollmentCredentials): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    if (message.natsCredentials !== "") {
      obj.natsCredentials = message.natsCredentials;
    }
    if (message.natsUrls?.length) {
      obj.natsUrls = message.natsUrls;
    }
    if (message.relayUrl !== "") {
      obj.relayUrl = message.relayUrl;
    }
    if (message.updatePubkey !== "") {
      obj.updatePubkey = message.updatePubkey;
    }
    if (message.consentedWholeMachine !== false) {
      obj.consentedWholeMachine = message.consentedWholeMachine;
    }
    if (message.consentedScreenControl !== false) {
      obj.consentedScreenControl = message.consentedScreenControl;
    }
    if (message.relayToken !== "") {
      obj.relayToken = message.relayToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EnrollmentCredentials>, I>>(base?: I): EnrollmentCredentials {
    return EnrollmentCredentials.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EnrollmentCredentials>, I>>(object: I): EnrollmentCredentials {
    const message = createBaseEnrollmentCredentials();
    message.agentId = object.agentId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.natsCredentials = object.natsCredentials ?? "";
    message.natsUrls = object.natsUrls?.map((e) => e) || [];
    message.relayUrl = object.relayUrl ?? "";
    message.updatePubkey = object.updatePubkey ?? "";
    message.consentedWholeMachine = object.consentedWholeMachine ?? false;
    message.consentedScreenControl = object.consentedScreenControl ?? false;
    message.relayToken = object.relayToken ?? "";
    return message;
  },
};

function createBaseExecRequest(): ExecRequest {
  return { command: [], shell: false, cwd: "", env: {}, stdin: new Uint8Array(0), timeoutMs: 0 };
}

export const ExecRequest: MessageFns<ExecRequest> = {
  encode(message: ExecRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.command) {
      writer.uint32(10).string(v!);
    }
    if (message.shell !== false) {
      writer.uint32(16).bool(message.shell);
    }
    if (message.cwd !== "") {
      writer.uint32(26).string(message.cwd);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      ExecRequest_EnvEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.stdin.length !== 0) {
      writer.uint32(42).bytes(message.stdin);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(48).uint32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.shell = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ExecRequest_EnvEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.env[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.stdin = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecRequest {
    return {
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      shell: isSet(object.shell) ? globalThis.Boolean(object.shell) : false,
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      stdin: isSet(object.stdin) ? bytesFromBase64(object.stdin) : new Uint8Array(0),
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
    };
  },

  toJSON(message: ExecRequest): unknown {
    const obj: any = {};
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.shell !== false) {
      obj.shell = message.shell;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.stdin.length !== 0) {
      obj.stdin = base64FromBytes(message.stdin);
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecRequest>, I>>(base?: I): ExecRequest {
    return ExecRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecRequest>, I>>(object: I): ExecRequest {
    const message = createBaseExecRequest();
    message.command = object.command?.map((e) => e) || [];
    message.shell = object.shell ?? false;
    message.cwd = object.cwd ?? "";
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.stdin = object.stdin ?? new Uint8Array(0);
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseExecRequest_EnvEntry(): ExecRequest_EnvEntry {
  return { key: "", value: "" };
}

export const ExecRequest_EnvEntry: MessageFns<ExecRequest_EnvEntry> = {
  encode(message: ExecRequest_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecRequest_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecRequest_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecRequest_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ExecRequest_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecRequest_EnvEntry>, I>>(base?: I): ExecRequest_EnvEntry {
    return ExecRequest_EnvEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecRequest_EnvEntry>, I>>(object: I): ExecRequest_EnvEntry {
    const message = createBaseExecRequest_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseExecResponse(): ExecResponse {
  return { exitCode: 0, stdout: new Uint8Array(0), stderr: new Uint8Array(0), timedOut: false, durationMs: "0" };
}

export const ExecResponse: MessageFns<ExecResponse> = {
  encode(message: ExecResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    if (message.stdout.length !== 0) {
      writer.uint32(18).bytes(message.stdout);
    }
    if (message.stderr.length !== 0) {
      writer.uint32(26).bytes(message.stderr);
    }
    if (message.timedOut !== false) {
      writer.uint32(32).bool(message.timedOut);
    }
    if (message.durationMs !== "0") {
      writer.uint32(40).uint64(message.durationMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stdout = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stderr = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timedOut = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.durationMs = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecResponse {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      stdout: isSet(object.stdout) ? bytesFromBase64(object.stdout) : new Uint8Array(0),
      stderr: isSet(object.stderr) ? bytesFromBase64(object.stderr) : new Uint8Array(0),
      timedOut: isSet(object.timedOut)
        ? globalThis.Boolean(object.timedOut)
        : isSet(object.timed_out)
        ? globalThis.Boolean(object.timed_out)
        : false,
      durationMs: isSet(object.durationMs)
        ? globalThis.String(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.String(object.duration_ms)
        : "0",
    };
  },

  toJSON(message: ExecResponse): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.stdout.length !== 0) {
      obj.stdout = base64FromBytes(message.stdout);
    }
    if (message.stderr.length !== 0) {
      obj.stderr = base64FromBytes(message.stderr);
    }
    if (message.timedOut !== false) {
      obj.timedOut = message.timedOut;
    }
    if (message.durationMs !== "0") {
      obj.durationMs = message.durationMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecResponse>, I>>(base?: I): ExecResponse {
    return ExecResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecResponse>, I>>(object: I): ExecResponse {
    const message = createBaseExecResponse();
    message.exitCode = object.exitCode ?? 0;
    message.stdout = object.stdout ?? new Uint8Array(0);
    message.stderr = object.stderr ?? new Uint8Array(0);
    message.timedOut = object.timedOut ?? false;
    message.durationMs = object.durationMs ?? "0";
    return message;
  },
};

function createBaseFsReadRequest(): FsReadRequest {
  return { path: "", offset: "0", length: "0" };
}

export const FsReadRequest: MessageFns<FsReadRequest> = {
  encode(message: FsReadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.offset !== "0") {
      writer.uint32(16).uint64(message.offset);
    }
    if (message.length !== "0") {
      writer.uint32(24).uint64(message.length);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.length = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      offset: isSet(object.offset) ? globalThis.String(object.offset) : "0",
      length: isSet(object.length) ? globalThis.String(object.length) : "0",
    };
  },

  toJSON(message: FsReadRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.offset !== "0") {
      obj.offset = message.offset;
    }
    if (message.length !== "0") {
      obj.length = message.length;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsReadRequest>, I>>(base?: I): FsReadRequest {
    return FsReadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsReadRequest>, I>>(object: I): FsReadRequest {
    const message = createBaseFsReadRequest();
    message.path = object.path ?? "";
    message.offset = object.offset ?? "0";
    message.length = object.length ?? "0";
    return message;
  },
};

function createBaseFsReadResponse(): FsReadResponse {
  return { content: new Uint8Array(0), totalSize: "0" };
}

export const FsReadResponse: MessageFns<FsReadResponse> = {
  encode(message: FsReadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content.length !== 0) {
      writer.uint32(10).bytes(message.content);
    }
    if (message.totalSize !== "0") {
      writer.uint32(16).uint64(message.totalSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsReadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsReadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalSize = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsReadResponse {
    return {
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      totalSize: isSet(object.totalSize)
        ? globalThis.String(object.totalSize)
        : isSet(object.total_size)
        ? globalThis.String(object.total_size)
        : "0",
    };
  },

  toJSON(message: FsReadResponse): unknown {
    const obj: any = {};
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.totalSize !== "0") {
      obj.totalSize = message.totalSize;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsReadResponse>, I>>(base?: I): FsReadResponse {
    return FsReadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsReadResponse>, I>>(object: I): FsReadResponse {
    const message = createBaseFsReadResponse();
    message.content = object.content ?? new Uint8Array(0);
    message.totalSize = object.totalSize ?? "0";
    return message;
  },
};

function createBaseFsWriteRequest(): FsWriteRequest {
  return { path: "", content: new Uint8Array(0), createParents: false, append: false, mode: 0 };
}

export const FsWriteRequest: MessageFns<FsWriteRequest> = {
  encode(message: FsWriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.content.length !== 0) {
      writer.uint32(18).bytes(message.content);
    }
    if (message.createParents !== false) {
      writer.uint32(24).bool(message.createParents);
    }
    if (message.append !== false) {
      writer.uint32(32).bool(message.append);
    }
    if (message.mode !== 0) {
      writer.uint32(40).uint32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsWriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.createParents = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.append = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsWriteRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      createParents: isSet(object.createParents)
        ? globalThis.Boolean(object.createParents)
        : isSet(object.create_parents)
        ? globalThis.Boolean(object.create_parents)
        : false,
      append: isSet(object.append) ? globalThis.Boolean(object.append) : false,
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : 0,
    };
  },

  toJSON(message: FsWriteRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.createParents !== false) {
      obj.createParents = message.createParents;
    }
    if (message.append !== false) {
      obj.append = message.append;
    }
    if (message.mode !== 0) {
      obj.mode = Math.round(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsWriteRequest>, I>>(base?: I): FsWriteRequest {
    return FsWriteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsWriteRequest>, I>>(object: I): FsWriteRequest {
    const message = createBaseFsWriteRequest();
    message.path = object.path ?? "";
    message.content = object.content ?? new Uint8Array(0);
    message.createParents = object.createParents ?? false;
    message.append = object.append ?? false;
    message.mode = object.mode ?? 0;
    return message;
  },
};

function createBaseFsWriteResponse(): FsWriteResponse {
  return { bytesWritten: "0" };
}

export const FsWriteResponse: MessageFns<FsWriteResponse> = {
  encode(message: FsWriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bytesWritten !== "0") {
      writer.uint32(8).uint64(message.bytesWritten);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsWriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsWriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.bytesWritten = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsWriteResponse {
    return {
      bytesWritten: isSet(object.bytesWritten)
        ? globalThis.String(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.String(object.bytes_written)
        : "0",
    };
  },

  toJSON(message: FsWriteResponse): unknown {
    const obj: any = {};
    if (message.bytesWritten !== "0") {
      obj.bytesWritten = message.bytesWritten;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsWriteResponse>, I>>(base?: I): FsWriteResponse {
    return FsWriteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsWriteResponse>, I>>(object: I): FsWriteResponse {
    const message = createBaseFsWriteResponse();
    message.bytesWritten = object.bytesWritten ?? "0";
    return message;
  },
};

function createBaseFsListRequest(): FsListRequest {
  return { path: "", recursive: false };
}

export const FsListRequest: MessageFns<FsListRequest> = {
  encode(message: FsListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.recursive !== false) {
      writer.uint32(16).bool(message.recursive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsListRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
    };
  },

  toJSON(message: FsListRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsListRequest>, I>>(base?: I): FsListRequest {
    return FsListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsListRequest>, I>>(object: I): FsListRequest {
    const message = createBaseFsListRequest();
    message.path = object.path ?? "";
    message.recursive = object.recursive ?? false;
    return message;
  },
};

function createBaseFsListResponse(): FsListResponse {
  return { entries: [] };
}

export const FsListResponse: MessageFns<FsListResponse> = {
  encode(message: FsListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      FsEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(FsEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsListResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => FsEntry.fromJSON(e)) : [],
    };
  },

  toJSON(message: FsListResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => FsEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsListResponse>, I>>(base?: I): FsListResponse {
    return FsListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsListResponse>, I>>(object: I): FsListResponse {
    const message = createBaseFsListResponse();
    message.entries = object.entries?.map((e) => FsEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFsEntry(): FsEntry {
  return { name: "", path: "", kind: 0, size: "0", modifiedMs: "0", mode: 0 };
}

export const FsEntry: MessageFns<FsEntry> = {
  encode(message: FsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.size !== "0") {
      writer.uint32(32).uint64(message.size);
    }
    if (message.modifiedMs !== "0") {
      writer.uint32(40).int64(message.modifiedMs);
    }
    if (message.mode !== 0) {
      writer.uint32(48).uint32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.modifiedMs = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsEntry {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      kind: isSet(object.kind) ? fsEntryKindFromJSON(object.kind) : 0,
      size: isSet(object.size) ? globalThis.String(object.size) : "0",
      modifiedMs: isSet(object.modifiedMs)
        ? globalThis.String(object.modifiedMs)
        : isSet(object.modified_ms)
        ? globalThis.String(object.modified_ms)
        : "0",
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : 0,
    };
  },

  toJSON(message: FsEntry): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.kind !== 0) {
      obj.kind = fsEntryKindToJSON(message.kind);
    }
    if (message.size !== "0") {
      obj.size = message.size;
    }
    if (message.modifiedMs !== "0") {
      obj.modifiedMs = message.modifiedMs;
    }
    if (message.mode !== 0) {
      obj.mode = Math.round(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsEntry>, I>>(base?: I): FsEntry {
    return FsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsEntry>, I>>(object: I): FsEntry {
    const message = createBaseFsEntry();
    message.name = object.name ?? "";
    message.path = object.path ?? "";
    message.kind = object.kind ?? 0;
    message.size = object.size ?? "0";
    message.modifiedMs = object.modifiedMs ?? "0";
    message.mode = object.mode ?? 0;
    return message;
  },
};

function createBaseFsMkdirRequest(): FsMkdirRequest {
  return { path: "", parents: false, mode: 0 };
}

export const FsMkdirRequest: MessageFns<FsMkdirRequest> = {
  encode(message: FsMkdirRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.parents !== false) {
      writer.uint32(16).bool(message.parents);
    }
    if (message.mode !== 0) {
      writer.uint32(24).uint32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsMkdirRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsMkdirRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.parents = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsMkdirRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      parents: isSet(object.parents) ? globalThis.Boolean(object.parents) : false,
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : 0,
    };
  },

  toJSON(message: FsMkdirRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.parents !== false) {
      obj.parents = message.parents;
    }
    if (message.mode !== 0) {
      obj.mode = Math.round(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsMkdirRequest>, I>>(base?: I): FsMkdirRequest {
    return FsMkdirRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsMkdirRequest>, I>>(object: I): FsMkdirRequest {
    const message = createBaseFsMkdirRequest();
    message.path = object.path ?? "";
    message.parents = object.parents ?? false;
    message.mode = object.mode ?? 0;
    return message;
  },
};

function createBaseFsMkdirResponse(): FsMkdirResponse {
  return {};
}

export const FsMkdirResponse: MessageFns<FsMkdirResponse> = {
  encode(_: FsMkdirResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsMkdirResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsMkdirResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FsMkdirResponse {
    return {};
  },

  toJSON(_: FsMkdirResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FsMkdirResponse>, I>>(base?: I): FsMkdirResponse {
    return FsMkdirResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsMkdirResponse>, I>>(_: I): FsMkdirResponse {
    const message = createBaseFsMkdirResponse();
    return message;
  },
};

function createBaseFsMoveRequest(): FsMoveRequest {
  return { from: "", to: "", overwrite: false };
}

export const FsMoveRequest: MessageFns<FsMoveRequest> = {
  encode(message: FsMoveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== "") {
      writer.uint32(10).string(message.from);
    }
    if (message.to !== "") {
      writer.uint32(18).string(message.to);
    }
    if (message.overwrite !== false) {
      writer.uint32(24).bool(message.overwrite);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsMoveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsMoveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.overwrite = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsMoveRequest {
    return {
      from: isSet(object.from) ? globalThis.String(object.from) : "",
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      overwrite: isSet(object.overwrite) ? globalThis.Boolean(object.overwrite) : false,
    };
  },

  toJSON(message: FsMoveRequest): unknown {
    const obj: any = {};
    if (message.from !== "") {
      obj.from = message.from;
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.overwrite !== false) {
      obj.overwrite = message.overwrite;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsMoveRequest>, I>>(base?: I): FsMoveRequest {
    return FsMoveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsMoveRequest>, I>>(object: I): FsMoveRequest {
    const message = createBaseFsMoveRequest();
    message.from = object.from ?? "";
    message.to = object.to ?? "";
    message.overwrite = object.overwrite ?? false;
    return message;
  },
};

function createBaseFsMoveResponse(): FsMoveResponse {
  return {};
}

export const FsMoveResponse: MessageFns<FsMoveResponse> = {
  encode(_: FsMoveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsMoveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsMoveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FsMoveResponse {
    return {};
  },

  toJSON(_: FsMoveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FsMoveResponse>, I>>(base?: I): FsMoveResponse {
    return FsMoveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsMoveResponse>, I>>(_: I): FsMoveResponse {
    const message = createBaseFsMoveResponse();
    return message;
  },
};

function createBaseFsStatRequest(): FsStatRequest {
  return { path: "" };
}

export const FsStatRequest: MessageFns<FsStatRequest> = {
  encode(message: FsStatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsStatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsStatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsStatRequest {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: FsStatRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsStatRequest>, I>>(base?: I): FsStatRequest {
    return FsStatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsStatRequest>, I>>(object: I): FsStatRequest {
    const message = createBaseFsStatRequest();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseFsStatResponse(): FsStatResponse {
  return { exists: false, entry: undefined };
}

export const FsStatResponse: MessageFns<FsStatResponse> = {
  encode(message: FsStatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exists !== false) {
      writer.uint32(8).bool(message.exists);
    }
    if (message.entry !== undefined) {
      FsEntry.encode(message.entry, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsStatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsStatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entry = FsEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsStatResponse {
    return {
      exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false,
      entry: isSet(object.entry) ? FsEntry.fromJSON(object.entry) : undefined,
    };
  },

  toJSON(message: FsStatResponse): unknown {
    const obj: any = {};
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    if (message.entry !== undefined) {
      obj.entry = FsEntry.toJSON(message.entry);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsStatResponse>, I>>(base?: I): FsStatResponse {
    return FsStatResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsStatResponse>, I>>(object: I): FsStatResponse {
    const message = createBaseFsStatResponse();
    message.exists = object.exists ?? false;
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? FsEntry.fromPartial(object.entry)
      : undefined;
    return message;
  },
};

function createBaseFsRemoveRequest(): FsRemoveRequest {
  return { path: "", recursive: false };
}

export const FsRemoveRequest: MessageFns<FsRemoveRequest> = {
  encode(message: FsRemoveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.recursive !== false) {
      writer.uint32(16).bool(message.recursive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsRemoveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsRemoveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsRemoveRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
    };
  },

  toJSON(message: FsRemoveRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FsRemoveRequest>, I>>(base?: I): FsRemoveRequest {
    return FsRemoveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsRemoveRequest>, I>>(object: I): FsRemoveRequest {
    const message = createBaseFsRemoveRequest();
    message.path = object.path ?? "";
    message.recursive = object.recursive ?? false;
    return message;
  },
};

function createBaseFsRemoveResponse(): FsRemoveResponse {
  return {};
}

export const FsRemoveResponse: MessageFns<FsRemoveResponse> = {
  encode(_: FsRemoveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsRemoveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsRemoveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FsRemoveResponse {
    return {};
  },

  toJSON(_: FsRemoveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FsRemoveResponse>, I>>(base?: I): FsRemoveResponse {
    return FsRemoveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FsRemoveResponse>, I>>(_: I): FsRemoveResponse {
    const message = createBaseFsRemoveResponse();
    return message;
  },
};

function createBaseGitRequest(): GitRequest {
  return { op: 0, cwd: "", args: [] };
}

export const GitRequest: MessageFns<GitRequest> = {
  encode(message: GitRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.cwd !== "") {
      writer.uint32(18).string(message.cwd);
    }
    for (const v of message.args) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitRequest {
    return {
      op: isSet(object.op) ? gitOpFromJSON(object.op) : 0,
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GitRequest): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = gitOpToJSON(message.op);
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GitRequest>, I>>(base?: I): GitRequest {
    return GitRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GitRequest>, I>>(object: I): GitRequest {
    const message = createBaseGitRequest();
    message.op = object.op ?? 0;
    message.cwd = object.cwd ?? "";
    message.args = object.args?.map((e) => e) || [];
    return message;
  },
};

function createBaseGitResponse(): GitResponse {
  return { exitCode: 0, stdout: new Uint8Array(0), stderr: new Uint8Array(0), status: undefined };
}

export const GitResponse: MessageFns<GitResponse> = {
  encode(message: GitResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    if (message.stdout.length !== 0) {
      writer.uint32(18).bytes(message.stdout);
    }
    if (message.stderr.length !== 0) {
      writer.uint32(26).bytes(message.stderr);
    }
    if (message.status !== undefined) {
      GitStatus.encode(message.status, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stdout = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stderr = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = GitStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitResponse {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      stdout: isSet(object.stdout) ? bytesFromBase64(object.stdout) : new Uint8Array(0),
      stderr: isSet(object.stderr) ? bytesFromBase64(object.stderr) : new Uint8Array(0),
      status: isSet(object.status) ? GitStatus.fromJSON(object.status) : undefined,
    };
  },

  toJSON(message: GitResponse): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.stdout.length !== 0) {
      obj.stdout = base64FromBytes(message.stdout);
    }
    if (message.stderr.length !== 0) {
      obj.stderr = base64FromBytes(message.stderr);
    }
    if (message.status !== undefined) {
      obj.status = GitStatus.toJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GitResponse>, I>>(base?: I): GitResponse {
    return GitResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GitResponse>, I>>(object: I): GitResponse {
    const message = createBaseGitResponse();
    message.exitCode = object.exitCode ?? 0;
    message.stdout = object.stdout ?? new Uint8Array(0);
    message.stderr = object.stderr ?? new Uint8Array(0);
    message.status = (object.status !== undefined && object.status !== null)
      ? GitStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseGitStatus(): GitStatus {
  return { branch: "", upstream: "", ahead: 0, behind: 0, files: [], clean: false };
}

export const GitStatus: MessageFns<GitStatus> = {
  encode(message: GitStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.branch !== "") {
      writer.uint32(10).string(message.branch);
    }
    if (message.upstream !== "") {
      writer.uint32(18).string(message.upstream);
    }
    if (message.ahead !== 0) {
      writer.uint32(24).int32(message.ahead);
    }
    if (message.behind !== 0) {
      writer.uint32(32).int32(message.behind);
    }
    for (const v of message.files) {
      GitFileStatus.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.clean !== false) {
      writer.uint32(48).bool(message.clean);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.upstream = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ahead = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.behind = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.files.push(GitFileStatus.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.clean = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatus {
    return {
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      upstream: isSet(object.upstream) ? globalThis.String(object.upstream) : "",
      ahead: isSet(object.ahead) ? globalThis.Number(object.ahead) : 0,
      behind: isSet(object.behind) ? globalThis.Number(object.behind) : 0,
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => GitFileStatus.fromJSON(e)) : [],
      clean: isSet(object.clean) ? globalThis.Boolean(object.clean) : false,
    };
  },

  toJSON(message: GitStatus): unknown {
    const obj: any = {};
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.upstream !== "") {
      obj.upstream = message.upstream;
    }
    if (message.ahead !== 0) {
      obj.ahead = Math.round(message.ahead);
    }
    if (message.behind !== 0) {
      obj.behind = Math.round(message.behind);
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => GitFileStatus.toJSON(e));
    }
    if (message.clean !== false) {
      obj.clean = message.clean;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GitStatus>, I>>(base?: I): GitStatus {
    return GitStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GitStatus>, I>>(object: I): GitStatus {
    const message = createBaseGitStatus();
    message.branch = object.branch ?? "";
    message.upstream = object.upstream ?? "";
    message.ahead = object.ahead ?? 0;
    message.behind = object.behind ?? 0;
    message.files = object.files?.map((e) => GitFileStatus.fromPartial(e)) || [];
    message.clean = object.clean ?? false;
    return message;
  },
};

function createBaseGitFileStatus(): GitFileStatus {
  return { path: "", code: "", staged: false };
}

export const GitFileStatus: MessageFns<GitFileStatus> = {
  encode(message: GitFileStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.staged !== false) {
      writer.uint32(24).bool(message.staged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitFileStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitFileStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.staged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitFileStatus {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      staged: isSet(object.staged) ? globalThis.Boolean(object.staged) : false,
    };
  },

  toJSON(message: GitFileStatus): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.staged !== false) {
      obj.staged = message.staged;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GitFileStatus>, I>>(base?: I): GitFileStatus {
    return GitFileStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GitFileStatus>, I>>(object: I): GitFileStatus {
    const message = createBaseGitFileStatus();
    message.path = object.path ?? "";
    message.code = object.code ?? "";
    message.staged = object.staged ?? false;
    return message;
  },
};

function createBasePtyOpenRequest(): PtyOpenRequest {
  return { command: [], cwd: "", env: {}, cols: 0, rows: 0, term: "" };
}

export const PtyOpenRequest: MessageFns<PtyOpenRequest> = {
  encode(message: PtyOpenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.command) {
      writer.uint32(10).string(v!);
    }
    if (message.cwd !== "") {
      writer.uint32(18).string(message.cwd);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      PtyOpenRequest_EnvEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.cols !== 0) {
      writer.uint32(32).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(40).uint32(message.rows);
    }
    if (message.term !== "") {
      writer.uint32(50).string(message.term);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyOpenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyOpenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = PtyOpenRequest_EnvEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.env[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.term = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyOpenRequest {
    return {
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      term: isSet(object.term) ? globalThis.String(object.term) : "",
    };
  },

  toJSON(message: PtyOpenRequest): unknown {
    const obj: any = {};
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.term !== "") {
      obj.term = message.term;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyOpenRequest>, I>>(base?: I): PtyOpenRequest {
    return PtyOpenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyOpenRequest>, I>>(object: I): PtyOpenRequest {
    const message = createBasePtyOpenRequest();
    message.command = object.command?.map((e) => e) || [];
    message.cwd = object.cwd ?? "";
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    message.term = object.term ?? "";
    return message;
  },
};

function createBasePtyOpenRequest_EnvEntry(): PtyOpenRequest_EnvEntry {
  return { key: "", value: "" };
}

export const PtyOpenRequest_EnvEntry: MessageFns<PtyOpenRequest_EnvEntry> = {
  encode(message: PtyOpenRequest_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyOpenRequest_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyOpenRequest_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyOpenRequest_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PtyOpenRequest_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyOpenRequest_EnvEntry>, I>>(base?: I): PtyOpenRequest_EnvEntry {
    return PtyOpenRequest_EnvEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyOpenRequest_EnvEntry>, I>>(object: I): PtyOpenRequest_EnvEntry {
    const message = createBasePtyOpenRequest_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePtyOpenResponse(): PtyOpenResponse {
  return { ptyId: "", channel: undefined };
}

export const PtyOpenResponse: MessageFns<PtyOpenResponse> = {
  encode(message: PtyOpenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== "") {
      writer.uint32(10).string(message.ptyId);
    }
    if (message.channel !== undefined) {
      StreamChannel.encode(message.channel, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyOpenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyOpenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ptyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channel = StreamChannel.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyOpenResponse {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.String(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.String(object.pty_id)
        : "",
      channel: isSet(object.channel) ? StreamChannel.fromJSON(object.channel) : undefined,
    };
  },

  toJSON(message: PtyOpenResponse): unknown {
    const obj: any = {};
    if (message.ptyId !== "") {
      obj.ptyId = message.ptyId;
    }
    if (message.channel !== undefined) {
      obj.channel = StreamChannel.toJSON(message.channel);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyOpenResponse>, I>>(base?: I): PtyOpenResponse {
    return PtyOpenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyOpenResponse>, I>>(object: I): PtyOpenResponse {
    const message = createBasePtyOpenResponse();
    message.ptyId = object.ptyId ?? "";
    message.channel = (object.channel !== undefined && object.channel !== null)
      ? StreamChannel.fromPartial(object.channel)
      : undefined;
    return message;
  },
};

function createBasePtyWriteRequest(): PtyWriteRequest {
  return { ptyId: "", data: new Uint8Array(0) };
}

export const PtyWriteRequest: MessageFns<PtyWriteRequest> = {
  encode(message: PtyWriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== "") {
      writer.uint32(10).string(message.ptyId);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyWriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ptyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyWriteRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.String(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.String(object.pty_id)
        : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: PtyWriteRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== "") {
      obj.ptyId = message.ptyId;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyWriteRequest>, I>>(base?: I): PtyWriteRequest {
    return PtyWriteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyWriteRequest>, I>>(object: I): PtyWriteRequest {
    const message = createBasePtyWriteRequest();
    message.ptyId = object.ptyId ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBasePtyWriteResponse(): PtyWriteResponse {
  return { bytesWritten: "0" };
}

export const PtyWriteResponse: MessageFns<PtyWriteResponse> = {
  encode(message: PtyWriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bytesWritten !== "0") {
      writer.uint32(8).uint64(message.bytesWritten);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyWriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyWriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.bytesWritten = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyWriteResponse {
    return {
      bytesWritten: isSet(object.bytesWritten)
        ? globalThis.String(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.String(object.bytes_written)
        : "0",
    };
  },

  toJSON(message: PtyWriteResponse): unknown {
    const obj: any = {};
    if (message.bytesWritten !== "0") {
      obj.bytesWritten = message.bytesWritten;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyWriteResponse>, I>>(base?: I): PtyWriteResponse {
    return PtyWriteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyWriteResponse>, I>>(object: I): PtyWriteResponse {
    const message = createBasePtyWriteResponse();
    message.bytesWritten = object.bytesWritten ?? "0";
    return message;
  },
};

function createBasePtyResizeRequest(): PtyResizeRequest {
  return { ptyId: "", cols: 0, rows: 0 };
}

export const PtyResizeRequest: MessageFns<PtyResizeRequest> = {
  encode(message: PtyResizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== "") {
      writer.uint32(10).string(message.ptyId);
    }
    if (message.cols !== 0) {
      writer.uint32(16).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(24).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyResizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyResizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ptyId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyResizeRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.String(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.String(object.pty_id)
        : "",
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: PtyResizeRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== "") {
      obj.ptyId = message.ptyId;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyResizeRequest>, I>>(base?: I): PtyResizeRequest {
    return PtyResizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyResizeRequest>, I>>(object: I): PtyResizeRequest {
    const message = createBasePtyResizeRequest();
    message.ptyId = object.ptyId ?? "";
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBasePtyResizeResponse(): PtyResizeResponse {
  return {};
}

export const PtyResizeResponse: MessageFns<PtyResizeResponse> = {
  encode(_: PtyResizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyResizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyResizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PtyResizeResponse {
    return {};
  },

  toJSON(_: PtyResizeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyResizeResponse>, I>>(base?: I): PtyResizeResponse {
    return PtyResizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyResizeResponse>, I>>(_: I): PtyResizeResponse {
    const message = createBasePtyResizeResponse();
    return message;
  },
};

function createBasePtyCloseRequest(): PtyCloseRequest {
  return { ptyId: "" };
}

export const PtyCloseRequest: MessageFns<PtyCloseRequest> = {
  encode(message: PtyCloseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ptyId !== "") {
      writer.uint32(10).string(message.ptyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyCloseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyCloseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ptyId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyCloseRequest {
    return {
      ptyId: isSet(object.ptyId)
        ? globalThis.String(object.ptyId)
        : isSet(object.pty_id)
        ? globalThis.String(object.pty_id)
        : "",
    };
  },

  toJSON(message: PtyCloseRequest): unknown {
    const obj: any = {};
    if (message.ptyId !== "") {
      obj.ptyId = message.ptyId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyCloseRequest>, I>>(base?: I): PtyCloseRequest {
    return PtyCloseRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyCloseRequest>, I>>(object: I): PtyCloseRequest {
    const message = createBasePtyCloseRequest();
    message.ptyId = object.ptyId ?? "";
    return message;
  },
};

function createBasePtyCloseResponse(): PtyCloseResponse {
  return { exitCode: 0 };
}

export const PtyCloseResponse: MessageFns<PtyCloseResponse> = {
  encode(message: PtyCloseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PtyCloseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePtyCloseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PtyCloseResponse {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
    };
  },

  toJSON(message: PtyCloseResponse): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PtyCloseResponse>, I>>(base?: I): PtyCloseResponse {
    return PtyCloseResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PtyCloseResponse>, I>>(object: I): PtyCloseResponse {
    const message = createBasePtyCloseResponse();
    message.exitCode = object.exitCode ?? 0;
    return message;
  },
};

function createBaseDesktopEnsureRequest(): DesktopEnsureRequest {
  return { width: 0, height: 0 };
}

export const DesktopEnsureRequest: MessageFns<DesktopEnsureRequest> = {
  encode(message: DesktopEnsureRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).uint32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopEnsureRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopEnsureRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DesktopEnsureRequest {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: DesktopEnsureRequest): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopEnsureRequest>, I>>(base?: I): DesktopEnsureRequest {
    return DesktopEnsureRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopEnsureRequest>, I>>(object: I): DesktopEnsureRequest {
    const message = createBaseDesktopEnsureRequest();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseDesktopEnsureResponse(): DesktopEnsureResponse {
  return { channel: undefined, display: undefined };
}

export const DesktopEnsureResponse: MessageFns<DesktopEnsureResponse> = {
  encode(message: DesktopEnsureResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== undefined) {
      StreamChannel.encode(message.channel, writer.uint32(10).fork()).join();
    }
    if (message.display !== undefined) {
      Display.encode(message.display, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopEnsureResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopEnsureResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channel = StreamChannel.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.display = Display.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DesktopEnsureResponse {
    return {
      channel: isSet(object.channel) ? StreamChannel.fromJSON(object.channel) : undefined,
      display: isSet(object.display) ? Display.fromJSON(object.display) : undefined,
    };
  },

  toJSON(message: DesktopEnsureResponse): unknown {
    const obj: any = {};
    if (message.channel !== undefined) {
      obj.channel = StreamChannel.toJSON(message.channel);
    }
    if (message.display !== undefined) {
      obj.display = Display.toJSON(message.display);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopEnsureResponse>, I>>(base?: I): DesktopEnsureResponse {
    return DesktopEnsureResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopEnsureResponse>, I>>(object: I): DesktopEnsureResponse {
    const message = createBaseDesktopEnsureResponse();
    message.channel = (object.channel !== undefined && object.channel !== null)
      ? StreamChannel.fromPartial(object.channel)
      : undefined;
    message.display = (object.display !== undefined && object.display !== null)
      ? Display.fromPartial(object.display)
      : undefined;
    return message;
  },
};

function createBaseDesktopInputRequest(): DesktopInputRequest {
  return { event: undefined };
}

export const DesktopInputRequest: MessageFns<DesktopInputRequest> = {
  encode(message: DesktopInputRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.event?.$case) {
      case "pointer":
        PointerEvent.encode(message.event.pointer, writer.uint32(10).fork()).join();
        break;
      case "key":
        KeyEvent.encode(message.event.key, writer.uint32(18).fork()).join();
        break;
      case "scroll":
        ScrollEvent.encode(message.event.scroll, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopInputRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopInputRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = { $case: "pointer", pointer: PointerEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = { $case: "key", key: KeyEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.event = { $case: "scroll", scroll: ScrollEvent.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DesktopInputRequest {
    return {
      event: isSet(object.pointer)
        ? { $case: "pointer", pointer: PointerEvent.fromJSON(object.pointer) }
        : isSet(object.key)
        ? { $case: "key", key: KeyEvent.fromJSON(object.key) }
        : isSet(object.scroll)
        ? { $case: "scroll", scroll: ScrollEvent.fromJSON(object.scroll) }
        : undefined,
    };
  },

  toJSON(message: DesktopInputRequest): unknown {
    const obj: any = {};
    if (message.event?.$case === "pointer") {
      obj.pointer = PointerEvent.toJSON(message.event.pointer);
    } else if (message.event?.$case === "key") {
      obj.key = KeyEvent.toJSON(message.event.key);
    } else if (message.event?.$case === "scroll") {
      obj.scroll = ScrollEvent.toJSON(message.event.scroll);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopInputRequest>, I>>(base?: I): DesktopInputRequest {
    return DesktopInputRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopInputRequest>, I>>(object: I): DesktopInputRequest {
    const message = createBaseDesktopInputRequest();
    switch (object.event?.$case) {
      case "pointer": {
        if (object.event?.pointer !== undefined && object.event?.pointer !== null) {
          message.event = { $case: "pointer", pointer: PointerEvent.fromPartial(object.event.pointer) };
        }
        break;
      }
      case "key": {
        if (object.event?.key !== undefined && object.event?.key !== null) {
          message.event = { $case: "key", key: KeyEvent.fromPartial(object.event.key) };
        }
        break;
      }
      case "scroll": {
        if (object.event?.scroll !== undefined && object.event?.scroll !== null) {
          message.event = { $case: "scroll", scroll: ScrollEvent.fromPartial(object.event.scroll) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseDesktopInputResponse(): DesktopInputResponse {
  return {};
}

export const DesktopInputResponse: MessageFns<DesktopInputResponse> = {
  encode(_: DesktopInputResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopInputResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopInputResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DesktopInputResponse {
    return {};
  },

  toJSON(_: DesktopInputResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopInputResponse>, I>>(base?: I): DesktopInputResponse {
    return DesktopInputResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopInputResponse>, I>>(_: I): DesktopInputResponse {
    const message = createBaseDesktopInputResponse();
    return message;
  },
};

function createBaseDesktopScreenshotRequest(): DesktopScreenshotRequest {
  return {};
}

export const DesktopScreenshotRequest: MessageFns<DesktopScreenshotRequest> = {
  encode(_: DesktopScreenshotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopScreenshotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopScreenshotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DesktopScreenshotRequest {
    return {};
  },

  toJSON(_: DesktopScreenshotRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopScreenshotRequest>, I>>(base?: I): DesktopScreenshotRequest {
    return DesktopScreenshotRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopScreenshotRequest>, I>>(_: I): DesktopScreenshotRequest {
    const message = createBaseDesktopScreenshotRequest();
    return message;
  },
};

function createBaseDesktopScreenshotResponse(): DesktopScreenshotResponse {
  return { png: new Uint8Array(0), width: 0, height: 0 };
}

export const DesktopScreenshotResponse: MessageFns<DesktopScreenshotResponse> = {
  encode(message: DesktopScreenshotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.png.length !== 0) {
      writer.uint32(10).bytes(message.png);
    }
    if (message.width !== 0) {
      writer.uint32(16).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(24).uint32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopScreenshotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopScreenshotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.png = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DesktopScreenshotResponse {
    return {
      png: isSet(object.png) ? bytesFromBase64(object.png) : new Uint8Array(0),
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: DesktopScreenshotResponse): unknown {
    const obj: any = {};
    if (message.png.length !== 0) {
      obj.png = base64FromBytes(message.png);
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopScreenshotResponse>, I>>(base?: I): DesktopScreenshotResponse {
    return DesktopScreenshotResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopScreenshotResponse>, I>>(object: I): DesktopScreenshotResponse {
    const message = createBaseDesktopScreenshotResponse();
    message.png = object.png ?? new Uint8Array(0);
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseHeartbeat(): Heartbeat {
  return { seq: "0", uptimeMs: "0", activeSessions: 0, metrics: undefined, draining: false };
}

export const Heartbeat: MessageFns<Heartbeat> = {
  encode(message: Heartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seq !== "0") {
      writer.uint32(8).uint64(message.seq);
    }
    if (message.uptimeMs !== "0") {
      writer.uint32(16).uint64(message.uptimeMs);
    }
    if (message.activeSessions !== 0) {
      writer.uint32(24).uint32(message.activeSessions);
    }
    if (message.metrics !== undefined) {
      MetricsSample.encode(message.metrics, writer.uint32(34).fork()).join();
    }
    if (message.draining !== false) {
      writer.uint32(40).bool(message.draining);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Heartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seq = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.uptimeMs = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.activeSessions = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metrics = MetricsSample.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.draining = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Heartbeat {
    return {
      seq: isSet(object.seq) ? globalThis.String(object.seq) : "0",
      uptimeMs: isSet(object.uptimeMs)
        ? globalThis.String(object.uptimeMs)
        : isSet(object.uptime_ms)
        ? globalThis.String(object.uptime_ms)
        : "0",
      activeSessions: isSet(object.activeSessions)
        ? globalThis.Number(object.activeSessions)
        : isSet(object.active_sessions)
        ? globalThis.Number(object.active_sessions)
        : 0,
      metrics: isSet(object.metrics) ? MetricsSample.fromJSON(object.metrics) : undefined,
      draining: isSet(object.draining) ? globalThis.Boolean(object.draining) : false,
    };
  },

  toJSON(message: Heartbeat): unknown {
    const obj: any = {};
    if (message.seq !== "0") {
      obj.seq = message.seq;
    }
    if (message.uptimeMs !== "0") {
      obj.uptimeMs = message.uptimeMs;
    }
    if (message.activeSessions !== 0) {
      obj.activeSessions = Math.round(message.activeSessions);
    }
    if (message.metrics !== undefined) {
      obj.metrics = MetricsSample.toJSON(message.metrics);
    }
    if (message.draining !== false) {
      obj.draining = message.draining;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Heartbeat>, I>>(base?: I): Heartbeat {
    return Heartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Heartbeat>, I>>(object: I): Heartbeat {
    const message = createBaseHeartbeat();
    message.seq = object.seq ?? "0";
    message.uptimeMs = object.uptimeMs ?? "0";
    message.activeSessions = object.activeSessions ?? 0;
    message.metrics = (object.metrics !== undefined && object.metrics !== null)
      ? MetricsSample.fromPartial(object.metrics)
      : undefined;
    message.draining = object.draining ?? false;
    return message;
  },
};

function createBaseHeartbeatAck(): HeartbeatAck {
  return { nextIntervalMs: 0, updateAvailable: false };
}

export const HeartbeatAck: MessageFns<HeartbeatAck> = {
  encode(message: HeartbeatAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nextIntervalMs !== 0) {
      writer.uint32(8).uint32(message.nextIntervalMs);
    }
    if (message.updateAvailable !== false) {
      writer.uint32(16).bool(message.updateAvailable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeartbeatAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeatAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nextIntervalMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.updateAvailable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeartbeatAck {
    return {
      nextIntervalMs: isSet(object.nextIntervalMs)
        ? globalThis.Number(object.nextIntervalMs)
        : isSet(object.next_interval_ms)
        ? globalThis.Number(object.next_interval_ms)
        : 0,
      updateAvailable: isSet(object.updateAvailable)
        ? globalThis.Boolean(object.updateAvailable)
        : isSet(object.update_available)
        ? globalThis.Boolean(object.update_available)
        : false,
    };
  },

  toJSON(message: HeartbeatAck): unknown {
    const obj: any = {};
    if (message.nextIntervalMs !== 0) {
      obj.nextIntervalMs = Math.round(message.nextIntervalMs);
    }
    if (message.updateAvailable !== false) {
      obj.updateAvailable = message.updateAvailable;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeartbeatAck>, I>>(base?: I): HeartbeatAck {
    return HeartbeatAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeartbeatAck>, I>>(object: I): HeartbeatAck {
    const message = createBaseHeartbeatAck();
    message.nextIntervalMs = object.nextIntervalMs ?? 0;
    message.updateAvailable = object.updateAvailable ?? false;
    return message;
  },
};

function createBaseMetricsRequest(): MetricsRequest {
  return {};
}

export const MetricsRequest: MessageFns<MetricsRequest> = {
  encode(_: MetricsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetricsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MetricsRequest {
    return {};
  },

  toJSON(_: MetricsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricsRequest>, I>>(base?: I): MetricsRequest {
    return MetricsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricsRequest>, I>>(_: I): MetricsRequest {
    const message = createBaseMetricsRequest();
    return message;
  },
};

function createBaseMetricsSample(): MetricsSample {
  return {
    sampledAtMs: "0",
    cpuPercent: 0,
    load1: 0,
    load5: 0,
    load15: 0,
    memUsedBytes: "0",
    memTotalBytes: "0",
    diskUsedBytes: "0",
    diskTotalBytes: "0",
    runQueue: 0,
    gpus: [],
  };
}

export const MetricsSample: MessageFns<MetricsSample> = {
  encode(message: MetricsSample, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sampledAtMs !== "0") {
      writer.uint32(8).int64(message.sampledAtMs);
    }
    if (message.cpuPercent !== 0) {
      writer.uint32(17).double(message.cpuPercent);
    }
    if (message.load1 !== 0) {
      writer.uint32(25).double(message.load1);
    }
    if (message.load5 !== 0) {
      writer.uint32(33).double(message.load5);
    }
    if (message.load15 !== 0) {
      writer.uint32(41).double(message.load15);
    }
    if (message.memUsedBytes !== "0") {
      writer.uint32(48).uint64(message.memUsedBytes);
    }
    if (message.memTotalBytes !== "0") {
      writer.uint32(56).uint64(message.memTotalBytes);
    }
    if (message.diskUsedBytes !== "0") {
      writer.uint32(64).uint64(message.diskUsedBytes);
    }
    if (message.diskTotalBytes !== "0") {
      writer.uint32(72).uint64(message.diskTotalBytes);
    }
    if (message.runQueue !== 0) {
      writer.uint32(81).double(message.runQueue);
    }
    for (const v of message.gpus) {
      GpuSample.encode(v!, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetricsSample {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricsSample();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sampledAtMs = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.cpuPercent = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.load1 = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.load5 = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.load15 = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.memUsedBytes = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.memTotalBytes = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.diskUsedBytes = reader.uint64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.diskTotalBytes = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.runQueue = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.gpus.push(GpuSample.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetricsSample {
    return {
      sampledAtMs: isSet(object.sampledAtMs)
        ? globalThis.String(object.sampledAtMs)
        : isSet(object.sampled_at_ms)
        ? globalThis.String(object.sampled_at_ms)
        : "0",
      cpuPercent: isSet(object.cpuPercent)
        ? globalThis.Number(object.cpuPercent)
        : isSet(object.cpu_percent)
        ? globalThis.Number(object.cpu_percent)
        : 0,
      load1: isSet(object.load1) ? globalThis.Number(object.load1) : 0,
      load5: isSet(object.load5) ? globalThis.Number(object.load5) : 0,
      load15: isSet(object.load15) ? globalThis.Number(object.load15) : 0,
      memUsedBytes: isSet(object.memUsedBytes)
        ? globalThis.String(object.memUsedBytes)
        : isSet(object.mem_used_bytes)
        ? globalThis.String(object.mem_used_bytes)
        : "0",
      memTotalBytes: isSet(object.memTotalBytes)
        ? globalThis.String(object.memTotalBytes)
        : isSet(object.mem_total_bytes)
        ? globalThis.String(object.mem_total_bytes)
        : "0",
      diskUsedBytes: isSet(object.diskUsedBytes)
        ? globalThis.String(object.diskUsedBytes)
        : isSet(object.disk_used_bytes)
        ? globalThis.String(object.disk_used_bytes)
        : "0",
      diskTotalBytes: isSet(object.diskTotalBytes)
        ? globalThis.String(object.diskTotalBytes)
        : isSet(object.disk_total_bytes)
        ? globalThis.String(object.disk_total_bytes)
        : "0",
      runQueue: isSet(object.runQueue)
        ? globalThis.Number(object.runQueue)
        : isSet(object.run_queue)
        ? globalThis.Number(object.run_queue)
        : 0,
      gpus: globalThis.Array.isArray(object?.gpus)
        ? object.gpus.map((e: any) => GpuSample.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MetricsSample): unknown {
    const obj: any = {};
    if (message.sampledAtMs !== "0") {
      obj.sampledAtMs = message.sampledAtMs;
    }
    if (message.cpuPercent !== 0) {
      obj.cpuPercent = message.cpuPercent;
    }
    if (message.load1 !== 0) {
      obj.load1 = message.load1;
    }
    if (message.load5 !== 0) {
      obj.load5 = message.load5;
    }
    if (message.load15 !== 0) {
      obj.load15 = message.load15;
    }
    if (message.memUsedBytes !== "0") {
      obj.memUsedBytes = message.memUsedBytes;
    }
    if (message.memTotalBytes !== "0") {
      obj.memTotalBytes = message.memTotalBytes;
    }
    if (message.diskUsedBytes !== "0") {
      obj.diskUsedBytes = message.diskUsedBytes;
    }
    if (message.diskTotalBytes !== "0") {
      obj.diskTotalBytes = message.diskTotalBytes;
    }
    if (message.runQueue !== 0) {
      obj.runQueue = message.runQueue;
    }
    if (message.gpus?.length) {
      obj.gpus = message.gpus.map((e) => GpuSample.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricsSample>, I>>(base?: I): MetricsSample {
    return MetricsSample.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricsSample>, I>>(object: I): MetricsSample {
    const message = createBaseMetricsSample();
    message.sampledAtMs = object.sampledAtMs ?? "0";
    message.cpuPercent = object.cpuPercent ?? 0;
    message.load1 = object.load1 ?? 0;
    message.load5 = object.load5 ?? 0;
    message.load15 = object.load15 ?? 0;
    message.memUsedBytes = object.memUsedBytes ?? "0";
    message.memTotalBytes = object.memTotalBytes ?? "0";
    message.diskUsedBytes = object.diskUsedBytes ?? "0";
    message.diskTotalBytes = object.diskTotalBytes ?? "0";
    message.runQueue = object.runQueue ?? 0;
    message.gpus = object.gpus?.map((e) => GpuSample.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGpuSample(): GpuSample {
  return { name: "", utilPercent: 0, memUsedBytes: "0", memTotalBytes: "0" };
}

export const GpuSample: MessageFns<GpuSample> = {
  encode(message: GpuSample, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.utilPercent !== 0) {
      writer.uint32(17).double(message.utilPercent);
    }
    if (message.memUsedBytes !== "0") {
      writer.uint32(24).uint64(message.memUsedBytes);
    }
    if (message.memTotalBytes !== "0") {
      writer.uint32(32).uint64(message.memTotalBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GpuSample {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGpuSample();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.utilPercent = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.memUsedBytes = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.memTotalBytes = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GpuSample {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      utilPercent: isSet(object.utilPercent)
        ? globalThis.Number(object.utilPercent)
        : isSet(object.util_percent)
        ? globalThis.Number(object.util_percent)
        : 0,
      memUsedBytes: isSet(object.memUsedBytes)
        ? globalThis.String(object.memUsedBytes)
        : isSet(object.mem_used_bytes)
        ? globalThis.String(object.mem_used_bytes)
        : "0",
      memTotalBytes: isSet(object.memTotalBytes)
        ? globalThis.String(object.memTotalBytes)
        : isSet(object.mem_total_bytes)
        ? globalThis.String(object.mem_total_bytes)
        : "0",
    };
  },

  toJSON(message: GpuSample): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.utilPercent !== 0) {
      obj.utilPercent = message.utilPercent;
    }
    if (message.memUsedBytes !== "0") {
      obj.memUsedBytes = message.memUsedBytes;
    }
    if (message.memTotalBytes !== "0") {
      obj.memTotalBytes = message.memTotalBytes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GpuSample>, I>>(base?: I): GpuSample {
    return GpuSample.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GpuSample>, I>>(object: I): GpuSample {
    const message = createBaseGpuSample();
    message.name = object.name ?? "";
    message.utilPercent = object.utilPercent ?? 0;
    message.memUsedBytes = object.memUsedBytes ?? "0";
    message.memTotalBytes = object.memTotalBytes ?? "0";
    return message;
  },
};

function createBasePingRequest(): PingRequest {
  return { nonce: "0" };
}

export const PingRequest: MessageFns<PingRequest> = {
  encode(message: PingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nonce !== "0") {
      writer.uint32(8).uint64(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nonce = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingRequest {
    return { nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "0" };
  },

  toJSON(message: PingRequest): unknown {
    const obj: any = {};
    if (message.nonce !== "0") {
      obj.nonce = message.nonce;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingRequest>, I>>(base?: I): PingRequest {
    return PingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingRequest>, I>>(object: I): PingRequest {
    const message = createBasePingRequest();
    message.nonce = object.nonce ?? "0";
    return message;
  },
};

function createBasePingResponse(): PingResponse {
  return { nonce: "0", agentMonotonicMs: "0" };
}

export const PingResponse: MessageFns<PingResponse> = {
  encode(message: PingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nonce !== "0") {
      writer.uint32(8).uint64(message.nonce);
    }
    if (message.agentMonotonicMs !== "0") {
      writer.uint32(16).uint64(message.agentMonotonicMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nonce = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.agentMonotonicMs = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingResponse {
    return {
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "0",
      agentMonotonicMs: isSet(object.agentMonotonicMs)
        ? globalThis.String(object.agentMonotonicMs)
        : isSet(object.agent_monotonic_ms)
        ? globalThis.String(object.agent_monotonic_ms)
        : "0",
    };
  },

  toJSON(message: PingResponse): unknown {
    const obj: any = {};
    if (message.nonce !== "0") {
      obj.nonce = message.nonce;
    }
    if (message.agentMonotonicMs !== "0") {
      obj.agentMonotonicMs = message.agentMonotonicMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingResponse>, I>>(base?: I): PingResponse {
    return PingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingResponse>, I>>(object: I): PingResponse {
    const message = createBasePingResponse();
    message.nonce = object.nonce ?? "0";
    message.agentMonotonicMs = object.agentMonotonicMs ?? "0";
    return message;
  },
};

function createBaseGoingOffline(): GoingOffline {
  return { reason: 0, message: "" };
}

export const GoingOffline: MessageFns<GoingOffline> = {
  encode(message: GoingOffline, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GoingOffline {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGoingOffline();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GoingOffline {
    return {
      reason: isSet(object.reason) ? goingOfflineReasonFromJSON(object.reason) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: GoingOffline): unknown {
    const obj: any = {};
    if (message.reason !== 0) {
      obj.reason = goingOfflineReasonToJSON(message.reason);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GoingOffline>, I>>(base?: I): GoingOffline {
    return GoingOffline.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GoingOffline>, I>>(object: I): GoingOffline {
    const message = createBaseGoingOffline();
    message.reason = object.reason ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResumeRequest(): ResumeRequest {
  return { agentId: "", resumeToken: "", lastKnownEpoch: 0 };
}

export const ResumeRequest: MessageFns<ResumeRequest> = {
  encode(message: ResumeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.resumeToken !== "") {
      writer.uint32(18).string(message.resumeToken);
    }
    if (message.lastKnownEpoch !== 0) {
      writer.uint32(24).uint32(message.lastKnownEpoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResumeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResumeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resumeToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lastKnownEpoch = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResumeRequest {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      resumeToken: isSet(object.resumeToken)
        ? globalThis.String(object.resumeToken)
        : isSet(object.resume_token)
        ? globalThis.String(object.resume_token)
        : "",
      lastKnownEpoch: isSet(object.lastKnownEpoch)
        ? globalThis.Number(object.lastKnownEpoch)
        : isSet(object.last_known_epoch)
        ? globalThis.Number(object.last_known_epoch)
        : 0,
    };
  },

  toJSON(message: ResumeRequest): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.resumeToken !== "") {
      obj.resumeToken = message.resumeToken;
    }
    if (message.lastKnownEpoch !== 0) {
      obj.lastKnownEpoch = Math.round(message.lastKnownEpoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResumeRequest>, I>>(base?: I): ResumeRequest {
    return ResumeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResumeRequest>, I>>(object: I): ResumeRequest {
    const message = createBaseResumeRequest();
    message.agentId = object.agentId ?? "";
    message.resumeToken = object.resumeToken ?? "";
    message.lastKnownEpoch = object.lastKnownEpoch ?? 0;
    return message;
  },
};

function createBaseResumeResponse(): ResumeResponse {
  return { accepted: false, epoch: 0, resumeToken: "" };
}

export const ResumeResponse: MessageFns<ResumeResponse> = {
  encode(message: ResumeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.epoch !== 0) {
      writer.uint32(16).uint32(message.epoch);
    }
    if (message.resumeToken !== "") {
      writer.uint32(26).string(message.resumeToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResumeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResumeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resumeToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResumeResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      resumeToken: isSet(object.resumeToken)
        ? globalThis.String(object.resumeToken)
        : isSet(object.resume_token)
        ? globalThis.String(object.resume_token)
        : "",
    };
  },

  toJSON(message: ResumeResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.resumeToken !== "") {
      obj.resumeToken = message.resumeToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResumeResponse>, I>>(base?: I): ResumeResponse {
    return ResumeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResumeResponse>, I>>(object: I): ResumeResponse {
    const message = createBaseResumeResponse();
    message.accepted = object.accepted ?? false;
    message.epoch = object.epoch ?? 0;
    message.resumeToken = object.resumeToken ?? "";
    return message;
  },
};

function createBaseUpdateCheckRequest(): UpdateCheckRequest {
  return { currentVersion: "", channel: "", os: 0, arch: 0 };
}

export const UpdateCheckRequest: MessageFns<UpdateCheckRequest> = {
  encode(message: UpdateCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentVersion !== "") {
      writer.uint32(10).string(message.currentVersion);
    }
    if (message.channel !== "") {
      writer.uint32(18).string(message.channel);
    }
    if (message.os !== 0) {
      writer.uint32(24).int32(message.os);
    }
    if (message.arch !== 0) {
      writer.uint32(32).int32(message.arch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.currentVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.os = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.arch = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateCheckRequest {
    return {
      currentVersion: isSet(object.currentVersion)
        ? globalThis.String(object.currentVersion)
        : isSet(object.current_version)
        ? globalThis.String(object.current_version)
        : "",
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      os: isSet(object.os) ? osFromJSON(object.os) : 0,
      arch: isSet(object.arch) ? archFromJSON(object.arch) : 0,
    };
  },

  toJSON(message: UpdateCheckRequest): unknown {
    const obj: any = {};
    if (message.currentVersion !== "") {
      obj.currentVersion = message.currentVersion;
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.os !== 0) {
      obj.os = osToJSON(message.os);
    }
    if (message.arch !== 0) {
      obj.arch = archToJSON(message.arch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateCheckRequest>, I>>(base?: I): UpdateCheckRequest {
    return UpdateCheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateCheckRequest>, I>>(object: I): UpdateCheckRequest {
    const message = createBaseUpdateCheckRequest();
    message.currentVersion = object.currentVersion ?? "";
    message.channel = object.channel ?? "";
    message.os = object.os ?? 0;
    message.arch = object.arch ?? 0;
    return message;
  },
};

function createBaseUpdateCheckResponse(): UpdateCheckResponse {
  return { updateAvailable: false, manifest: undefined };
}

export const UpdateCheckResponse: MessageFns<UpdateCheckResponse> = {
  encode(message: UpdateCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.updateAvailable !== false) {
      writer.uint32(8).bool(message.updateAvailable);
    }
    if (message.manifest !== undefined) {
      UpdateManifest.encode(message.manifest, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.updateAvailable = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.manifest = UpdateManifest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateCheckResponse {
    return {
      updateAvailable: isSet(object.updateAvailable)
        ? globalThis.Boolean(object.updateAvailable)
        : isSet(object.update_available)
        ? globalThis.Boolean(object.update_available)
        : false,
      manifest: isSet(object.manifest) ? UpdateManifest.fromJSON(object.manifest) : undefined,
    };
  },

  toJSON(message: UpdateCheckResponse): unknown {
    const obj: any = {};
    if (message.updateAvailable !== false) {
      obj.updateAvailable = message.updateAvailable;
    }
    if (message.manifest !== undefined) {
      obj.manifest = UpdateManifest.toJSON(message.manifest);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateCheckResponse>, I>>(base?: I): UpdateCheckResponse {
    return UpdateCheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateCheckResponse>, I>>(object: I): UpdateCheckResponse {
    const message = createBaseUpdateCheckResponse();
    message.updateAvailable = object.updateAvailable ?? false;
    message.manifest = (object.manifest !== undefined && object.manifest !== null)
      ? UpdateManifest.fromPartial(object.manifest)
      : undefined;
    return message;
  },
};

function createBaseUpdateManifest(): UpdateManifest {
  return {
    channel: "",
    version: "",
    minSupported: "",
    rolloutPercent: 0,
    cohortSalt: "",
    artifacts: [],
    notesUrl: "",
    signedAtMs: "0",
    force: false,
  };
}

export const UpdateManifest: MessageFns<UpdateManifest> = {
  encode(message: UpdateManifest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== "") {
      writer.uint32(10).string(message.channel);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.minSupported !== "") {
      writer.uint32(26).string(message.minSupported);
    }
    if (message.rolloutPercent !== 0) {
      writer.uint32(32).uint32(message.rolloutPercent);
    }
    if (message.cohortSalt !== "") {
      writer.uint32(42).string(message.cohortSalt);
    }
    for (const v of message.artifacts) {
      UpdateArtifact.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.notesUrl !== "") {
      writer.uint32(58).string(message.notesUrl);
    }
    if (message.signedAtMs !== "0") {
      writer.uint32(64).int64(message.signedAtMs);
    }
    if (message.force !== false) {
      writer.uint32(72).bool(message.force);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateManifest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateManifest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.minSupported = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rolloutPercent = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cohortSalt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.artifacts.push(UpdateArtifact.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.notesUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.signedAtMs = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.force = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateManifest {
    return {
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      minSupported: isSet(object.minSupported)
        ? globalThis.String(object.minSupported)
        : isSet(object.min_supported)
        ? globalThis.String(object.min_supported)
        : "",
      rolloutPercent: isSet(object.rolloutPercent)
        ? globalThis.Number(object.rolloutPercent)
        : isSet(object.rollout_percent)
        ? globalThis.Number(object.rollout_percent)
        : 0,
      cohortSalt: isSet(object.cohortSalt)
        ? globalThis.String(object.cohortSalt)
        : isSet(object.cohort_salt)
        ? globalThis.String(object.cohort_salt)
        : "",
      artifacts: globalThis.Array.isArray(object?.artifacts)
        ? object.artifacts.map((e: any) => UpdateArtifact.fromJSON(e))
        : [],
      notesUrl: isSet(object.notesUrl)
        ? globalThis.String(object.notesUrl)
        : isSet(object.notes_url)
        ? globalThis.String(object.notes_url)
        : "",
      signedAtMs: isSet(object.signedAtMs)
        ? globalThis.String(object.signedAtMs)
        : isSet(object.signed_at_ms)
        ? globalThis.String(object.signed_at_ms)
        : "0",
      force: isSet(object.force) ? globalThis.Boolean(object.force) : false,
    };
  },

  toJSON(message: UpdateManifest): unknown {
    const obj: any = {};
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.minSupported !== "") {
      obj.minSupported = message.minSupported;
    }
    if (message.rolloutPercent !== 0) {
      obj.rolloutPercent = Math.round(message.rolloutPercent);
    }
    if (message.cohortSalt !== "") {
      obj.cohortSalt = message.cohortSalt;
    }
    if (message.artifacts?.length) {
      obj.artifacts = message.artifacts.map((e) => UpdateArtifact.toJSON(e));
    }
    if (message.notesUrl !== "") {
      obj.notesUrl = message.notesUrl;
    }
    if (message.signedAtMs !== "0") {
      obj.signedAtMs = message.signedAtMs;
    }
    if (message.force !== false) {
      obj.force = message.force;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateManifest>, I>>(base?: I): UpdateManifest {
    return UpdateManifest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateManifest>, I>>(object: I): UpdateManifest {
    const message = createBaseUpdateManifest();
    message.channel = object.channel ?? "";
    message.version = object.version ?? "";
    message.minSupported = object.minSupported ?? "";
    message.rolloutPercent = object.rolloutPercent ?? 0;
    message.cohortSalt = object.cohortSalt ?? "";
    message.artifacts = object.artifacts?.map((e) => UpdateArtifact.fromPartial(e)) || [];
    message.notesUrl = object.notesUrl ?? "";
    message.signedAtMs = object.signedAtMs ?? "0";
    message.force = object.force ?? false;
    return message;
  },
};

function createBaseUpdateArtifact(): UpdateArtifact {
  return { target: "", url: "", size: "0", sha256: "", minisigUrl: "" };
}

export const UpdateArtifact: MessageFns<UpdateArtifact> = {
  encode(message: UpdateArtifact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.target !== "") {
      writer.uint32(10).string(message.target);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.size !== "0") {
      writer.uint32(24).uint64(message.size);
    }
    if (message.sha256 !== "") {
      writer.uint32(34).string(message.sha256);
    }
    if (message.minisigUrl !== "") {
      writer.uint32(42).string(message.minisigUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateArtifact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateArtifact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sha256 = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.minisigUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateArtifact {
    return {
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      size: isSet(object.size) ? globalThis.String(object.size) : "0",
      sha256: isSet(object.sha256) ? globalThis.String(object.sha256) : "",
      minisigUrl: isSet(object.minisigUrl)
        ? globalThis.String(object.minisigUrl)
        : isSet(object.minisig_url)
        ? globalThis.String(object.minisig_url)
        : "",
    };
  },

  toJSON(message: UpdateArtifact): unknown {
    const obj: any = {};
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.size !== "0") {
      obj.size = message.size;
    }
    if (message.sha256 !== "") {
      obj.sha256 = message.sha256;
    }
    if (message.minisigUrl !== "") {
      obj.minisigUrl = message.minisigUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateArtifact>, I>>(base?: I): UpdateArtifact {
    return UpdateArtifact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateArtifact>, I>>(object: I): UpdateArtifact {
    const message = createBaseUpdateArtifact();
    message.target = object.target ?? "";
    message.url = object.url ?? "";
    message.size = object.size ?? "0";
    message.sha256 = object.sha256 ?? "";
    message.minisigUrl = object.minisigUrl ?? "";
    return message;
  },
};

function createBaseUpdateMayProceedRequest(): UpdateMayProceedRequest {
  return { agentId: "", targetVersion: "" };
}

export const UpdateMayProceedRequest: MessageFns<UpdateMayProceedRequest> = {
  encode(message: UpdateMayProceedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    if (message.targetVersion !== "") {
      writer.uint32(18).string(message.targetVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateMayProceedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateMayProceedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.targetVersion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateMayProceedRequest {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      targetVersion: isSet(object.targetVersion)
        ? globalThis.String(object.targetVersion)
        : isSet(object.target_version)
        ? globalThis.String(object.target_version)
        : "",
    };
  },

  toJSON(message: UpdateMayProceedRequest): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.targetVersion !== "") {
      obj.targetVersion = message.targetVersion;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateMayProceedRequest>, I>>(base?: I): UpdateMayProceedRequest {
    return UpdateMayProceedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateMayProceedRequest>, I>>(object: I): UpdateMayProceedRequest {
    const message = createBaseUpdateMayProceedRequest();
    message.agentId = object.agentId ?? "";
    message.targetVersion = object.targetVersion ?? "";
    return message;
  },
};

function createBaseUpdateMayProceedResponse(): UpdateMayProceedResponse {
  return { mayProceed: false, activeSessions: 0 };
}

export const UpdateMayProceedResponse: MessageFns<UpdateMayProceedResponse> = {
  encode(message: UpdateMayProceedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mayProceed !== false) {
      writer.uint32(8).bool(message.mayProceed);
    }
    if (message.activeSessions !== 0) {
      writer.uint32(16).uint32(message.activeSessions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateMayProceedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateMayProceedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.mayProceed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.activeSessions = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateMayProceedResponse {
    return {
      mayProceed: isSet(object.mayProceed)
        ? globalThis.Boolean(object.mayProceed)
        : isSet(object.may_proceed)
        ? globalThis.Boolean(object.may_proceed)
        : false,
      activeSessions: isSet(object.activeSessions)
        ? globalThis.Number(object.activeSessions)
        : isSet(object.active_sessions)
        ? globalThis.Number(object.active_sessions)
        : 0,
    };
  },

  toJSON(message: UpdateMayProceedResponse): unknown {
    const obj: any = {};
    if (message.mayProceed !== false) {
      obj.mayProceed = message.mayProceed;
    }
    if (message.activeSessions !== 0) {
      obj.activeSessions = Math.round(message.activeSessions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateMayProceedResponse>, I>>(base?: I): UpdateMayProceedResponse {
    return UpdateMayProceedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateMayProceedResponse>, I>>(object: I): UpdateMayProceedResponse {
    const message = createBaseUpdateMayProceedResponse();
    message.mayProceed = object.mayProceed ?? false;
    message.activeSessions = object.activeSessions ?? 0;
    return message;
  },
};

function createBaseControlRequest(): ControlRequest {
  return { requestId: "", epoch: 0, op: undefined };
}

export const ControlRequest: MessageFns<ControlRequest> = {
  encode(message: ControlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.epoch !== 0) {
      writer.uint32(16).uint32(message.epoch);
    }
    switch (message.op?.$case) {
      case "ping":
        PingRequest.encode(message.op.ping, writer.uint32(82).fork()).join();
        break;
      case "hello":
        Hello.encode(message.op.hello, writer.uint32(90).fork()).join();
        break;
      case "resume":
        ResumeRequest.encode(message.op.resume, writer.uint32(98).fork()).join();
        break;
      case "exec":
        ExecRequest.encode(message.op.exec, writer.uint32(106).fork()).join();
        break;
      case "fsRead":
        FsReadRequest.encode(message.op.fsRead, writer.uint32(114).fork()).join();
        break;
      case "fsWrite":
        FsWriteRequest.encode(message.op.fsWrite, writer.uint32(122).fork()).join();
        break;
      case "fsList":
        FsListRequest.encode(message.op.fsList, writer.uint32(130).fork()).join();
        break;
      case "fsMkdir":
        FsMkdirRequest.encode(message.op.fsMkdir, writer.uint32(138).fork()).join();
        break;
      case "fsMove":
        FsMoveRequest.encode(message.op.fsMove, writer.uint32(146).fork()).join();
        break;
      case "fsStat":
        FsStatRequest.encode(message.op.fsStat, writer.uint32(154).fork()).join();
        break;
      case "fsRemove":
        FsRemoveRequest.encode(message.op.fsRemove, writer.uint32(162).fork()).join();
        break;
      case "git":
        GitRequest.encode(message.op.git, writer.uint32(170).fork()).join();
        break;
      case "ptyOpen":
        PtyOpenRequest.encode(message.op.ptyOpen, writer.uint32(178).fork()).join();
        break;
      case "ptyWrite":
        PtyWriteRequest.encode(message.op.ptyWrite, writer.uint32(186).fork()).join();
        break;
      case "ptyResize":
        PtyResizeRequest.encode(message.op.ptyResize, writer.uint32(194).fork()).join();
        break;
      case "ptyClose":
        PtyCloseRequest.encode(message.op.ptyClose, writer.uint32(202).fork()).join();
        break;
      case "desktopEnsure":
        DesktopEnsureRequest.encode(message.op.desktopEnsure, writer.uint32(210).fork()).join();
        break;
      case "metrics":
        MetricsRequest.encode(message.op.metrics, writer.uint32(218).fork()).join();
        break;
      case "updateMayProceed":
        UpdateMayProceedRequest.encode(message.op.updateMayProceed, writer.uint32(226).fork()).join();
        break;
      case "desktopInput":
        DesktopInputRequest.encode(message.op.desktopInput, writer.uint32(234).fork()).join();
        break;
      case "desktopScreenshot":
        DesktopScreenshotRequest.encode(message.op.desktopScreenshot, writer.uint32(242).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.epoch = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.op = { $case: "ping", ping: PingRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.op = { $case: "hello", hello: Hello.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.op = { $case: "resume", resume: ResumeRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.op = { $case: "exec", exec: ExecRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.op = { $case: "fsRead", fsRead: FsReadRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.op = { $case: "fsWrite", fsWrite: FsWriteRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.op = { $case: "fsList", fsList: FsListRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.op = { $case: "fsMkdir", fsMkdir: FsMkdirRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.op = { $case: "fsMove", fsMove: FsMoveRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.op = { $case: "fsStat", fsStat: FsStatRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.op = { $case: "fsRemove", fsRemove: FsRemoveRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.op = { $case: "git", git: GitRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.op = { $case: "ptyOpen", ptyOpen: PtyOpenRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.op = { $case: "ptyWrite", ptyWrite: PtyWriteRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.op = { $case: "ptyResize", ptyResize: PtyResizeRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.op = { $case: "ptyClose", ptyClose: PtyCloseRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.op = { $case: "desktopEnsure", desktopEnsure: DesktopEnsureRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.op = { $case: "metrics", metrics: MetricsRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.op = {
            $case: "updateMayProceed",
            updateMayProceed: UpdateMayProceedRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.op = { $case: "desktopInput", desktopInput: DesktopInputRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.op = {
            $case: "desktopScreenshot",
            desktopScreenshot: DesktopScreenshotRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      op: isSet(object.ping)
        ? { $case: "ping", ping: PingRequest.fromJSON(object.ping) }
        : isSet(object.hello)
        ? { $case: "hello", hello: Hello.fromJSON(object.hello) }
        : isSet(object.resume)
        ? { $case: "resume", resume: ResumeRequest.fromJSON(object.resume) }
        : isSet(object.exec)
        ? { $case: "exec", exec: ExecRequest.fromJSON(object.exec) }
        : isSet(object.fsRead)
        ? { $case: "fsRead", fsRead: FsReadRequest.fromJSON(object.fsRead) }
        : isSet(object.fs_read)
        ? { $case: "fsRead", fsRead: FsReadRequest.fromJSON(object.fs_read) }
        : isSet(object.fsWrite)
        ? { $case: "fsWrite", fsWrite: FsWriteRequest.fromJSON(object.fsWrite) }
        : isSet(object.fs_write)
        ? { $case: "fsWrite", fsWrite: FsWriteRequest.fromJSON(object.fs_write) }
        : isSet(object.fsList)
        ? { $case: "fsList", fsList: FsListRequest.fromJSON(object.fsList) }
        : isSet(object.fs_list)
        ? { $case: "fsList", fsList: FsListRequest.fromJSON(object.fs_list) }
        : isSet(object.fsMkdir)
        ? { $case: "fsMkdir", fsMkdir: FsMkdirRequest.fromJSON(object.fsMkdir) }
        : isSet(object.fs_mkdir)
        ? { $case: "fsMkdir", fsMkdir: FsMkdirRequest.fromJSON(object.fs_mkdir) }
        : isSet(object.fsMove)
        ? { $case: "fsMove", fsMove: FsMoveRequest.fromJSON(object.fsMove) }
        : isSet(object.fs_move)
        ? { $case: "fsMove", fsMove: FsMoveRequest.fromJSON(object.fs_move) }
        : isSet(object.fsStat)
        ? { $case: "fsStat", fsStat: FsStatRequest.fromJSON(object.fsStat) }
        : isSet(object.fs_stat)
        ? { $case: "fsStat", fsStat: FsStatRequest.fromJSON(object.fs_stat) }
        : isSet(object.fsRemove)
        ? { $case: "fsRemove", fsRemove: FsRemoveRequest.fromJSON(object.fsRemove) }
        : isSet(object.fs_remove)
        ? { $case: "fsRemove", fsRemove: FsRemoveRequest.fromJSON(object.fs_remove) }
        : isSet(object.git)
        ? { $case: "git", git: GitRequest.fromJSON(object.git) }
        : isSet(object.ptyOpen)
        ? { $case: "ptyOpen", ptyOpen: PtyOpenRequest.fromJSON(object.ptyOpen) }
        : isSet(object.pty_open)
        ? { $case: "ptyOpen", ptyOpen: PtyOpenRequest.fromJSON(object.pty_open) }
        : isSet(object.ptyWrite)
        ? { $case: "ptyWrite", ptyWrite: PtyWriteRequest.fromJSON(object.ptyWrite) }
        : isSet(object.pty_write)
        ? { $case: "ptyWrite", ptyWrite: PtyWriteRequest.fromJSON(object.pty_write) }
        : isSet(object.ptyResize)
        ? { $case: "ptyResize", ptyResize: PtyResizeRequest.fromJSON(object.ptyResize) }
        : isSet(object.pty_resize)
        ? { $case: "ptyResize", ptyResize: PtyResizeRequest.fromJSON(object.pty_resize) }
        : isSet(object.ptyClose)
        ? { $case: "ptyClose", ptyClose: PtyCloseRequest.fromJSON(object.ptyClose) }
        : isSet(object.pty_close)
        ? { $case: "ptyClose", ptyClose: PtyCloseRequest.fromJSON(object.pty_close) }
        : isSet(object.desktopEnsure)
        ? { $case: "desktopEnsure", desktopEnsure: DesktopEnsureRequest.fromJSON(object.desktopEnsure) }
        : isSet(object.desktop_ensure)
        ? { $case: "desktopEnsure", desktopEnsure: DesktopEnsureRequest.fromJSON(object.desktop_ensure) }
        : isSet(object.metrics)
        ? { $case: "metrics", metrics: MetricsRequest.fromJSON(object.metrics) }
        : isSet(object.updateMayProceed)
        ? { $case: "updateMayProceed", updateMayProceed: UpdateMayProceedRequest.fromJSON(object.updateMayProceed) }
        : isSet(object.update_may_proceed)
        ? { $case: "updateMayProceed", updateMayProceed: UpdateMayProceedRequest.fromJSON(object.update_may_proceed) }
        : isSet(object.desktopInput)
        ? { $case: "desktopInput", desktopInput: DesktopInputRequest.fromJSON(object.desktopInput) }
        : isSet(object.desktop_input)
        ? { $case: "desktopInput", desktopInput: DesktopInputRequest.fromJSON(object.desktop_input) }
        : isSet(object.desktopScreenshot)
        ? { $case: "desktopScreenshot", desktopScreenshot: DesktopScreenshotRequest.fromJSON(object.desktopScreenshot) }
        : isSet(object.desktop_screenshot)
        ? {
          $case: "desktopScreenshot",
          desktopScreenshot: DesktopScreenshotRequest.fromJSON(object.desktop_screenshot),
        }
        : undefined,
    };
  },

  toJSON(message: ControlRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.op?.$case === "ping") {
      obj.ping = PingRequest.toJSON(message.op.ping);
    } else if (message.op?.$case === "hello") {
      obj.hello = Hello.toJSON(message.op.hello);
    } else if (message.op?.$case === "resume") {
      obj.resume = ResumeRequest.toJSON(message.op.resume);
    } else if (message.op?.$case === "exec") {
      obj.exec = ExecRequest.toJSON(message.op.exec);
    } else if (message.op?.$case === "fsRead") {
      obj.fsRead = FsReadRequest.toJSON(message.op.fsRead);
    } else if (message.op?.$case === "fsWrite") {
      obj.fsWrite = FsWriteRequest.toJSON(message.op.fsWrite);
    } else if (message.op?.$case === "fsList") {
      obj.fsList = FsListRequest.toJSON(message.op.fsList);
    } else if (message.op?.$case === "fsMkdir") {
      obj.fsMkdir = FsMkdirRequest.toJSON(message.op.fsMkdir);
    } else if (message.op?.$case === "fsMove") {
      obj.fsMove = FsMoveRequest.toJSON(message.op.fsMove);
    } else if (message.op?.$case === "fsStat") {
      obj.fsStat = FsStatRequest.toJSON(message.op.fsStat);
    } else if (message.op?.$case === "fsRemove") {
      obj.fsRemove = FsRemoveRequest.toJSON(message.op.fsRemove);
    } else if (message.op?.$case === "git") {
      obj.git = GitRequest.toJSON(message.op.git);
    } else if (message.op?.$case === "ptyOpen") {
      obj.ptyOpen = PtyOpenRequest.toJSON(message.op.ptyOpen);
    } else if (message.op?.$case === "ptyWrite") {
      obj.ptyWrite = PtyWriteRequest.toJSON(message.op.ptyWrite);
    } else if (message.op?.$case === "ptyResize") {
      obj.ptyResize = PtyResizeRequest.toJSON(message.op.ptyResize);
    } else if (message.op?.$case === "ptyClose") {
      obj.ptyClose = PtyCloseRequest.toJSON(message.op.ptyClose);
    } else if (message.op?.$case === "desktopEnsure") {
      obj.desktopEnsure = DesktopEnsureRequest.toJSON(message.op.desktopEnsure);
    } else if (message.op?.$case === "metrics") {
      obj.metrics = MetricsRequest.toJSON(message.op.metrics);
    } else if (message.op?.$case === "updateMayProceed") {
      obj.updateMayProceed = UpdateMayProceedRequest.toJSON(message.op.updateMayProceed);
    } else if (message.op?.$case === "desktopInput") {
      obj.desktopInput = DesktopInputRequest.toJSON(message.op.desktopInput);
    } else if (message.op?.$case === "desktopScreenshot") {
      obj.desktopScreenshot = DesktopScreenshotRequest.toJSON(message.op.desktopScreenshot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlRequest>, I>>(base?: I): ControlRequest {
    return ControlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlRequest>, I>>(object: I): ControlRequest {
    const message = createBaseControlRequest();
    message.requestId = object.requestId ?? "";
    message.epoch = object.epoch ?? 0;
    switch (object.op?.$case) {
      case "ping": {
        if (object.op?.ping !== undefined && object.op?.ping !== null) {
          message.op = { $case: "ping", ping: PingRequest.fromPartial(object.op.ping) };
        }
        break;
      }
      case "hello": {
        if (object.op?.hello !== undefined && object.op?.hello !== null) {
          message.op = { $case: "hello", hello: Hello.fromPartial(object.op.hello) };
        }
        break;
      }
      case "resume": {
        if (object.op?.resume !== undefined && object.op?.resume !== null) {
          message.op = { $case: "resume", resume: ResumeRequest.fromPartial(object.op.resume) };
        }
        break;
      }
      case "exec": {
        if (object.op?.exec !== undefined && object.op?.exec !== null) {
          message.op = { $case: "exec", exec: ExecRequest.fromPartial(object.op.exec) };
        }
        break;
      }
      case "fsRead": {
        if (object.op?.fsRead !== undefined && object.op?.fsRead !== null) {
          message.op = { $case: "fsRead", fsRead: FsReadRequest.fromPartial(object.op.fsRead) };
        }
        break;
      }
      case "fsWrite": {
        if (object.op?.fsWrite !== undefined && object.op?.fsWrite !== null) {
          message.op = { $case: "fsWrite", fsWrite: FsWriteRequest.fromPartial(object.op.fsWrite) };
        }
        break;
      }
      case "fsList": {
        if (object.op?.fsList !== undefined && object.op?.fsList !== null) {
          message.op = { $case: "fsList", fsList: FsListRequest.fromPartial(object.op.fsList) };
        }
        break;
      }
      case "fsMkdir": {
        if (object.op?.fsMkdir !== undefined && object.op?.fsMkdir !== null) {
          message.op = { $case: "fsMkdir", fsMkdir: FsMkdirRequest.fromPartial(object.op.fsMkdir) };
        }
        break;
      }
      case "fsMove": {
        if (object.op?.fsMove !== undefined && object.op?.fsMove !== null) {
          message.op = { $case: "fsMove", fsMove: FsMoveRequest.fromPartial(object.op.fsMove) };
        }
        break;
      }
      case "fsStat": {
        if (object.op?.fsStat !== undefined && object.op?.fsStat !== null) {
          message.op = { $case: "fsStat", fsStat: FsStatRequest.fromPartial(object.op.fsStat) };
        }
        break;
      }
      case "fsRemove": {
        if (object.op?.fsRemove !== undefined && object.op?.fsRemove !== null) {
          message.op = { $case: "fsRemove", fsRemove: FsRemoveRequest.fromPartial(object.op.fsRemove) };
        }
        break;
      }
      case "git": {
        if (object.op?.git !== undefined && object.op?.git !== null) {
          message.op = { $case: "git", git: GitRequest.fromPartial(object.op.git) };
        }
        break;
      }
      case "ptyOpen": {
        if (object.op?.ptyOpen !== undefined && object.op?.ptyOpen !== null) {
          message.op = { $case: "ptyOpen", ptyOpen: PtyOpenRequest.fromPartial(object.op.ptyOpen) };
        }
        break;
      }
      case "ptyWrite": {
        if (object.op?.ptyWrite !== undefined && object.op?.ptyWrite !== null) {
          message.op = { $case: "ptyWrite", ptyWrite: PtyWriteRequest.fromPartial(object.op.ptyWrite) };
        }
        break;
      }
      case "ptyResize": {
        if (object.op?.ptyResize !== undefined && object.op?.ptyResize !== null) {
          message.op = { $case: "ptyResize", ptyResize: PtyResizeRequest.fromPartial(object.op.ptyResize) };
        }
        break;
      }
      case "ptyClose": {
        if (object.op?.ptyClose !== undefined && object.op?.ptyClose !== null) {
          message.op = { $case: "ptyClose", ptyClose: PtyCloseRequest.fromPartial(object.op.ptyClose) };
        }
        break;
      }
      case "desktopEnsure": {
        if (object.op?.desktopEnsure !== undefined && object.op?.desktopEnsure !== null) {
          message.op = {
            $case: "desktopEnsure",
            desktopEnsure: DesktopEnsureRequest.fromPartial(object.op.desktopEnsure),
          };
        }
        break;
      }
      case "metrics": {
        if (object.op?.metrics !== undefined && object.op?.metrics !== null) {
          message.op = { $case: "metrics", metrics: MetricsRequest.fromPartial(object.op.metrics) };
        }
        break;
      }
      case "updateMayProceed": {
        if (object.op?.updateMayProceed !== undefined && object.op?.updateMayProceed !== null) {
          message.op = {
            $case: "updateMayProceed",
            updateMayProceed: UpdateMayProceedRequest.fromPartial(object.op.updateMayProceed),
          };
        }
        break;
      }
      case "desktopInput": {
        if (object.op?.desktopInput !== undefined && object.op?.desktopInput !== null) {
          message.op = { $case: "desktopInput", desktopInput: DesktopInputRequest.fromPartial(object.op.desktopInput) };
        }
        break;
      }
      case "desktopScreenshot": {
        if (object.op?.desktopScreenshot !== undefined && object.op?.desktopScreenshot !== null) {
          message.op = {
            $case: "desktopScreenshot",
            desktopScreenshot: DesktopScreenshotRequest.fromPartial(object.op.desktopScreenshot),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseControlResponse(): ControlResponse {
  return { requestId: "", error: undefined, result: undefined };
}

export const ControlResponse: MessageFns<ControlResponse> = {
  encode(message: ControlResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.error !== undefined) {
      AgentError.encode(message.error, writer.uint32(18).fork()).join();
    }
    switch (message.result?.$case) {
      case "ping":
        PingResponse.encode(message.result.ping, writer.uint32(82).fork()).join();
        break;
      case "hello":
        HelloAck.encode(message.result.hello, writer.uint32(90).fork()).join();
        break;
      case "resume":
        ResumeResponse.encode(message.result.resume, writer.uint32(98).fork()).join();
        break;
      case "exec":
        ExecResponse.encode(message.result.exec, writer.uint32(106).fork()).join();
        break;
      case "fsRead":
        FsReadResponse.encode(message.result.fsRead, writer.uint32(114).fork()).join();
        break;
      case "fsWrite":
        FsWriteResponse.encode(message.result.fsWrite, writer.uint32(122).fork()).join();
        break;
      case "fsList":
        FsListResponse.encode(message.result.fsList, writer.uint32(130).fork()).join();
        break;
      case "fsMkdir":
        FsMkdirResponse.encode(message.result.fsMkdir, writer.uint32(138).fork()).join();
        break;
      case "fsMove":
        FsMoveResponse.encode(message.result.fsMove, writer.uint32(146).fork()).join();
        break;
      case "fsStat":
        FsStatResponse.encode(message.result.fsStat, writer.uint32(154).fork()).join();
        break;
      case "fsRemove":
        FsRemoveResponse.encode(message.result.fsRemove, writer.uint32(162).fork()).join();
        break;
      case "git":
        GitResponse.encode(message.result.git, writer.uint32(170).fork()).join();
        break;
      case "ptyOpen":
        PtyOpenResponse.encode(message.result.ptyOpen, writer.uint32(178).fork()).join();
        break;
      case "ptyWrite":
        PtyWriteResponse.encode(message.result.ptyWrite, writer.uint32(186).fork()).join();
        break;
      case "ptyResize":
        PtyResizeResponse.encode(message.result.ptyResize, writer.uint32(194).fork()).join();
        break;
      case "ptyClose":
        PtyCloseResponse.encode(message.result.ptyClose, writer.uint32(202).fork()).join();
        break;
      case "desktopEnsure":
        DesktopEnsureResponse.encode(message.result.desktopEnsure, writer.uint32(210).fork()).join();
        break;
      case "metrics":
        MetricsSample.encode(message.result.metrics, writer.uint32(218).fork()).join();
        break;
      case "updateMayProceed":
        UpdateMayProceedResponse.encode(message.result.updateMayProceed, writer.uint32(226).fork()).join();
        break;
      case "desktopInput":
        DesktopInputResponse.encode(message.result.desktopInput, writer.uint32(234).fork()).join();
        break;
      case "desktopScreenshot":
        DesktopScreenshotResponse.encode(message.result.desktopScreenshot, writer.uint32(242).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AgentError.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.result = { $case: "ping", ping: PingResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.result = { $case: "hello", hello: HelloAck.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.result = { $case: "resume", resume: ResumeResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.result = { $case: "exec", exec: ExecResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.result = { $case: "fsRead", fsRead: FsReadResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.result = { $case: "fsWrite", fsWrite: FsWriteResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.result = { $case: "fsList", fsList: FsListResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.result = { $case: "fsMkdir", fsMkdir: FsMkdirResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.result = { $case: "fsMove", fsMove: FsMoveResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.result = { $case: "fsStat", fsStat: FsStatResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.result = { $case: "fsRemove", fsRemove: FsRemoveResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.result = { $case: "git", git: GitResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.result = { $case: "ptyOpen", ptyOpen: PtyOpenResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.result = { $case: "ptyWrite", ptyWrite: PtyWriteResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.result = { $case: "ptyResize", ptyResize: PtyResizeResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.result = { $case: "ptyClose", ptyClose: PtyCloseResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.result = {
            $case: "desktopEnsure",
            desktopEnsure: DesktopEnsureResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.result = { $case: "metrics", metrics: MetricsSample.decode(reader, reader.uint32()) };
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.result = {
            $case: "updateMayProceed",
            updateMayProceed: UpdateMayProceedResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.result = {
            $case: "desktopInput",
            desktopInput: DesktopInputResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.result = {
            $case: "desktopScreenshot",
            desktopScreenshot: DesktopScreenshotResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ControlResponse {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      error: isSet(object.error) ? AgentError.fromJSON(object.error) : undefined,
      result: isSet(object.ping)
        ? { $case: "ping", ping: PingResponse.fromJSON(object.ping) }
        : isSet(object.hello)
        ? { $case: "hello", hello: HelloAck.fromJSON(object.hello) }
        : isSet(object.resume)
        ? { $case: "resume", resume: ResumeResponse.fromJSON(object.resume) }
        : isSet(object.exec)
        ? { $case: "exec", exec: ExecResponse.fromJSON(object.exec) }
        : isSet(object.fsRead)
        ? { $case: "fsRead", fsRead: FsReadResponse.fromJSON(object.fsRead) }
        : isSet(object.fs_read)
        ? { $case: "fsRead", fsRead: FsReadResponse.fromJSON(object.fs_read) }
        : isSet(object.fsWrite)
        ? { $case: "fsWrite", fsWrite: FsWriteResponse.fromJSON(object.fsWrite) }
        : isSet(object.fs_write)
        ? { $case: "fsWrite", fsWrite: FsWriteResponse.fromJSON(object.fs_write) }
        : isSet(object.fsList)
        ? { $case: "fsList", fsList: FsListResponse.fromJSON(object.fsList) }
        : isSet(object.fs_list)
        ? { $case: "fsList", fsList: FsListResponse.fromJSON(object.fs_list) }
        : isSet(object.fsMkdir)
        ? { $case: "fsMkdir", fsMkdir: FsMkdirResponse.fromJSON(object.fsMkdir) }
        : isSet(object.fs_mkdir)
        ? { $case: "fsMkdir", fsMkdir: FsMkdirResponse.fromJSON(object.fs_mkdir) }
        : isSet(object.fsMove)
        ? { $case: "fsMove", fsMove: FsMoveResponse.fromJSON(object.fsMove) }
        : isSet(object.fs_move)
        ? { $case: "fsMove", fsMove: FsMoveResponse.fromJSON(object.fs_move) }
        : isSet(object.fsStat)
        ? { $case: "fsStat", fsStat: FsStatResponse.fromJSON(object.fsStat) }
        : isSet(object.fs_stat)
        ? { $case: "fsStat", fsStat: FsStatResponse.fromJSON(object.fs_stat) }
        : isSet(object.fsRemove)
        ? { $case: "fsRemove", fsRemove: FsRemoveResponse.fromJSON(object.fsRemove) }
        : isSet(object.fs_remove)
        ? { $case: "fsRemove", fsRemove: FsRemoveResponse.fromJSON(object.fs_remove) }
        : isSet(object.git)
        ? { $case: "git", git: GitResponse.fromJSON(object.git) }
        : isSet(object.ptyOpen)
        ? { $case: "ptyOpen", ptyOpen: PtyOpenResponse.fromJSON(object.ptyOpen) }
        : isSet(object.pty_open)
        ? { $case: "ptyOpen", ptyOpen: PtyOpenResponse.fromJSON(object.pty_open) }
        : isSet(object.ptyWrite)
        ? { $case: "ptyWrite", ptyWrite: PtyWriteResponse.fromJSON(object.ptyWrite) }
        : isSet(object.pty_write)
        ? { $case: "ptyWrite", ptyWrite: PtyWriteResponse.fromJSON(object.pty_write) }
        : isSet(object.ptyResize)
        ? { $case: "ptyResize", ptyResize: PtyResizeResponse.fromJSON(object.ptyResize) }
        : isSet(object.pty_resize)
        ? { $case: "ptyResize", ptyResize: PtyResizeResponse.fromJSON(object.pty_resize) }
        : isSet(object.ptyClose)
        ? { $case: "ptyClose", ptyClose: PtyCloseResponse.fromJSON(object.ptyClose) }
        : isSet(object.pty_close)
        ? { $case: "ptyClose", ptyClose: PtyCloseResponse.fromJSON(object.pty_close) }
        : isSet(object.desktopEnsure)
        ? { $case: "desktopEnsure", desktopEnsure: DesktopEnsureResponse.fromJSON(object.desktopEnsure) }
        : isSet(object.desktop_ensure)
        ? { $case: "desktopEnsure", desktopEnsure: DesktopEnsureResponse.fromJSON(object.desktop_ensure) }
        : isSet(object.metrics)
        ? { $case: "metrics", metrics: MetricsSample.fromJSON(object.metrics) }
        : isSet(object.updateMayProceed)
        ? { $case: "updateMayProceed", updateMayProceed: UpdateMayProceedResponse.fromJSON(object.updateMayProceed) }
        : isSet(object.update_may_proceed)
        ? { $case: "updateMayProceed", updateMayProceed: UpdateMayProceedResponse.fromJSON(object.update_may_proceed) }
        : isSet(object.desktopInput)
        ? { $case: "desktopInput", desktopInput: DesktopInputResponse.fromJSON(object.desktopInput) }
        : isSet(object.desktop_input)
        ? { $case: "desktopInput", desktopInput: DesktopInputResponse.fromJSON(object.desktop_input) }
        : isSet(object.desktopScreenshot)
        ? {
          $case: "desktopScreenshot",
          desktopScreenshot: DesktopScreenshotResponse.fromJSON(object.desktopScreenshot),
        }
        : isSet(object.desktop_screenshot)
        ? {
          $case: "desktopScreenshot",
          desktopScreenshot: DesktopScreenshotResponse.fromJSON(object.desktop_screenshot),
        }
        : undefined,
    };
  },

  toJSON(message: ControlResponse): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.error !== undefined) {
      obj.error = AgentError.toJSON(message.error);
    }
    if (message.result?.$case === "ping") {
      obj.ping = PingResponse.toJSON(message.result.ping);
    } else if (message.result?.$case === "hello") {
      obj.hello = HelloAck.toJSON(message.result.hello);
    } else if (message.result?.$case === "resume") {
      obj.resume = ResumeResponse.toJSON(message.result.resume);
    } else if (message.result?.$case === "exec") {
      obj.exec = ExecResponse.toJSON(message.result.exec);
    } else if (message.result?.$case === "fsRead") {
      obj.fsRead = FsReadResponse.toJSON(message.result.fsRead);
    } else if (message.result?.$case === "fsWrite") {
      obj.fsWrite = FsWriteResponse.toJSON(message.result.fsWrite);
    } else if (message.result?.$case === "fsList") {
      obj.fsList = FsListResponse.toJSON(message.result.fsList);
    } else if (message.result?.$case === "fsMkdir") {
      obj.fsMkdir = FsMkdirResponse.toJSON(message.result.fsMkdir);
    } else if (message.result?.$case === "fsMove") {
      obj.fsMove = FsMoveResponse.toJSON(message.result.fsMove);
    } else if (message.result?.$case === "fsStat") {
      obj.fsStat = FsStatResponse.toJSON(message.result.fsStat);
    } else if (message.result?.$case === "fsRemove") {
      obj.fsRemove = FsRemoveResponse.toJSON(message.result.fsRemove);
    } else if (message.result?.$case === "git") {
      obj.git = GitResponse.toJSON(message.result.git);
    } else if (message.result?.$case === "ptyOpen") {
      obj.ptyOpen = PtyOpenResponse.toJSON(message.result.ptyOpen);
    } else if (message.result?.$case === "ptyWrite") {
      obj.ptyWrite = PtyWriteResponse.toJSON(message.result.ptyWrite);
    } else if (message.result?.$case === "ptyResize") {
      obj.ptyResize = PtyResizeResponse.toJSON(message.result.ptyResize);
    } else if (message.result?.$case === "ptyClose") {
      obj.ptyClose = PtyCloseResponse.toJSON(message.result.ptyClose);
    } else if (message.result?.$case === "desktopEnsure") {
      obj.desktopEnsure = DesktopEnsureResponse.toJSON(message.result.desktopEnsure);
    } else if (message.result?.$case === "metrics") {
      obj.metrics = MetricsSample.toJSON(message.result.metrics);
    } else if (message.result?.$case === "updateMayProceed") {
      obj.updateMayProceed = UpdateMayProceedResponse.toJSON(message.result.updateMayProceed);
    } else if (message.result?.$case === "desktopInput") {
      obj.desktopInput = DesktopInputResponse.toJSON(message.result.desktopInput);
    } else if (message.result?.$case === "desktopScreenshot") {
      obj.desktopScreenshot = DesktopScreenshotResponse.toJSON(message.result.desktopScreenshot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ControlResponse>, I>>(base?: I): ControlResponse {
    return ControlResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlResponse>, I>>(object: I): ControlResponse {
    const message = createBaseControlResponse();
    message.requestId = object.requestId ?? "";
    message.error = (object.error !== undefined && object.error !== null)
      ? AgentError.fromPartial(object.error)
      : undefined;
    switch (object.result?.$case) {
      case "ping": {
        if (object.result?.ping !== undefined && object.result?.ping !== null) {
          message.result = { $case: "ping", ping: PingResponse.fromPartial(object.result.ping) };
        }
        break;
      }
      case "hello": {
        if (object.result?.hello !== undefined && object.result?.hello !== null) {
          message.result = { $case: "hello", hello: HelloAck.fromPartial(object.result.hello) };
        }
        break;
      }
      case "resume": {
        if (object.result?.resume !== undefined && object.result?.resume !== null) {
          message.result = { $case: "resume", resume: ResumeResponse.fromPartial(object.result.resume) };
        }
        break;
      }
      case "exec": {
        if (object.result?.exec !== undefined && object.result?.exec !== null) {
          message.result = { $case: "exec", exec: ExecResponse.fromPartial(object.result.exec) };
        }
        break;
      }
      case "fsRead": {
        if (object.result?.fsRead !== undefined && object.result?.fsRead !== null) {
          message.result = { $case: "fsRead", fsRead: FsReadResponse.fromPartial(object.result.fsRead) };
        }
        break;
      }
      case "fsWrite": {
        if (object.result?.fsWrite !== undefined && object.result?.fsWrite !== null) {
          message.result = { $case: "fsWrite", fsWrite: FsWriteResponse.fromPartial(object.result.fsWrite) };
        }
        break;
      }
      case "fsList": {
        if (object.result?.fsList !== undefined && object.result?.fsList !== null) {
          message.result = { $case: "fsList", fsList: FsListResponse.fromPartial(object.result.fsList) };
        }
        break;
      }
      case "fsMkdir": {
        if (object.result?.fsMkdir !== undefined && object.result?.fsMkdir !== null) {
          message.result = { $case: "fsMkdir", fsMkdir: FsMkdirResponse.fromPartial(object.result.fsMkdir) };
        }
        break;
      }
      case "fsMove": {
        if (object.result?.fsMove !== undefined && object.result?.fsMove !== null) {
          message.result = { $case: "fsMove", fsMove: FsMoveResponse.fromPartial(object.result.fsMove) };
        }
        break;
      }
      case "fsStat": {
        if (object.result?.fsStat !== undefined && object.result?.fsStat !== null) {
          message.result = { $case: "fsStat", fsStat: FsStatResponse.fromPartial(object.result.fsStat) };
        }
        break;
      }
      case "fsRemove": {
        if (object.result?.fsRemove !== undefined && object.result?.fsRemove !== null) {
          message.result = { $case: "fsRemove", fsRemove: FsRemoveResponse.fromPartial(object.result.fsRemove) };
        }
        break;
      }
      case "git": {
        if (object.result?.git !== undefined && object.result?.git !== null) {
          message.result = { $case: "git", git: GitResponse.fromPartial(object.result.git) };
        }
        break;
      }
      case "ptyOpen": {
        if (object.result?.ptyOpen !== undefined && object.result?.ptyOpen !== null) {
          message.result = { $case: "ptyOpen", ptyOpen: PtyOpenResponse.fromPartial(object.result.ptyOpen) };
        }
        break;
      }
      case "ptyWrite": {
        if (object.result?.ptyWrite !== undefined && object.result?.ptyWrite !== null) {
          message.result = { $case: "ptyWrite", ptyWrite: PtyWriteResponse.fromPartial(object.result.ptyWrite) };
        }
        break;
      }
      case "ptyResize": {
        if (object.result?.ptyResize !== undefined && object.result?.ptyResize !== null) {
          message.result = { $case: "ptyResize", ptyResize: PtyResizeResponse.fromPartial(object.result.ptyResize) };
        }
        break;
      }
      case "ptyClose": {
        if (object.result?.ptyClose !== undefined && object.result?.ptyClose !== null) {
          message.result = { $case: "ptyClose", ptyClose: PtyCloseResponse.fromPartial(object.result.ptyClose) };
        }
        break;
      }
      case "desktopEnsure": {
        if (object.result?.desktopEnsure !== undefined && object.result?.desktopEnsure !== null) {
          message.result = {
            $case: "desktopEnsure",
            desktopEnsure: DesktopEnsureResponse.fromPartial(object.result.desktopEnsure),
          };
        }
        break;
      }
      case "metrics": {
        if (object.result?.metrics !== undefined && object.result?.metrics !== null) {
          message.result = { $case: "metrics", metrics: MetricsSample.fromPartial(object.result.metrics) };
        }
        break;
      }
      case "updateMayProceed": {
        if (object.result?.updateMayProceed !== undefined && object.result?.updateMayProceed !== null) {
          message.result = {
            $case: "updateMayProceed",
            updateMayProceed: UpdateMayProceedResponse.fromPartial(object.result.updateMayProceed),
          };
        }
        break;
      }
      case "desktopInput": {
        if (object.result?.desktopInput !== undefined && object.result?.desktopInput !== null) {
          message.result = {
            $case: "desktopInput",
            desktopInput: DesktopInputResponse.fromPartial(object.result.desktopInput),
          };
        }
        break;
      }
      case "desktopScreenshot": {
        if (object.result?.desktopScreenshot !== undefined && object.result?.desktopScreenshot !== null) {
          message.result = {
            $case: "desktopScreenshot",
            desktopScreenshot: DesktopScreenshotResponse.fromPartial(object.result.desktopScreenshot),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseHelloAck(): HelloAck {
  return { epoch: 0, resumeToken: "", heartbeatIntervalMs: 0 };
}

export const HelloAck: MessageFns<HelloAck> = {
  encode(message: HelloAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.epoch !== 0) {
      writer.uint32(8).uint32(message.epoch);
    }
    if (message.resumeToken !== "") {
      writer.uint32(18).string(message.resumeToken);
    }
    if (message.heartbeatIntervalMs !== 0) {
      writer.uint32(24).uint32(message.heartbeatIntervalMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HelloAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHelloAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.epoch = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resumeToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.heartbeatIntervalMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HelloAck {
    return {
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      resumeToken: isSet(object.resumeToken)
        ? globalThis.String(object.resumeToken)
        : isSet(object.resume_token)
        ? globalThis.String(object.resume_token)
        : "",
      heartbeatIntervalMs: isSet(object.heartbeatIntervalMs)
        ? globalThis.Number(object.heartbeatIntervalMs)
        : isSet(object.heartbeat_interval_ms)
        ? globalThis.Number(object.heartbeat_interval_ms)
        : 0,
    };
  },

  toJSON(message: HelloAck): unknown {
    const obj: any = {};
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.resumeToken !== "") {
      obj.resumeToken = message.resumeToken;
    }
    if (message.heartbeatIntervalMs !== 0) {
      obj.heartbeatIntervalMs = Math.round(message.heartbeatIntervalMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HelloAck>, I>>(base?: I): HelloAck {
    return HelloAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HelloAck>, I>>(object: I): HelloAck {
    const message = createBaseHelloAck();
    message.epoch = object.epoch ?? 0;
    message.resumeToken = object.resumeToken ?? "";
    message.heartbeatIntervalMs = object.heartbeatIntervalMs ?? 0;
    return message;
  },
};

function createBaseAgentEvent(): AgentEvent {
  return { agentId: "", event: undefined };
}

export const AgentEvent: MessageFns<AgentEvent> = {
  encode(message: AgentEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentId !== "") {
      writer.uint32(10).string(message.agentId);
    }
    switch (message.event?.$case) {
      case "heartbeat":
        Heartbeat.encode(message.event.heartbeat, writer.uint32(82).fork()).join();
        break;
      case "goingOffline":
        GoingOffline.encode(message.event.goingOffline, writer.uint32(90).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.event = { $case: "heartbeat", heartbeat: Heartbeat.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.event = { $case: "goingOffline", goingOffline: GoingOffline.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentEvent {
    return {
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      event: isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: Heartbeat.fromJSON(object.heartbeat) }
        : isSet(object.goingOffline)
        ? { $case: "goingOffline", goingOffline: GoingOffline.fromJSON(object.goingOffline) }
        : isSet(object.going_offline)
        ? { $case: "goingOffline", goingOffline: GoingOffline.fromJSON(object.going_offline) }
        : undefined,
    };
  },

  toJSON(message: AgentEvent): unknown {
    const obj: any = {};
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.event?.$case === "heartbeat") {
      obj.heartbeat = Heartbeat.toJSON(message.event.heartbeat);
    } else if (message.event?.$case === "goingOffline") {
      obj.goingOffline = GoingOffline.toJSON(message.event.goingOffline);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentEvent>, I>>(base?: I): AgentEvent {
    return AgentEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentEvent>, I>>(object: I): AgentEvent {
    const message = createBaseAgentEvent();
    message.agentId = object.agentId ?? "";
    switch (object.event?.$case) {
      case "heartbeat": {
        if (object.event?.heartbeat !== undefined && object.event?.heartbeat !== null) {
          message.event = { $case: "heartbeat", heartbeat: Heartbeat.fromPartial(object.event.heartbeat) };
        }
        break;
      }
      case "goingOffline": {
        if (object.event?.goingOffline !== undefined && object.event?.goingOffline !== null) {
          message.event = { $case: "goingOffline", goingOffline: GoingOffline.fromPartial(object.event.goingOffline) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseAgentEventAck(): AgentEventAck {
  return { ack: undefined };
}

export const AgentEventAck: MessageFns<AgentEventAck> = {
  encode(message: AgentEventAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.ack?.$case) {
      case "heartbeat":
        HeartbeatAck.encode(message.ack.heartbeat, writer.uint32(82).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentEventAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentEventAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.ack = { $case: "heartbeat", heartbeat: HeartbeatAck.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentEventAck {
    return {
      ack: isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: HeartbeatAck.fromJSON(object.heartbeat) }
        : undefined,
    };
  },

  toJSON(message: AgentEventAck): unknown {
    const obj: any = {};
    if (message.ack?.$case === "heartbeat") {
      obj.heartbeat = HeartbeatAck.toJSON(message.ack.heartbeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentEventAck>, I>>(base?: I): AgentEventAck {
    return AgentEventAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentEventAck>, I>>(object: I): AgentEventAck {
    const message = createBaseAgentEventAck();
    switch (object.ack?.$case) {
      case "heartbeat": {
        if (object.ack?.heartbeat !== undefined && object.ack?.heartbeat !== null) {
          message.ack = { $case: "heartbeat", heartbeat: HeartbeatAck.fromPartial(object.ack.heartbeat) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseStreamChannel(): StreamChannel {
  return { channelId: "", workspaceId: "", agentId: "", kind: 0, port: 0 };
}

export const StreamChannel: MessageFns<StreamChannel> = {
  encode(message: StreamChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.agentId !== "") {
      writer.uint32(26).string(message.agentId);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.port !== 0) {
      writer.uint32(40).uint32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamChannel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamChannel {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
      agentId: isSet(object.agentId)
        ? globalThis.String(object.agentId)
        : isSet(object.agent_id)
        ? globalThis.String(object.agent_id)
        : "",
      kind: isSet(object.kind) ? streamKindFromJSON(object.kind) : 0,
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: StreamChannel): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    if (message.agentId !== "") {
      obj.agentId = message.agentId;
    }
    if (message.kind !== 0) {
      obj.kind = streamKindToJSON(message.kind);
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamChannel>, I>>(base?: I): StreamChannel {
    return StreamChannel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamChannel>, I>>(object: I): StreamChannel {
    const message = createBaseStreamChannel();
    message.channelId = object.channelId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.agentId = object.agentId ?? "";
    message.kind = object.kind ?? 0;
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseStreamOpen(): StreamOpen {
  return { channel: undefined, token: "", role: 0, resumeFromSeq: "0" };
}

export const StreamOpen: MessageFns<StreamOpen> = {
  encode(message: StreamOpen, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== undefined) {
      StreamChannel.encode(message.channel, writer.uint32(10).fork()).join();
    }
    if (message.token !== "") {
      writer.uint32(18).string(message.token);
    }
    if (message.role !== 0) {
      writer.uint32(24).int32(message.role);
    }
    if (message.resumeFromSeq !== "0") {
      writer.uint32(32).uint64(message.resumeFromSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamOpen {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamOpen();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channel = StreamChannel.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.resumeFromSeq = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamOpen {
    return {
      channel: isSet(object.channel) ? StreamChannel.fromJSON(object.channel) : undefined,
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      role: isSet(object.role) ? streamRoleFromJSON(object.role) : 0,
      resumeFromSeq: isSet(object.resumeFromSeq)
        ? globalThis.String(object.resumeFromSeq)
        : isSet(object.resume_from_seq)
        ? globalThis.String(object.resume_from_seq)
        : "0",
    };
  },

  toJSON(message: StreamOpen): unknown {
    const obj: any = {};
    if (message.channel !== undefined) {
      obj.channel = StreamChannel.toJSON(message.channel);
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.role !== 0) {
      obj.role = streamRoleToJSON(message.role);
    }
    if (message.resumeFromSeq !== "0") {
      obj.resumeFromSeq = message.resumeFromSeq;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamOpen>, I>>(base?: I): StreamOpen {
    return StreamOpen.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamOpen>, I>>(object: I): StreamOpen {
    const message = createBaseStreamOpen();
    message.channel = (object.channel !== undefined && object.channel !== null)
      ? StreamChannel.fromPartial(object.channel)
      : undefined;
    message.token = object.token ?? "";
    message.role = object.role ?? 0;
    message.resumeFromSeq = object.resumeFromSeq ?? "0";
    return message;
  },
};

function createBaseStreamOpenAck(): StreamOpenAck {
  return { accepted: false, error: undefined, resumeFromSeq: "0" };
}

export const StreamOpenAck: MessageFns<StreamOpenAck> = {
  encode(message: StreamOpenAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.error !== undefined) {
      AgentError.encode(message.error, writer.uint32(18).fork()).join();
    }
    if (message.resumeFromSeq !== "0") {
      writer.uint32(24).uint64(message.resumeFromSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamOpenAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamOpenAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AgentError.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.resumeFromSeq = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamOpenAck {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      error: isSet(object.error) ? AgentError.fromJSON(object.error) : undefined,
      resumeFromSeq: isSet(object.resumeFromSeq)
        ? globalThis.String(object.resumeFromSeq)
        : isSet(object.resume_from_seq)
        ? globalThis.String(object.resume_from_seq)
        : "0",
    };
  },

  toJSON(message: StreamOpenAck): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.error !== undefined) {
      obj.error = AgentError.toJSON(message.error);
    }
    if (message.resumeFromSeq !== "0") {
      obj.resumeFromSeq = message.resumeFromSeq;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamOpenAck>, I>>(base?: I): StreamOpenAck {
    return StreamOpenAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamOpenAck>, I>>(object: I): StreamOpenAck {
    const message = createBaseStreamOpenAck();
    message.accepted = object.accepted ?? false;
    message.error = (object.error !== undefined && object.error !== null)
      ? AgentError.fromPartial(object.error)
      : undefined;
    message.resumeFromSeq = object.resumeFromSeq ?? "0";
    return message;
  },
};

function createBaseStreamFrame(): StreamFrame {
  return { channelId: "", seq: "0", data: new Uint8Array(0), producedAtMs: "0" };
}

export const StreamFrame: MessageFns<StreamFrame> = {
  encode(message: StreamFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.seq !== "0") {
      writer.uint32(16).uint64(message.seq);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.producedAtMs !== "0") {
      writer.uint32(32).int64(message.producedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seq = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.producedAtMs = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamFrame {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      seq: isSet(object.seq) ? globalThis.String(object.seq) : "0",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      producedAtMs: isSet(object.producedAtMs)
        ? globalThis.String(object.producedAtMs)
        : isSet(object.produced_at_ms)
        ? globalThis.String(object.produced_at_ms)
        : "0",
    };
  },

  toJSON(message: StreamFrame): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.seq !== "0") {
      obj.seq = message.seq;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.producedAtMs !== "0") {
      obj.producedAtMs = message.producedAtMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamFrame>, I>>(base?: I): StreamFrame {
    return StreamFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamFrame>, I>>(object: I): StreamFrame {
    const message = createBaseStreamFrame();
    message.channelId = object.channelId ?? "";
    message.seq = object.seq ?? "0";
    message.data = object.data ?? new Uint8Array(0);
    message.producedAtMs = object.producedAtMs ?? "0";
    return message;
  },
};

function createBaseStreamClose(): StreamClose {
  return { channelId: "", reason: 0, message: "" };
}

export const StreamClose: MessageFns<StreamClose> = {
  encode(message: StreamClose, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.reason !== 0) {
      writer.uint32(16).int32(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamClose {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamClose();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamClose {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      reason: isSet(object.reason) ? streamCloseReasonFromJSON(object.reason) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: StreamClose): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.reason !== 0) {
      obj.reason = streamCloseReasonToJSON(message.reason);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamClose>, I>>(base?: I): StreamClose {
    return StreamClose.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamClose>, I>>(object: I): StreamClose {
    const message = createBaseStreamClose();
    message.channelId = object.channelId ?? "";
    message.reason = object.reason ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseDesktopInput(): DesktopInput {
  return { channelId: "", event: undefined };
}

export const DesktopInput: MessageFns<DesktopInput> = {
  encode(message: DesktopInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    switch (message.event?.$case) {
      case "pointer":
        PointerEvent.encode(message.event.pointer, writer.uint32(18).fork()).join();
        break;
      case "key":
        KeyEvent.encode(message.event.key, writer.uint32(26).fork()).join();
        break;
      case "scroll":
        ScrollEvent.encode(message.event.scroll, writer.uint32(34).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DesktopInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesktopInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = { $case: "pointer", pointer: PointerEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.event = { $case: "key", key: KeyEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.event = { $case: "scroll", scroll: ScrollEvent.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DesktopInput {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      event: isSet(object.pointer)
        ? { $case: "pointer", pointer: PointerEvent.fromJSON(object.pointer) }
        : isSet(object.key)
        ? { $case: "key", key: KeyEvent.fromJSON(object.key) }
        : isSet(object.scroll)
        ? { $case: "scroll", scroll: ScrollEvent.fromJSON(object.scroll) }
        : undefined,
    };
  },

  toJSON(message: DesktopInput): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.event?.$case === "pointer") {
      obj.pointer = PointerEvent.toJSON(message.event.pointer);
    } else if (message.event?.$case === "key") {
      obj.key = KeyEvent.toJSON(message.event.key);
    } else if (message.event?.$case === "scroll") {
      obj.scroll = ScrollEvent.toJSON(message.event.scroll);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DesktopInput>, I>>(base?: I): DesktopInput {
    return DesktopInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DesktopInput>, I>>(object: I): DesktopInput {
    const message = createBaseDesktopInput();
    message.channelId = object.channelId ?? "";
    switch (object.event?.$case) {
      case "pointer": {
        if (object.event?.pointer !== undefined && object.event?.pointer !== null) {
          message.event = { $case: "pointer", pointer: PointerEvent.fromPartial(object.event.pointer) };
        }
        break;
      }
      case "key": {
        if (object.event?.key !== undefined && object.event?.key !== null) {
          message.event = { $case: "key", key: KeyEvent.fromPartial(object.event.key) };
        }
        break;
      }
      case "scroll": {
        if (object.event?.scroll !== undefined && object.event?.scroll !== null) {
          message.event = { $case: "scroll", scroll: ScrollEvent.fromPartial(object.event.scroll) };
        }
        break;
      }
    }
    return message;
  },
};

function createBasePointerEvent(): PointerEvent {
  return { x: 0, y: 0, action: 0, button: 0 };
}

export const PointerEvent: MessageFns<PointerEvent> = {
  encode(message: PointerEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(8).int32(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(16).int32(message.y);
    }
    if (message.action !== 0) {
      writer.uint32(24).int32(message.action);
    }
    if (message.button !== 0) {
      writer.uint32(32).int32(message.button);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PointerEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePointerEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.x = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.y = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.button = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PointerEvent {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      action: isSet(object.action) ? pointerActionFromJSON(object.action) : 0,
      button: isSet(object.button) ? pointerButtonFromJSON(object.button) : 0,
    };
  },

  toJSON(message: PointerEvent): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== 0) {
      obj.y = Math.round(message.y);
    }
    if (message.action !== 0) {
      obj.action = pointerActionToJSON(message.action);
    }
    if (message.button !== 0) {
      obj.button = pointerButtonToJSON(message.button);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PointerEvent>, I>>(base?: I): PointerEvent {
    return PointerEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PointerEvent>, I>>(object: I): PointerEvent {
    const message = createBasePointerEvent();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.action = object.action ?? 0;
    message.button = object.button ?? 0;
    return message;
  },
};

function createBaseKeyEvent(): KeyEvent {
  return { key: "", isText: false, action: 0 };
}

export const KeyEvent: MessageFns<KeyEvent> = {
  encode(message: KeyEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.isText !== false) {
      writer.uint32(16).bool(message.isText);
    }
    if (message.action !== 0) {
      writer.uint32(24).int32(message.action);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isText = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyEvent {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      isText: isSet(object.isText)
        ? globalThis.Boolean(object.isText)
        : isSet(object.is_text)
        ? globalThis.Boolean(object.is_text)
        : false,
      action: isSet(object.action) ? keyActionFromJSON(object.action) : 0,
    };
  },

  toJSON(message: KeyEvent): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.isText !== false) {
      obj.isText = message.isText;
    }
    if (message.action !== 0) {
      obj.action = keyActionToJSON(message.action);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyEvent>, I>>(base?: I): KeyEvent {
    return KeyEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyEvent>, I>>(object: I): KeyEvent {
    const message = createBaseKeyEvent();
    message.key = object.key ?? "";
    message.isText = object.isText ?? false;
    message.action = object.action ?? 0;
    return message;
  },
};

function createBaseScrollEvent(): ScrollEvent {
  return { x: 0, y: 0, deltaX: 0, deltaY: 0 };
}

export const ScrollEvent: MessageFns<ScrollEvent> = {
  encode(message: ScrollEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(8).int32(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(16).int32(message.y);
    }
    if (message.deltaX !== 0) {
      writer.uint32(24).int32(message.deltaX);
    }
    if (message.deltaY !== 0) {
      writer.uint32(32).int32(message.deltaY);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScrollEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScrollEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.x = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.y = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deltaX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deltaY = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScrollEvent {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      deltaX: isSet(object.deltaX)
        ? globalThis.Number(object.deltaX)
        : isSet(object.delta_x)
        ? globalThis.Number(object.delta_x)
        : 0,
      deltaY: isSet(object.deltaY)
        ? globalThis.Number(object.deltaY)
        : isSet(object.delta_y)
        ? globalThis.Number(object.delta_y)
        : 0,
    };
  },

  toJSON(message: ScrollEvent): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== 0) {
      obj.y = Math.round(message.y);
    }
    if (message.deltaX !== 0) {
      obj.deltaX = Math.round(message.deltaX);
    }
    if (message.deltaY !== 0) {
      obj.deltaY = Math.round(message.deltaY);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScrollEvent>, I>>(base?: I): ScrollEvent {
    return ScrollEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScrollEvent>, I>>(object: I): ScrollEvent {
    const message = createBaseScrollEvent();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.deltaX = object.deltaX ?? 0;
    message.deltaY = object.deltaY ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
